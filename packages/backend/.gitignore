# Editorial blog posts are generated from packages/web by scripts/syncBlogPosts.js
# (runs on dev/build). Single source of truth lives in the web package.
src/content/posts/

# Google Analytics service account key (never commit)
ga-service-account.json

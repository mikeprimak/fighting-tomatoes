# HANDOFF — 2026-04-25 — Android Google Sign-In DEVELOPER_ERROR

**Read this top-to-bottom before doing anything.** Investigation is mid-flight; user is blocked from logging into his own production app on Android. Previous session window got slow, this is a fresh-window resume.

## TL;DR

- User can't log into the **production Android app (Play Store, 2.0.2 / versionCode 36)** via Google Sign-In. Email login works.
- Sentry confirms `DEVELOPER_ERROR` from `@react-native-google-signin/google-signin`. First seen ~5 days ago. Tag `feature: google-signin`.
- Backend is fine. iOS sign-in is fine. Other Android users are also affected on cold logins (they're staying in via JWT refresh until token expiry).
- Root cause: the 4/18 fix flipped mobile `webClientId` from project `fight-app-ba5cd` (`1082468109842-...`) to `good-fights-app` (`499367908516-...`). The new project has iOS + Web OAuth clients but **no Android OAuth client with the Play Store signing SHA-1**. Android Play Services rejects the sign-in attempt before any network call.
- **No code change has been made this session.** User is in investigation phase, not implementation. Don't commit, don't push, don't build.

## What's been ruled out

Don't redo this work — it's done:

1. **Not a recent code change.** Four parallel Claude windows audited their own and each other's branches; no auth-related changes in the past 5 days. Last auth-touching commits are `207c86c` (4/19) and `d40c60c` (4/14).
2. **Not Render env corruption.** User confirmed `GOOGLE_CLIENT_ID` on Render is correctly set to a 3-value comma-separated list:
   ```
   499367908516-f5qu2rjeot6iqnhld7o3tg71tqdqlngk.apps.googleusercontent.com,499367908516-j03poule51s7sfvpvdufna0upqa3oseg.apps.googleusercontent.com,1082468109842-pehb7kkuclbv8g4acjba9eeeajprd8j7.apps.googleusercontent.com
   ```
3. **Not stale Render deploy.** Render is on commit `60bd71b` (well past `207c86c`'s comma-split parser).
4. **Not a network/health issue.** `/health` returns 200. `logs.txt` shows other iOS users (`GoodFights/19 CFNetwork/...`) actively refreshing tokens and fetching profiles right now.
5. **Not the backend rejecting tokens.** `POST /api/auth/google` is **never seen in Render logs** during the user's failed attempts — confirming the failure is client-side, before the network request.

## What changed on 4/18 (the actual cause)

Read `docs/daily/2026-04-18.md` lines 152–209 ("iOS Google Sign-In crash — root-caused + new builds shipped") for full context. Summary:

- Pre-4/18: `useGoogleAuth.ts` had `1082468109842-...` IDs (project `fight-app-ba5cd`). Render `GOOGLE_CLIENT_ID` was **unset**, so backend audience validation was silently skipped (`audience: undefined`). Android sign-in worked because `fight-app-ba5cd` had a correct Android OAuth client.
- 4/18 fix flipped mobile to `499367908516-...` (`good-fights-app`) to resolve an iOS native crash from a URL-scheme/client-ID mismatch. Also turned on backend audience validation for the first time and populated Render env.
- Side effect: `good-fights-app` does not have an Android OAuth client with the Play Store signing SHA-1, so Android Google Sign-In has been broken since versionCode 36 rolled out (~4/19-20).

**Mobile-side change in `useGoogleAuth.ts` was uncommitted on 4/18 and is still uncommitted.** The shipped 2.0.2 binaries were built from working tree, not HEAD. HEAD currently has the OLD `1082468109842-...` IDs.

**Earlier in this session I (the previous window) accidentally `git checkout`'d `useGoogleAuth.ts` to HEAD.** The user paused before I could re-apply the working-tree state. Working tree currently matches HEAD (old IDs). This doesn't affect the production bug — production runs the binary, not local files — but **be aware**: if a dev build is started, it'll use the wrong (old) IDs, and dev Google Sign-In will fail. Do NOT proactively re-apply the diff without user approval.

## Why the user only got logged out today

JWTs. Access token = 15 min, refresh token = 7 days, rotating. As long as the refresh chain stayed alive, no Google Sign-In was needed. Today the user's refresh token expired (or the chain broke), kicking him to the login screen — his first personal encounter with the 5-day-old bug.

## Two paths forward (user knows about both, hasn't picked yet)

### Path A: GCP fix (zero code changes — user's preference)

Add an Android OAuth 2.0 Client ID to the `good-fights-app` GCP project (`499367908516`) with:
- **Package name**: `com.fightcrewapp.mobile`
- **SHA-1 fingerprint**: from Play Console → app → Setup → App integrity → **App signing key certificate** (NOT the upload key — Play re-signs every AAB with its own app-signing key, that's what's on the device)
- SHA-256 optional, doesn't hurt to include

**Current blocker on this path**: when the user tried to save, GCP returned:
```
Save failed
Requested entity already exists
Tracking number: c7797109035934461
```

This usually means the same package+SHA-1 combo is already registered on a different OAuth client in the same project, OR the user is creating a duplicate of an existing client. **The user has not yet listed the existing Android OAuth clients in `good-fights-app`.** That's the next step on this path. Ask him:
> In `good-fights-app` (project `499367908516`) → APIs & Services → Credentials, how many "Android" OAuth 2.0 Client IDs are listed? For each, what package name and SHA-1?

If a matching client already exists with the wrong SHA-1, edit it. If a duplicate exists, delete one. If somehow the SHA-1 is registered against a different package, fix that.

### Path B: OTA fix (one-line JS edit, no GCP changes)

Edit `packages/mobile/hooks/useGoogleAuth.ts` to set:
```ts
const GOOGLE_CLIENT_ID_WEB = '1082468109842-pehb7kkuclbv8g4acjba9eeeajprd8j7.apps.googleusercontent.com';  // old project, has Android OAuth client + correct SHA-1
const GOOGLE_CLIENT_ID_IOS = '499367908516-j03poule51s7sfvpvdufna0upqa3oseg.apps.googleusercontent.com';   // new project, matches iOS URL scheme in app.json
```

Then `cd packages/mobile && eas update --branch production --message "Fix Android Google Sign-In: webClientId back to fight-app-ba5cd"`.

Why this works:
- Android: `webClientId` matches `fight-app-ba5cd`, which has the correctly-configured Android OAuth client. DEVELOPER_ERROR resolved.
- iOS: `iosClientId` stays at `499367908516-...` matching the URL scheme in `app.json`. iOS crash stays fixed because iOS uses `iosClientId` for sign-in, not `webClientId` (webClientId on iOS is only used for offline-access server tokens).
- Backend: tokens minted by either platform have audiences (`1082468109842-pehb...` for Android, `499367908516-j03p...` for iOS) that are both already in Render's `GOOGLE_CLIENT_ID` env list.
- No new EAS build needed. Update lands on next app open.

**Don't run `eas update` without explicit user approval.** Per CLAUDE.md, build credits are limited; per memory `feedback_ship_full_pipeline`, finishing means commit + push + ship — but the user is investigating, not yet shipping. Wait for go.

## Test creds (in case relevant)

From CLAUDE.md:
- `test@goodfights.app` / `Testpass1!`
- `applereview@goodfights.app` / `AppleTest2026!`

User confirmed email login works on the affected device.

## Reference data

- **Sentry event**: tag `feature: google-signin`, error `DEVELOPER_ERROR: Follow troubleshooting instructions at https://react-native-google-signin.github.io/docs/troubleshooting`. First seen ~5 days ago. Device: Pixel 6a, Android 16, build `2.0.2+19`, dist `36`, environment `production`.
- **Render service**: `fightcrewapp-backend.onrender.com`, currently on commit `60bd71b`.
- **Mobile google-services.json** at `packages/mobile/google-services.json` and `packages/mobile/android/app/google-services.json` both still say `project_number: 1082468109842` (old project). This is consistent with Path B's assumption that the bundled native config is for the old project. Path A doesn't require regenerating these, but it's a future cleanup.
- **Output of investigation reports** from the four parallel windows: `output.txt` (line 1 has the verified Render env value).
- **Render log dump from earlier today**: `logs.txt`. Shows iOS users actively refreshing tokens; no `/api/auth/google` POSTs during the user's failed attempts.
- **Current store versions** (CLAUDE.md): Android Play Store 2.0.2 / versionCode 36 (in Google review, manually uploaded 2026-04-19). iOS App Store 2.0.2 / build 19 (live since 2026-04-19).

## What NOT to do

- **Don't `git commit` or `git push` anything.** User is investigating.
- **Don't `eas build`.** Per CLAUDE.md, build credits are limited and explicit approval required.
- **Don't `eas update` without explicit approval.** Even though it's an OTA, it's still a production change.
- **Don't run any `git checkout`, `git restore`, or `git reset`** on `useGoogleAuth.ts` — the prior window already shuffled it once. Leave it at HEAD.
- **Don't rewrite `google-services.json`.** Even if you think it's wrong, it's load-bearing for the shipped binary; changing it requires a rebuild.
- **Don't propose Path A vs Path B as a menu** — per memory `feedback_ops_choices_no_menus`, lead with one recommendation. User is currently leaning Path A but blocked on the "already exists" GCP error. Stay on that thread unless the user pivots.

## Immediate next action

Ask the user the question at the bottom of the Path A section: how many Android OAuth clients exist in the `good-fights-app` GCP project, and what are their package names + SHA-1s. That answer will unblock the "Requested entity already exists" error.

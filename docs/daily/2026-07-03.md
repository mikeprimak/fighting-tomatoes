# 2026-07-03

## Blog: Ben Askren comeback feature published

- Published `askren-comeback-feature.md` (root draft) as a blog post: **"Ben Askren Returns to Competition a Year After Lung Transplant"** → https://goodfights.app/blog/ben-askren-comeback-lung-transplant
- Post file: `packages/web/src/content/posts/2026-07-03-ben-askren-comeback-lung-transplant.md` (tags: ben-askren, belal-muhammad, raf, wrestling, feature)
- Hero image: RAF 11 Askren vs Muhammad poster at `packages/web/public/blog/ben-askren-hero.jpg`, referenced as `/blog/ben-askren-hero.jpg`. First cut was a low-res Google thumbnail (399×501); replaced same-session with the high-res poster Mike dropped in root (`askren-return-raf-11.png`, 3580×3563 / 13 MB), downscaled via sharp to 1600×1592 JPEG q82 (414 KB) — commit `79c2c48f`
- Post commit `1ef11045` (rebased over the daily odds-bot commit `060f018a`); Vercel auto-deploy verified live
- Root files `askren-comeback-feature.md` + `askren-return-raf-11.png` left in place (untracked)
- Added closing CTA linking to the RAF 11 event page (`goodfights.app/events/raf11`) so readers can rate their hype — matches the house style (italic footer + "Never miss a Good Fight.") — commit `425177f6`
- Note: post is timely for RAF 11 (July 18) — if it should be surfaced on the web hero / mobile "Latest", add the slug to SystemConfig `blog_highlights` via admin.html (admin-controlled, not automatic)

## SEO: prod indexing verified + Search Console API access wired

Follow-up to the 2026-07-01 programmatic SEO steps 1–6 (`docs/plans/programmatic-seo-2026-07-01.md`).

**Prod verification (all healthy):**
- `robots.txt` lists all 4 sitemaps; root sitemap has hubs + all 18 `/fights/best/[year]` URLs (the step-6 build-race concern self-healed)
- Child sitemaps live: 1,130 fighter/division + 618 event + 3,881 fight URLs (~5.6k total)
- Page signals spot-checked on prod: canonical slug URL, `SportsEvent`+`AggregateRating` JSON-LD, semantic `<h1>`, thin pages emit `noindex, follow`

**Search Console:**
- All 4 sitemaps were **already submitted** (Mike, 2026-07-02) — Google downloaded each within the last day, 0 errors/warnings, submitted counts match prod exactly. Indexed counts still 0 (normal at T+2 days).
- **New durable API access:** Mike enabled the Search Console API on GCP project `fight-app-ba5cd` and added `ga-reader@fight-app-ba5cd.iam.gserviceaccount.com` as a Full user on the `https://goodfights.app/` URL-prefix property. Same key file as `ga.js`.
- **New script `packages/backend/scripts/gsc.js`** — `sites | sitemaps | submit <url> | query [days] [dimension]`. Run from `packages/backend`.
- Recurring check added to `docs/operations/maintenance.md` (biweekly during rollout, then monthly).

**Baseline (7d search analytics, 2026-06-26 → 07-03):** blog dominates — how-to-watch UFC 329 post 10,642 impressions / 56 clicks (pos 6.9), McGregor–Holloway news post 12,961 impr / 18 clicks. Home 5,028 impr / 42 clicks. Event pages still surface as UUID URLs (308s will migrate them as Google recrawls). First slug-page impressions already: `/fights/stefan-struve-vs-jared-rosholt`, `/fighters` hub.

## SEO step 7 shipped: long-form fight previews + fighter facts (session 2)

Closes the last build item in `docs/plans/programmatic-seo-2026-07-01.md` (all 7 steps now ✅). GSC checked at session start: sitemaps healthy, indexed=0 (normal at T+2 days), analytics match yesterday's baseline — nothing to act on.

**(a) Long-form `aiPreview` generation is ON:**
- `extractFightEnrichment.ts`: per-fight `preview` field (2-3 grounded paragraphs, "Preview rules" in the system prompt: editorial-grounded facts + style-inference analysis, pre-fight tense, omit-when-thin, no em dashes); `MAX_TOKENS` 4096→16384 (a truncated JSON response would drop the whole event); `stripDashes` now also applied to `whyCare`.
- `persist.ts`: writes `aiPreview` keep-if-absent — a thin T-2 pass never wipes a grounded T-10/T-5 preview. Cron only touches UPCOMING fights, so the Opus-authored legacy `aiPreview` rows (COMPLETED) are untouchable by it.
- Web `FightDetailClient` "The Story": renders multi-paragraph `aiPreview` (SSR via initial render), falls back to `aiPreviewShort`, same 0.5 confidence gate.
- Validated end-to-end with a synthetic-card live Haiku call: grounded 2-paragraph preview for the covered fight, namedropped prelim correctly omitted, ~500 output tokens (couple cents/event extra — nowhere near the $300/yr ceiling). NOT the de-scoped article-length mass generation: preview prose on existing gated pages only.
- Local gotcha: running the enrichment runner via tsx on Windows fails in the ufc.com Puppeteer path with `__name is not defined` (esbuild keep-names serialization). Prod GH Actions cron is unaffected (UFC 329 enriched fine 06-30).

**(b) Fighter facts (nationality / height / reach / stance / dateOfBirth):**
- Migration `20260703000000_add_fighter_physical_facts` — 5 nullable columns, hand-authored SQL, applied with `migrate deploy` (no shadow DB). Deployed to prod this session.
- `scrapeUFCStatsFighters.ts` now captures Ht./Reach/Stance from the directory table (was parsed & discarded); new `src/scripts/backfillFighterPhysicals.ts` (conservative name-match like the records backfill, fill-only) — **applied: 3,156 fighters filled** (audit: `prisma/fighter-physicals-backfill.json`). Quarterly re-run added to maintenance.md next to the record refresh.
- Fighter-profile cron: extractor emits a grounded `facts` object (nationality, DOB, height/reach/stance; null unless in sources, DOB regex-guarded); `persistFighterFacts` writes FILL-ONLY (scraped values always win; populated columns never overwritten) and independent of the 0.5 profile floor. Hand-authored write path passes empty facts.
- API `/api/fighters/:id`: selects + returns the 5 fields (replaced the vestigial team/birthDate/reach-as-number schema entries).
- Web fighter page: Person JSON-LD gains `nationality` (Country), `birthDate`, `height`; new visible facts strip (Country · Height · Reach · Stance · Born) so the structured data mirrors on-page content.

**Deploys:** backend (Render) + web (Vercel) auto-deploy from this push. Fighter facts appear on fighter pages as ISR revalidates; previews start landing with the next fight-enrichment cron runs (T-10/T-5/T-2 windows).

**Watch next session:** `node scripts/gsc.js sitemaps` indexing counts; spot-check an upcoming main event's fight page for a rendered "The Story" long-form preview after the 14:00 UTC cron; fighter pages for the facts strip.

---

# 2026-07-03 (remote-control) — Pivot evaluated: CONTINUE; branch synced with main

## Pivot evaluation (Mike asked: continue or abandon?)

Full read of `identity-platform.md`, `sale-value.md`, the pivot branch (51 commits),
and the onboarding screens. **Verdict: continue.** Core logic: authority/aggregator
needs volume Good Fights doesn't have (a fight with 4 ratings isn't an authority);
identity works at n=1 — the only thesis that delivers value at ~200 actives. It's
also low-regret (data keeps accumulating either way) and not either/or: **the SEO/
editorial push on main IS the authority play — authority is the marketing, identity
is the product.** Risks named: fights ≠ movies (slower diary deposits), everything
validated on a sample of one (Mike), the day-2 promise gap (onboarding says "the app
knows you", then lands on the old content feed — the home mirror, objective #1, is
unbuilt), and the branch was rotting (214 behind main, stale since 2026-06-15).
Release gate to measure before shipping: % of fresh accounts getting ≥3 insights
after the 10-classic stack.

## Branch sync (the git-mess fix) — DONE

Mike: keep building, ship nothing. So: merged `main` INTO the branch (branch never
deploys; main untouched, no OTA, no prod publish).

- **Pre-merge cleanup of the pivot worktree:** committed stranded uncommitted work
  (`c81ff6de`: rewarding-users identity link + controversy-polls item, NEW
  `docs/areas/casual-fan-appeal.md` brainstorm, daily 2026-06-02 video-marketing +
  2026-06-13 Scrapfly logs, maintenance VPS pre-flight, backend .gitignore
  ga-service-account). Restored accidental deletions (live-event-data JSONs, BKFC
  scripts — still on main). Discarded a next.config.ts copy identical to main.
  Parked an older untracked `scripts/ga.js` draft in the session scratchpad (main's
  173-line version is newer).
- **Merge `1b1c5b2f`** (main → branch): 10 conflicts. Taken from main (newer):
  tapologyBrowser.js/.d.ts (501-line CapSolver evolution vs branch's 159-line
  Scrapfly draft), both 2026-05-30 blog posts, blog/[slug]/page.tsx (BlogArticle
  refactor already contains the branch's Updated-date), WeekendEventsSection +
  home.tsx Event-Last-Night hunks (main added lastNightTitle "Earlier Today").
  Union-merged (both sides kept): routes/index.ts (onboarding routes + main's
  sitemap/SEO), CLAUDE.md Next Session, daily 2026-06-09 (pivot session + video
  session).
- **Post-merge fix `42496753`:** home.tsx had `pastEventsData` declared twice
  (both sides added the same query; auto-merge kept both) — deduped.
- **Gates:** backend tsc --noEmit = 0; mobile = 81 errors, all pre-existing
  baseline (~82), none in touched files. Pushed to origin branch only.
- **State:** branch now 54 ahead / **0 behind** main. Keep syncing main in weekly
  while Phase 1 continues.

## Next (per evaluation)

1. **Home-screen mirror (Phase 1 objective #1)** — the biggest thesis-to-product
   gap; onboarding's promise has no day-2 surface without it.
2. Device-walk onboarding on a fresh account; measure the ≥3-insights rate.
3. Welcome screen: cut 5 beats to 3; add the contrast line ("Other apps tell you
   what fans think. Good Fights tells you what kind of fan you are").

---

## Home mirror BUILT (Phase 1 objective #1 — branch-only)

The identity dashboard above the fold, per the locked 2026-06-09 model.

- **Backend `GET /api/home/mirror`** (`src/routes/homeMirror.ts`, registered
  under `/api/home`): the urgency rail — `liveEvents` / `todayEvents` (events
  with a hyped fight or followed fighter on the card) + `pinnedFights` (hyped /
  followed fights, next ~week, cap 10, followed-first inside each day). One
  card per fight with hype AND follow names merged. Spoiler-safe by
  construction (UPCOMING/LIVE only, no result fields). Day semantics = UTC
  day of Event.date (home-feed convention); mainStartTime rides along for
  display. Gotchas hit: nested to-one relation filters needed `is:` wrapping,
  and the Event status field is **`eventStatus`**, not `status`.
- **Rotating rail** reuses `GET /api/fan-dna/taste-profile` with a **daily
  salt** (`?salt=YYYY-MM-DD`, param already existed) so insights change every
  day; insight #1 doubles as the greeting's rotating identity line.
- **Mobile `components/HomeMirror.tsx`**, rendered at the top of Home for
  signed-in users (logged-out = content-first, unchanged): "Welcome back,
  {firstName}" + identity line, LIVE NOW card (red border), TONIGHT card
  (main-card time), pinned fight rows with 🔥hype / ★Following badges,
  horizontal rail of 3 daily insight cards → taps into fan-dna screen.
  Silence > filler: empty profile renders just the greeting.
- **Verified live** (local backend, prod DB, testdev+onb0612): mirror returned
  the McGregor–Holloway UFC 329 pin off the account's real follows; hype-merge
  path exercised via real API (hype 9 → badge; deleted after; aggregates
  restored to 20 predictions / 9.8 avg; account left unverified as intended).
  Backend tsc 0; mobile 81 = baseline. NOT device-walked yet.
- **Follow-up found:** DELETE `/fights/:id/prediction` still carries the hard
  email-verification gate while CREATE is soft-capped — an unverified user can
  hype but can't un-hype. Decide + fix in the next branch session.
- **Env note:** the pivot worktree now has its own `packages/backend/.env`
  (copied from the main worktree). The dev server itself never loads dotenv —
  run it with the env sourced (`set -a; source <(tr -d '\r' < .env); set +a`).

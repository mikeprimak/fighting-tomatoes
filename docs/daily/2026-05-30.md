# 2026-05-30

## Blog: published McGregor vs Holloway 2 (UFC 329) SEO preview

Wrote and shipped a long-form, SEO-optimized preview for the just-announced
**Conor McGregor vs Max Holloway 2** welterweight main event at **UFC 329** (Sat
Jul 11, 2026, T-Mobile Arena, on Paramount+ in the US).

- **Format:** 27 question-headed H2 sections (one or two paragraphs each) to capture
  "People Also Ask" / featured-snippet / long-tail query traffic. Clusters: how-to-watch,
  when/where, full card, weight class + title status, 2013 first-fight history (McGregor
  won on a torn ACL), tale of the tape, odds, prediction, stakes, tickets, trilogy, plus a
  quick-answers recap. Light app CTA at top + bottom only.
- **Broadcast research:** US = Paramount+ (no separate PPV, sub-included). Researched and
  wrote per-country section reconciled against `seed-broadcast-channels.ts`: Canada =
  Sportsnet+ **still PPV** (~C$60); UK/Ireland = TNT Sports + discovery+ (early hrs Sun Jul 12);
  Australia = Main Event PPV on Kayo/Foxtel, prelims on Paramount+/Network 10; Europe = DAZN
  in most markets (DE/AT/IT/ES/PT), Fight Pass fallback. **Note:** current reporting contradicts
  the May-3 internal research doc's hypothesis that UK/AU moved fully to Paramount+ — they did
  not. Consider updating `docs/plans/how-to-watch-broadcaster-research-2026-05-03.md`.
- **In-page anchor:** blog renders markdown via `marked`, which does NOT auto-slug headings.
  Used an explicit `<a id="watch-other-countries">` (passes through since the page uses
  `dangerouslySetInnerHTML`, no sanitizer) so the top section's "viewers outside the US" link
  scrolls correctly.
- **Image:** hero `packages/web/public/blog/mcgregor-holloway-hero.jpg` (portrait 960x1200;
  rendered in a 16:9 `object-cover` frame so it center-crops).
- **Shipped:** commit `9e23a3b`, `draft: false`, pushed to `main` → Vercel auto-deploys
  `packages/web`. URL: `/blog/mcgregor-holloway-2-ufc-329`. Facts pulled via web search;
  Mike to eyeball live.

### SEO campaign (full push on the McGregor/Holloway article)

Turned the single post into a real SEO play. All shipped to `main`:

- **On-page technical SEO (reusable infra, commit `7eaf439` + `07d5671`):**
  - JSON-LD on every blog post: `BlogPosting` + `BreadcrumbList` + `FAQPage` (auto-extracted
    from `## ...?` headings, 24 Q&As on the hub) + `SportsEvent` (new optional `event:`
    frontmatter block). Code in `posts.ts` + `blog/[slug]/page.tsx`.
  - Explicit canonical, sitemap already includes posts, robots allows crawl.
  - Pinned hub to homepage hero (`featured: true`) for an internal link.
- **Search Console + Bing:** verified goodfights.app in GSC via meta tag in `layout.tsx`
  (commit `841829c`); submitted sitemap; Requested Indexing on the hub URL. Imported to Bing
  Webmaster + requested indexing there too.
- **Content cluster (commit `07d5671`):** two supporting articles
  (`max-holloway-career-explained`, `conor-mcgregor-return-five-years`), bidirectionally
  interlinked with the hub for topical authority. Fixed BMF accuracy on hub (Holloway lost the
  belt to Oliveira at UFC 326, now *former* BMF champ).
- **Social:** posted launch tweet from @GoodFightsApp (Option C hot-take, UTM-tagged). Reddit
  deferred to fight week (r/MMA kills self-promo).
- **Freshness routine:** `/schedule` remote agent `trig_01HygAsL5C12Sxa7Rnmn6s8W`, weekly Sat
  15:00 UTC through fight day (Jul 11), web-searches odds/news and refreshes the cluster.
  **Disable after the fight** at claude.ai/code/routines.
- **Playbook written:** `docs/playbooks/seo-fight-preview-cluster.md` documents the entire
  repeatable process. **Next planned application: Topuria vs Gaethje, UFC Freedom 250, Jun 14**
  (compressed timing path; do not start until Mike says go).
- **CLAUDE.md fixed:** web prod URL corrected to goodfights.app (commit `7b80c54`);
  web-jet-gamma-12.vercel.app flagged as dead dev.

## 2.1.0 store submissions — BOTH SUBMITTED & IN REVIEW

Both 2.1.0 builds from yesterday finished clean and were submitted today. **iOS and
Android are now both in review.**

- **iOS** — build `e2f2afda` (2.1.0 / buildNumber 19) uploaded to App Store Connect
  via `eas submit -p ios`. (Note: the earlier `65956717` build **errored** on the
  Sentry dSYM upload; the rebuild with the `@sentry/cli` devDep fix, commit `422f0c4`,
  is the one that shipped.) Created a new 2.1.0 version in App Store Connect, attached
  build 19, export compliance answered, **submitted → in review.**
- **Android** — first upload of build `3aec3d8c` (vc 35) **failed** in Play Console:
  *"You can't roll out this release because it doesn't allow any existing users to
  upgrade... none of the users of this APK will be able to upgrade."* **Root cause:**
  production was already on **versionCode 36** (the 2026-04-19 "Fix iOS Google Sign-In
  crash" build), not 34 as the stale CLAUDE.md table claimed. Play requires each new
  bundle's versionCode to be strictly greater than anything previously uploaded, and
  35 < 36. **Fix:** bumped `android/app/build.gradle` → **versionCode 37**, rebuilt
  (build `543a310d`, vc 37, `.aab` = `bpf7CRpJFcg2Pv56BiNdL9.aab`). 37 > 36 → passes.
  CLAUDE.md store-versions table corrected (was "as of Feb 27"; now reflects vc 36 live
  / vc 37 incoming + a note to always check the live Play versionCode before bumping).
  (`eas submit` still 403s on the service account → manual `.aab` upload as always.)
  The deobfuscation/R8-mapping warning on the bundle is harmless and ignored.
  Final: vc37 `.aab` uploaded to Play Console Production, **submitted → in review.**

## Home screen is live for real users (main is current)

Confirmed the Home-tab release is fully real-user-ready, not just working in Expo dev:

- **Backend endpoints were already on `main` (merged yesterday) and are live in prod.**
  Probed `https://fightcrewapp-backend.onrender.com`: `/api/editorial`,
  `/api/community/hot-fighters`, `/api/community/recently-booked-fighters` all **200**.
  So the "empty in prod" caveat from 2026-05-29 is resolved — Render has served these
  since yesterday's merge.
- **Mobile Home tab UI** ships in the 2.1.0 store builds (in review). Once approved,
  updating users get a populated Home screen.
- **`main` fast-forwarded** from `422f0c4` → `66cc711` (today's release commit:
  build.gradle vc37, CLAUDE.md store-table fix, `ensureAppleReviewUser.ts`, this doc).
  Clean fast-forward, no divergence. `home-screen` and `main` are now in sync at
  `66cc711`. Pushing to main may trigger a Render redeploy (touched `packages/backend`);
  harmless (token-rotation logout issue already mitigated with the 60s grace window).

### Session close-out

Both stores in review · Home screen real · `main` current · Apple review login fixed
· all changes committed (`66cc711`) and pushed to `home-screen` + `main`.

## Apple review test account was broken — fixed

`applereview@goodfights.app` / `AppleTest2026!` (the App Review sign-in account)
returned INVALID_CREDENTIALS at login. The user row existed but had a mismatched
password. New reusable script `packages/backend/src/scripts/ensureAppleReviewUser.ts`
upserts it: resets the password and forces `isActive=true` + `isEmailVerified=true`.
Ran against the Render external DB; verified `POST /api/auth/login` returns 200 in
prod. Run before every submission if in doubt.

## Android Play Console — 2 deprecation warnings flagged (deferred, address later)

Play Console flagged **2 "recommended actions"** on the 2.1.0 Android release (P2).
Neither blocks release; both are forward-compat warnings. Logging for a later pass.

### 1. Deprecated edge-to-edge / window APIs (Android 15)

Play reports these deprecated APIs in use:
- `android.view.Window.setStatusBarColor` / `setNavigationBarColor` / `getStatusBarColor` / `getNavigationBarColor`
- `LAYOUT_IN_DISPLAY_CUTOUT_MODE_SHORT_EDGES`, `LAYOUT_IN_DISPLAY_CUTOUT_MODE_DEFAULT`

Origins are all **third-party/framework code**, not our app source:
- `com.facebook.react.views.view.WindowUtilKt.enableEdgeToEdge` / `statusBarHide` / `statusBarShow` (React Native core)
- `com.google.android.material.bottomsheet.BottomSheetDialog.onCreate`, `EdgeToEdgeUtils.applyEdgeToEdge`, `sidesheet.SheetDialog.onCreate` (Material)
- `com.swmansion.rnscreens.ScreenWindowTraits.setColor` / `setNavigationBarColor` (react-native-screens)
- `expo.modules.devlauncher...DevLauncherExpoActivityConfigurator.setColor` (dev-launcher, dev-only)
- `expo.modules.imagepicker.ExpoCropImageUtils.applyWindowTheming` (expo-image-picker)

**Assessment:** these resolve as RN/Expo/Material upgrade away from the deprecated
window-color APIs. Not actionable in our code today — fix is to bump Expo SDK / RN /
react-native-screens once their releases drop the deprecated calls. Revisit on next
SDK upgrade.

### 2. Resizability / orientation restrictions (Android 16)

From Android 16, large-screen devices (foldables, tablets) will **ignore** orientation
restrictions. Play detected portrait locks on:
- `com.google.mlkit.vision.codescanner.internal.GmsBarcodeScanningDelegateActivity` (PORTRAIT) — third-party (ML Kit)
- `com.fightcrewapp.mobile.MainActivity` (PORTRAIT) — **ours**, from `app.json` `"orientation": "portrait"`

**Assessment:** the app is designed portrait-only (`supportsTablet: false` on iOS too),
so this is intentional, but on Android 16 tablets/foldables it'll render in landscape
regardless and may show layout issues. If we ever care about large-screen Android,
test landscape layouts and relax the lock. Low priority — combat-sports phone app,
no tablet story today.

## Android update REJECTED — invalid privacy policy + account-deletion link (FIXED in code; needs Play Console URL update)

The 2.1.0 Android submission was rejected twice on 2026-05-30:
- **Privacy Policy section of the User Data policy: Invalid Privacy policy**
- **Account Deletion Requirement: Invalid account deletion link on your Data safety form**

**Root cause — the 2026-05-28 landing→web migration.** `/privacy` and
`/delete-account` moved from static `.html` files on the old landing site into
Next.js routes (clean URLs). Play Console still has the **old `.html` URLs**
registered (`https://goodfights.app/privacy.html` per `archive/LAUNCH-DOC.md`),
which now **404**. On re-validation Google saw dead URLs → both rejected.

Second, compounding problem on deletion: even at the correct `/delete-account`,
the page was **auth-gated** — a logged-out Google reviewer saw only "You must be
signed in to delete your account," which fails Google's account-deletion policy
(the linked page must publicly state what's deleted and how to request it).

**Code fix — shipped to `main` (commit `c800b51`, rebased → pushed as `d032b9b`),
Vercel prod deploy verified live:**
- `/delete-account` logged-out view now shows public deletion instructions:
  how to delete (web / mobile app / email `privacy@goodfights.app`), what data is
  removed, and that it's permanent. Signed-in interactive delete form unchanged.
- Added 301/308 redirects `/privacy.html → /privacy` and
  `/delete-account.html → /delete-account` in `next.config.ts` so the legacy
  registered URLs resolve regardless. Verified live: both `.html` URLs return
  308 → clean URL; `/delete-account` serves the public info to logged-out hits.

**STILL NEEDS MIKE — Play Console (I can't access it):**
1. **App content → Privacy policy** → set URL to `https://goodfights.app/privacy`
   (replace the `.html`).
2. **App content → Data safety form** → set the account-deletion URL to
   `https://goodfights.app/delete-account`.
3. **Resubmit** the rejected release / policy declarations.

The redirects mean the old `.html` URLs no longer 404, but Google can still flag
redirecting policy URLs — update to the canonical clean URLs above to be safe.

### Follow-up: moved Delete Account out of the footer into Edit Profile (commit `368809a`)

Mike removed the "Delete Account" link from the web footer (correctly — it's
unconventional there) and the footer is also hard to reach behind infinite-scroll.
Decision: account deletion belongs in **account settings**, not the footer or a
terms page (no `/terms` route exists anyway). Added a small account/legal section
to `/profile/edit` with **Privacy Policy** + **Delete Account** links, mirroring
the mobile app's Settings → Delete Account placement. Shipped to `main`, deployed.

**Compliance note:** store account-deletion compliance depends on the *registered*
`/delete-account` URL being publicly reachable (it is) — NOT on a footer/site link.
So removing the footer button never put the store submission at risk. Footer still
has Privacy (standard); the Privacy page links `/delete-account` too. Left the
infinite-scroll footer-reachability issue alone for now — it's a separate, larger
UX task and doesn't affect compliance.

## Blog: published UFC Freedom 250 (White House) SEO cluster — SECOND playbook run

Ran the SEO fight-preview playbook (`docs/playbooks/seo-fight-preview-cluster.md`) for
**UFC Freedom 250** (Sun Jun 14, 2026, White House South Lawn, DC). **New variant:** this
is an *event hub* with four marquee fights, not a single-fight hub like UFC 329. Shipped
live to `main` (commit `97392d5` on main / `9b5878a` on home-screen), all URLs verified 200.

- **Hub** (`/blog/ufc-freedom-250-white-house`, `featured: true`): ~28 question-H2 sections.
  Shared logistics (date, White House setting, **free CBS prelims** + Paramount+ no-PPV,
  times, intl how-to-watch, free Ellipse Fan Fest tickets) then a dedicated block per marquee
  fight: Topuria vs Gaethje (LW title unification), Pereira vs Gane (interim HW, 3-division
  history bid), O'Malley vs Zahabi, Hokit vs Lewis. Plus rest-of-card, odds snapshot,
  must-watch, quick answers.
- **4 cluster articles** (all `draft: false` + `hideFromHome: true`, bidirectionally
  interlinked with hub + each other): `ilia-topuria-undefeated-run`,
  `alex-pereira-three-division-history`, `sean-omalley-road-back`, `josh-hokit-nfl-to-ufc`.
- **Editorial decisions (Mike, live in this session):** politics/controversy fully omitted
  (White House treated as novel setting only) — EXCEPT the in-sport fun fact that Trump
  personally asked Dana White to add Derrick Lewis (Mike OK'd). Prelims 4pm ET (ufc.com;
  could not corroborate via a secondary source — secondaries only confirm 8pm main card).
  Gaethje "fights wars" fan-favorite framing. O'Malley already 1-0 in comeback (beat Song
  Yadong UD at UFC 324) — articles reflect this. Pereira: no Ankalaev in the *hub* (Mike
  disliked the wording) but Ankalaev loss+reclaim at UFC 320 is in the *Pereira cluster*.
  Pereira pick leaned to Poatan (Mike's editorial call: "magical way of connecting").
- **Hero images:** event hub uses self-hosted UFC event art at
  `packages/web/public/blog/ufc-freedom-250-banner.jpg` (768x512, downloaded via curl — ufc.com
  blocks Node TLS). The 4 cluster posts each got **dedicated 1260x662 fighter art** Mike provided
  (`public/blog/{ilia-topuria,alex-pereira,sean-omalley,josh-hokit}.png`), each doubling as that
  post's OG/share card. Shipped in a follow-up commit (`b9530cc` on main); all 4 image URLs +
  og:image verified live. Source PNGs (`{topuria,periera,omalley,hokit}-sized.png`) left in repo
  root — note "pereira" is misspelled "periera" in the source filename only; web copy is correct.
- **Homepage:** Freedom 250 hub is now the EditorialHero (featured); UFC 329 hub unfeatured
  (`featured: false`) so it drops to a secondary card per Mike's request. The 4 clusters are
  `hideFromHome` so they don't crowd the 3 secondary slots → secondaries = 329 + the 2
  McGregor clusters. Verified via `/api/editorial`.
- **Publish mechanics:** `home-screen` was 3 commits behind `origin/main` (web privacy/delete
  + a blog note), so could not push `HEAD:main`. Committed on home-screen, then cherry-picked
  onto a detached worktree at `origin/main` and pushed (clean FF, zero conflict — main only
  touched `max-holloway-career-explained.md`). Dirty working tree + home-screen left untouched.

### Freedom 250 — remaining playbook steps (NOT yet done)

- **GSC + Bing:** Request Indexing on the hub URL (needs Mike / console access).
- **Twitter/X:** launch tweet w/ banner + UTM (`?utm_source=twitter&utm_medium=social&utm_campaign=ufc-freedom-250`),
  straight + humor variants.
- **Freshness routine:** DONE — two **one-time** `/schedule` remote agents created (auto-disable
  after firing; both have a date-guard that no-ops if run after June 14):
  - `trig_015AmXY7pBhyA2i2AkotYPm2` — **Wed Jun 10, 10am ET** (14:00 UTC), 4-days-out odds/card/news sweep.
  - `trig_019ALGmSPmKgpXzzCKVY9CUQ` — **Sat Jun 13, 6pm ET** (22:00 UTC), post-weigh-in pass (missed
    weight / scratches / final odds). Both edit only the 5 post files, no em dashes, publish via the
    detached-worktree cherry-pick onto origin/main, verify 200. Manage at claude.ai/code/routines.
- **Reddit:** fight week only, helpful comment in a real thread.
- **Stray flag:** `chimaev-danis-raf-wrestling` (2026-05-24) is still `featured: true` —
  harmless now (newest-featured wins) but should be cleared.

## Blog UI polish + sitemap/Bing Q&A (web)

Small web-app fixes:

- **Top vertical gap on blog pages** — both `blog/page.tsx` (all posts) and
  `blog/[slug]/page.tsx` (article) used `py-8`, stacking ~32px on top of the root
  layout's `main` `py-4` (~48px total above the first element). Changed both to
  `pb-8` (drop top padding, keep bottom) so the layout's `py-4` is the only top gap.
- **EditorialHero label** — renamed the featured-post eyebrow from "From the blog"
  to **"The Latest"** (`components/EditorialHero.tsx`).
- **Sitemap (answered, no action needed):** `src/app/sitemap.ts` is a *dynamic*
  Next.js sitemap that maps over `getAllPosts()` — every non-draft post is included
  automatically and `lastModified` tracks the post date. New articles appear in
  `/sitemap.xml` on the next deploy with no manual edits. Drafts are excluded (correct).
- **Bing "known but not indexed" (answered):** technically clean — robots allows
  crawl, canonical/OG/JSON-LD present, sitemap submitted. The message is Bing's
  normal soft state for a young, low-authority domain with fresh content: crawled,
  deprioritized, not a hard blocker. Remedy is authority + time (backlinks, internal
  links, share traffic), not a code fix.

## Sidebar event-line org dedupe (web)

Two sidebar recommendation blocks printed the promotion/org name twice on the event line:

- **SpotlightBlock** ("A good fight you might love") — `Event: UFC · UFC 328: Chimaev vs. Strickland`
- **MightLikeBlock** ("Upcoming fight you might like") — `Jun 6 · Event: UFC · UFC Fight Night Muhammad...`

Both built the line as `Event: <promotion> · <eventName>`, but the event name already
begins with the org, so it doubled. Fix (both files in `packages/web/src/components/sidebar/`):
compute one `eventLine` that drops the promotion when
`eventName.toLowerCase().startsWith(promotion.toLowerCase())`, falling back to whichever of
promotion/eventName is present. Now renders `Event: UFC 328: Chimaev vs. Strickland` and
`Jun 6 · Event: UFC Fight Night Muhammad...`.

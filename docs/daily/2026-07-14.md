# 2026-07-14 — Blog updates (Du Plessis/Usman, UFC 330, UFC 329 reactions) + Operation "Own The SERPs" + audit-feedback UI sweep

## Session part 2 (after Mike's review — all approved & shipped)

- **All three articles APPROVED and shipped live** (`3108b441`): drafts flipped
  to `draft: false`, banners committed.
- **NEW `gf-event-fights` blog embed** (`BlogEventFights.tsx`): one placeholder
  `<div class="gf-event-fights" data-event-id="EVENT_SLUG"></div>` portals the
  full interactive event fight list (same Upcoming/Completed cards + hype/rate
  modals as the event page) into a post. Added to all three event articles.
  Mobile blog does NOT render this block yet (native rich-block support TBD).
- **Audit-feedback UI sweep** (web unless noted):
  - Fighter pages: whyFansLove/whyFansHate moved ABOVE the See-more fold.
  - Event pages: removed the separate "Full results (spoilers)" `<details>`
    block (was on EVERY completed event by design; redundant — the SSR'd
    CompletedFightCards already carry winner/method). resultLine/methodLabel
    helpers deleted with it.
  - `/events` index: banner thumbnail (promo-logo fallback) left of each row.
  - Fight pages: Odds section now titled with plain-English labels
    (Slight/-/Heavy Favorite/Underdog, Even money) via `oddsLabel()`; user
    hype/rating now use the big filled flame/star with the score inside and
    heatmap colors (`BigScoreIcon`), in the stats sections AND the action
    buttons (scored state = card-style button with big icon).
  - `/fights/best/[year]`: Rating/My Rating column headers (exported
    `FightColumnHeader`); per-year editorial retrospectives
    (`src/lib/bestFightYearNotes.ts`, 2007-2026, grounded in our top-rated
    lists); FAQ section + FAQPage JSON-LD (dynamic best-fight answer +
    methodology); prev/next year links.
  - Mobile (via subagent): fighter detail default sort now 'newest' (Date);
    event detail screen renders confidence-gated `aiEventSummary`
    (`app/fighter/[id].tsx`, `app/event/[id].tsx`). NOTE: sibling screen
    `app/(tabs)/events/[id].tsx` exists — check which route is live before
    assuming coverage. Needs OTA to reach users.
- **🚨 Anthropic API key OUT OF CREDITS** — discovered running
  `scripts/enrich-event.ts` for UFC 330 (which has `aiEventSummary: null`).
  ALL enrichment crons are failing until Mike tops up console.anthropic.com
  billing. After top-up: re-run enrich-event for UFC 330 + UFC OKC.
- **Sale framing corrected** (Mike): brand+web+app+users+data = ONE asset;
  "separately-sellable traffic" retired from sale-value.md, both SEO plan docs,
  and memory.
- **Own The SERPs timeline compressed**: front-load all build in ~90 days,
  ongoing rhythm after; multi-year = ranking physics only.
- Mobile blog `gf-event-fights` support + `/schedule` hubs = next build items.
- Pre-existing bug spotted by subagent: `app/event/[id].tsx` fires a debug
  `Alert.alert("Debug: No Fights")` in production when an event has no fights.

## Session part 1 (below, written before review)

## Blog edits (original notes — since approved and shipped)

- **Du Plessis vs Usman preview** (`2026-07-13-du-plessis-vs-usman-preview.md`):
  - Banner downloaded → `public/blog/du-plessis-usman-banner.png` (bloodyelbow
    OKC poster, compressed 2.8MB → 825KB, 1600px). Frontmatter already pointed
    at this path.
  - Odds + both bio sections now carry Usman's inactivity: 3-fight skid 2022-23
    (Edwards ×2, Chimaev), one fight in last three years (dec. over Buckley,
    June 2025, first win since 2021), age 39.
  - **Fact correction vs Mike's note:** the DDP–Chimaev loss (UFC 319, Aug 2025)
    was NOT a close decision — 50-44 ×3, out-struck 529-45. Written accurately
    ("wide decision"). Flagged to Mike.
  - New section "Who else is worth watching on this card?": Kline vs Ricci
    (top-15 SW, Kline 3-0 in 2025), Bashi vs Delgado (prospect collision;
    Delgado 10-2, beat Fili/Amil/Matthews), Hooper two-fight skid stakes,
    Cannonier vs CLD veteran/riser framing.
- **UFC 330 preview** (`2026-07-13-ufc-330-makhachev-garry-preview.md`):
  main card **9 p.m. ET / 6 p.m. PT** added to "When is UFC 330?", the start-time
  section, and Quick Answers. Banner downloaded →
  `public/blog/ufc-330-makhachev-garry-banner.png` (Xfinity Mobile Arena, 760×460).
- **UFC 329 results** (`2026-06-17-ufc-329-full-card-odds-predictions.md`, LIVE
  article — do not push without review): three new sections after "What
  happened": McGregor's response (head-gasket/hell quotes, "Surgery. Prehab. …
  Final fight of the contract."), Holloway's (told Beltran to stop it, wants
  trilogy ~2027), MMA-world reactions (White/ACL, Makhachev "Conor beat Conor",
  Pimblett face-of-the-org grab, Ryan Garcia, Crawford, Poirier+Masvidal).
  Em-dash sweep clean on all three files.

## Operation "Own The SERPs" (NEW workstream doc)

`docs/plans/operation-own-the-serps.md` — formalizes SEO as the named multi-year
workstream. 9-category query universe, 5 pillars, 3 phases (2026 index+playbook,
2027 coverage+authority, 2028+ head terms+harvest). Memory:
`project_operation_own_the_serps`. **Phase 1 identified gap: no
/schedule / "fights tonight/this weekend" hub pages.**

## Programmatic SEO audit

Sampled live sitemaps (all serving: fighters 1,130 URLs incl. division hubs;
events; fights). Audit page list delivered to Mike in-session (template × state
matrix: top/mid/tail fighter, upcoming/completed/legacy event, rated/unrated
fight, hubs, best-of years, noindex checks).

## Also answered

Fight-list-on-blog-pages question: yes — `gf-fight-card` embed plumbing already
exists in `BlogFightCards.tsx` (per-fight placeholders); recommended embedding
cards for main-card bouts in event preview posts; a whole-card embed block is
the missing piece worth building.

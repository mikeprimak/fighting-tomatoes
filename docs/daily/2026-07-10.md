# 2026-07-10 — UFC 329 fight-week prep · booked-push routing fix

UFC 329 (McGregor vs Holloway 2) is tomorrow, July 11. Session prepped the blog content and verified the live-tracking pipeline.

## Blog refresh (committed `9eecb55a`)

Updated the three UFC 329 posts in `packages/web/src/content/posts/`:

- **Full-card / odds / predictions** — rewrote the card into final Main/Prelims/Early Prelims sections (per the prod DB, which Mike confirmed), added weigh-in results (all 28 made weight; McGregor 170.5, Holloway 170), added the two late replacements (John Garza in for Ethyn Ewing vs Basharat; Alessandro Costa in for Ode' Osbourne vs Durden), added a Green vs McKinney prediction, moved Whittaker–Krylov + Steveson–Ellison predictions under prelims framing, refreshed undercard odds (BSD tightened -200 → ~-145; Sandhagen–Bautista **flipped**, Sandhagen now ~-135; Kavanagh ~-218; McKinney ~-148; Whittaker -213 → ~-135).
- **Main guide (mcgregor-holloway-2-ufc-329)** — same card restructure, new "Did everyone make weight?" section, quick answers updated to consensus -225/+185.
- **How-to-watch** — start times "expected" → "confirmed".

Odds cron (`ufc-329-odds.yml`) is healthy — it appeared dead because local main was 3 commits behind origin; runs succeed daily at 17:00 UTC and rewrite the ODDS-SNAPSHOT blocks + `updated:` frontmatter. It self-no-ops after July 12.

## DB / live tracker verification (read-only)

Event `72ef8bf7-3a41-4f53-ba05-5e0753ced2c6` "UFC 329: McGregor vs. Holloway":

- 14 active fights (16 rows; Basharat–Ewing and Osbourne–Durden correctly CANCELLED). Mike manually confirmed the fight list.
- Section times exactly match the broadcast: early prelims 21:00Z (5pm ET), prelims 23:00Z (7pm ET), main 01:00Z July 12 (9pm ET). `scraperType='ufc'`, status UPCOMING.
- Lifecycle (Render, every 5 min) flips LIVE at the earliest section time (5pm ET) and dispatches the VPS tracker; step 1.5 `/track/check` re-checks every tick, so a missed first dispatch self-heals.
- Hetzner VPS scraper service healthy: `{"ok":true,"trackers":0}`, ~11 days uptime.
- Recent UFC events (Jun 14/20/27) all completed with winners recorded, so the ufc VPS handler is proven current.

**Risk flagged:** the GitHub Actions fallback (`ufc-live-tracker.yml`) depends on Render's `GITHUB_TOKEN`, which is still expired (401). If the VPS dies mid-event there is no automatic fallback. Mike should refresh the token (see `project_blog_publish_button_and_github_token` memory).

## Booked-fighter push now opens the notifications inbox (committed `eec7420c`, later session)

Mike tapped the "Arman Tsarukyan just got booked" push and it deep-linked to fight detail; he wants it to land on the notifications inbox instead.

- `packages/mobile/components/NotificationHandler.tsx` — new routing branch: a push whose `data.screen === 'notifications'` navigates to the Profile tab then pushes `/notifications`, so back returns to Profile (the inbox's normal entry point). Checked before the `fightId` deep-link branch.
- `packages/backend/src/services/followFighterNotifications.ts` — the booked lane now sends `data: { fightId, screen: 'notifications', lane: 'booked' }`. `fightId` is kept so clients that haven't taken the OTA still deep-link like before instead of dead-ending.
- Scope: **booked lane only.** The 3-day / morning-of lanes still send `screen: 'fight-detail'` / `'event-detail'` and deep-link directly — flag if those should change too.
- Shipped: pushed to main (backend auto-deploy) + production OTA published to both runtimes (iOS `2.1.3` group `43201478…`, Android `1.0.0` group `fa89596f…`). Verified targeting with `eas update:list` first.
- To test: cold-start the app twice (OTA applies on second cold start), then tap any booked push — or re-tap the Tsarukyan one if it's still in the tray (note: pushes sent *before* the backend deploy carry the old `fight-detail` payload and will still open fight detail; only newly sent booked pushes land on the inbox).

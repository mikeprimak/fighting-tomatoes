# 2026-07-24 — RIZIN Landmark 15 corruption (user feedback d708305f)

## Report

Kevin Sartori (Android 2.1.3, feedback `d708305f-2b49-40bd-b6d7-bf22b55f5137`):
Landmark 15 was missing Kojiro Shiba vs. Haruto, then started "showing a bunch
of fights from a different event."

## Root cause

`discoverTapologyUrl`'s keyword fallback (copy-pasted in `scraperService.ts`,
`runTapologyLiveTracker.ts`, `backfillTapologyResults.ts`) matched event-name
words of length > 2 against hub-page URLs and accepted any link with ≥ 2 hits.
For numbered series this is fatal: "Rizin FF - Landmark Vol. 15" reduces to
`rizin/landmark/vol` (the "15" is dropped by the length filter), so the FIRST
Landmark link on the Tapology RIZIN hub page wins, whatever its number. The
mismatched URL is then **persisted to `event.ufcUrl`**, and the nightly
`backfillTapologyResults` VPS cron scrapes that page — `tapologyLiveParser`
creates every fight it doesn't recognize, importing a foreign card wholesale.

Chain found in prod:

- Landmark 15 → pointed at Tapology **Landmark 13** page (137827); L13's whole
  card (16 fights) imported into L15 on 2026-07-23, forking 3 dup fighters
  (Ryusei Imamura, "Yu-ki", S. Payaklamphong).
- Landmark 13 → pointed at Tapology **Landmark 14** page (140753); 12 of L14's
  fights imported into L13 on 2026-05-29 (stuck UPCOMING on a COMPLETED event).
- Shiba vs. Haruto was missing because the RIZIN daily scraper is
  Sherdog-based and Sherdog's card lacked it (kickboxing bout); the Tapology
  backfill would have added it — but it scraped the wrong page.

## Fixes

**Data (prod, via script):**
- Deleted the 16 foreign fights on L15 and 12 on L13 (all zero engagement,
  verified counts before delete) + the 3 orphaned dup fighters.
- Corrected `ufcUrl` on L13 (137827-…landmark-13), L14 (140753-…landmark-14),
  L15 (140754-…landmark-15).
- Re-ran `runTapologyLiveTracker.ts <eventId>` **on the VPS** (local runs fail
  Cloudflare — no `TAPOLOGY_PROXY` in the local .env; the DataImpulse+CapSolver
  stack lives on the VPS). L15: all 16 fights got winners/methods, and the
  missing **Kojiro Shiba vs. Haruto was created (Shiba by UD)**.
- Post-run cleanup: Tapology spells "Jae **Hoon** Lee" vs our "Jae **Hun** Lee",
  so the parser created a dup Takagi fight + fighter — merged the result onto
  the existing rated fight, deleted the dup pair, and slotted Shiba vs. Haruto
  at its real card position (order 12, Main Card).

**Code (root cause):**
- New `src/utils/tapologyEventMatcher.ts` — shared `matchTapologyEventLink()`
  replacing all three copy-pasted discovery matchers. Rules (tuned against
  real prod URLs, see the unit test): CONFLICT-only numeric guard (a link is
  rejected only when it carries numbers that don't cover the event name's —
  number-less slugs like `144122-zuffa-boxing` stay eligible); roman numerals
  ii–xix in slugs count as numbers (`mvp-showcase-ii`); keyword scoring counts
  event words in slug + link display name and requires a UNIQUE best candidate
  (generic Zuffa slugs disambiguate via fighter names; ties refuse to guess).
- Unit test: `src/utils/tapologyEventMatcher.test.ts` (tsx assert script,
  11 cases incl. the incident and every prod shape the guard must not break).
- `backfillTapologyResults.ts` switched off its private `new PrismaClient()`
  to the `lib/prisma` singleton (per the 2026-06-06 connection-pool rule).

## Verification

- Matcher unit-tested against the real hub-link set: Vol. 13/14/15 each map to
  their own page; "Landmark Vol. 16" (absent) returns null; no-number names
  still match.
- Backend `tsc --noEmit` clean.

## Follow-ups

- RIZIN results generally rely on the Tapology backfill (Sherdog daily scraper
  covers upcoming only); L13/L14 winners were also backfilled in this session
  via targeted tracker runs.

---

# 2026-07-24 — Session 2: Mike's issue-list batch (14 items)

Batched fix list added to `docs/BACKLOG.md` §18, then worked through. All packages type-check (web 0, backend 0, mobile still 75 baseline — 0 new).

## Web
- **SERP favicon on white → #181818.** `app/icon.png` + `apple-icon.png` were the gold glove on a *transparent* bg, which Google renders on its white SERP (looks white-backed). Flattened both onto `#181818` via sharp. Google re-crawls favicons on its own slow cadence, so the SERP thumbnail updates whenever Google next refreshes it.
- **"null null" comment author** (home Classic Comments). `community.ts` built `displayName || `${firstName} ${lastName}`` → literal "null null" for legacy users with all three null. Added `commentAuthorName()` helper (displayName → non-null name parts → "Fan"); replaced all 4 sites (incl. the `/top-comments` throwback that surfaces on the home band).
- **My hype not showing in home "Hyped Upcoming".** `UpcomingFightCard` already renders `userHypePrediction`; the home queries (`hot-upcoming`, `recent-good`, `classic-good`) weren't auth-keyed, so the first tokenless fetch cached and never refetched with the user's data. Keyed all three on `isAuthenticated` + `enabled: !authLoading` (mirrors CommentSections). Fixes "My Hype" and the two "My Rating" columns.
- **LiveFightCard 'view comments' → wrong layout.** A LIVE fight hit neither `isCompleted` nor `isUpcoming` in `FightDetailClient`, so it fell through to the pre-fight/upcoming layout. Added `showRating = isCompleted || isLive`; rating section, rate button, and reviews now render for LIVE (outcome/spoiler blocks stay `isCompleted`-only).
- **Reply UI on web comments.** Backend reply endpoints + nested `replies` already existed (mobile uses them); web just didn't render/compose. Added `onReply`/`compact` to `CommentCard`, and `CommentThread` + `ReplyComposer` in `FightDetailClient` (works for both post-fight reviews and pre-fight comments; `patchUpvote` now recurses one level into replies).
- **Schedule moved** out of top nav (`Navbar` navLinks) — already present in `Footer`.
- **Download-the-app footer** on finite pages only. New `AppDownloadFooter` (hidden for app users, UTM `page-footer`) added to fighter/fight/event `page.tsx`; NOT the infinite-scroll feeds.
- **Mobile-browser styling.** (a) De-squished fighter names in Upcoming/Completed cards (smaller headshots + tighter gaps below `sm`; Live already stacks names full-width). (b) Home blog moved below the events list on mobile via new `SidebarLayout mobileBlogAtBottom` prop (home only — other feeds scroll forever). (c) "Never miss a Good Fight" logged-out box (`IdentityBlock`): larger heading, grey lucide icons (Flame/Star/Bell), 3 shorter bullets, dropped the Fan-DNA bullet + "your home for…" line, larger grey bullet text, "Already a fan?" → "Already have one?".
- **"Your kind of fighters" caption.** `community.ts` reason "Runs in X's division" → "Same division as X".

## Mobile
- **Comment counts on home hyped/rating badges.** `HypedFightRow` badge showed only the hype/rating count; added `commentCount` (reviews for rating surface, pre-fight comments for hype) next to it, matching the full cards. (Web already renders counts.)
- **'Load More' comments button** on `CompletedFightDetailScreen` was white text on the gold primary → changed to `#181818` (text + spinner).

## Backend / data
- **PFL prelim start times.** Real cause found in prod: `pflDataParser` update wrote `prelimStartTime`/`earlyPrelimStartTime` unconditionally, so when pflmma.com omitted the section header the daily scrape **clobbered discovery-filled times back to null**, and `startTimeDiscoveredAt` then throttled the 36h retry — PFL cards showed no prelim indefinitely (Washington DC Jul 26, 16 fights, still null). Now preserves discovery/manual section times (`startTimeSource`) when the scrape has none; scraper-owned values still clear. (Next daily discovery cycle fills the currently-null ones and they'll now survive.)
- **RAF 'completing every 5 min' — root cause + fix.** Two completers can fire; the tick-aligned one was `eventLifecycle.ts` Step 3 (`lifecycle-auto`). When the RAF11 Webflow selector break mass-cancelled the card, `pending` (UPCOMING/LIVE) hit 0 — CANCELLED falls outside it — so Step 3 force-completed on every 5-min tick (Mike kept restoring it to LIVE). Hardened Step 3: a tracker-backed event with 0 pending **and 0 completed** fights is an all-cancelled card, not a genuine finish → defer to the 10h hard cap (a real finish always has ≥1 COMPLETED fight). Cancellation-health guard (`403ba141`) already stops the underlying mass-cancel. Full analysis: `docs/HANDOFF-raf-live-mass-cancel-2026-07-18.md`.

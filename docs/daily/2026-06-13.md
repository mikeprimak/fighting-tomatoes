# 2026-06-13

## Tapology Cloudflare → Scrapfly (the real fix)

Continuation of the 2026-06-11/12 investigation. Root cause confirmed earlier:
Tapology escalated Cloudflare from JA3/TLS fingerprinting to a JS challenge that
escalates to an **interactive Turnstile widget on datacenter IPs** (GitHub
Actions runners + the Hetzner VPS). No headless browser solves Turnstile, so
every Tapology daily scrape / live track / backfill broke at once. The
real-Chrome+stealth path (`tapologyBrowser.js`) only clears it from a
**residential** IP — fixes local dev, not CI/VPS.

Decision (Mike): go with **Scrapfly** Web Unlocker, **test for one month**.

### Shipped to `main`

- **`5c93c85b` feat(scrapers): Scrapfly fallback for Tapology Cloudflare Turnstile**
  - Env-gated Scrapfly path added to `packages/backend/src/services/tapologyBrowser.js`.
    When `SCRAPFLY_KEY` is set, `launchTapologyBrowser` wraps the browser so every
    `page.goto()` to a `tapology.com` URL is served by Scrapfly (`asp=true` solves
    Turnstile server-side on a residential pool, `render_js=true`) + `page.setContent`,
    with an injected `<base href="https://www.tapology.com/">` so the scrapers'
    `link.href` reads resolve absolute (page URL is `about:blank` under setContent).
  - **Transparent — zero consumer edits.** All 8 `scrape*Tapology.js`, the live
    tracker (`tapologyLiveScraper.ts`), and `backfillTapologyResults.ts` route
    through this module. `SCRAPFLY_KEY` unset → original real-Chrome path runs.
  - Built on top of the `fix/tapology-cloudflare` real-Chrome work, which was
    merged to main in the same push (it was based on pre-shelving main; one
    conflict in `backfillTapologyResults.ts` import block, resolved to keep both
    the shelving helpers and the `fetchTapologyHtml` import).
- **`4f140fc9` ci(scrapers): inject SCRAPFLY_KEY into Tapology workflow envs** —
  added `SCRAPFLY_KEY: ${{ secrets.SCRAPFLY_KEY }}` to the env block of all 10
  Tapology-fetching workflows (4 active daily + 4 shelved for un-shelve
  readiness + `tapology-backfill` + `tapology-live-tracker`).

### Scrapfly account

- Key `scp-live-85a60a8c63164469a58f7a7cd1e77069`. **FREE plan = 1000
  requests/month**, request-COUNT metered (so `render_js` is effectively free
  against quota — the "30 credits/call" Scrapfly reports is the paid-plan price),
  5 concurrent. Cost driver for the month test is **live-tracker polling during
  events**, not the daily scrapes. If it blows 1000, upgrade the plan.

### Verification

- Local end-to-end smoke test (1 Scrapfly call): `launchTapologyBrowser` →
  `newPage` → `goto(MVP hub)` → status 200, `_viaScrapfly`, real title, the
  scrapers' `#content`/`#mainUpcoming` selectors found 6 events, **all
  `link.href` absolute tapology URLs** (base injection works).
- Backend production type-check: `tsc --project tsconfig.production.json` → **0 errors**.
- **Live CI proof:** manual `workflow_dispatch` of `mvp-scraper` on a GH runner
  (datacenter IP, the thing that was blocked) → run **success**, log shows
  `SCRAPFLY_KEY: ***`, **`✅ Found 25 MVP events`** (was 0), fights extracted from
  every event page. (A couple "Found 0 fights" entries are genuinely-empty past
  events, not fetch failures.)

### SCRAPFLY_KEY placement

| Runtime | Status |
|---|---|
| GitHub Actions (daily scrapers + backfill) | ✅ repo secret set via API + env in 10 workflows; verified live |
| Render backend | Not needed — never fetches Tapology (admin trigger dispatches GH workflows; live tracking goes to VPS; eventLifecycle only makes HTTP calls) |
| **Hetzner VPS (live trackers)** | ✅ Mike appended `SCRAPFLY_KEY` to `/opt/scraper-service/packages/backend/.env` + ran `vps-update.sh` (2026-06-13 17:35 UTC). Code pulled to `cbdc95ab`, Prisma regenerated, `scraper-service` restarted and active on port 3009. Loaded via the same `.env` that already supplies `DATABASE_URL`. |

### Notes

- Branch hygiene: all code shipped via a worktree off `origin/main`
  (`fix/tapology-scrapfly`), FF-pushed to main; pivot branch untouched.
- Still-active (unrelated) Tapology orgs covered: MVP, RIZIN, Karate Combat,
  Dirty Boxing, Gamebred. The 5 shelved boxing orgs already don't scrape (gate),
  but their workflows got the key too so un-shelving "just works".

## Scrapfly request budget — Tapology live polling slowed 30s → 150s

Follow-up to the Scrapfly fix above. Scrapfly free tier = 1000 req/mo, and
**each live poll is one request**. `scraperService.ts` ran a single global
`SCRAPE_INTERVAL_MS = 30s` for *every* tracker, so Tapology burned ~120 req/hr
— one long card (~6–8 hr LIVE window) could eat half the monthly cap. Earlier
belief that this was already bumped to 150s was wrong; it had never shipped.

**Shipped to `main` (`f5406eb4`, perf(scrapers))** — worktree off `origin/main`,
rebased onto `b884a824` (notify-flags commit) so it FF'd cleanly; pivot branch
untouched. Split the cadence per scraper type via `scrapeIntervalFor()`:
- `tapology` → `TAPOLOGY_SCRAPE_INTERVAL_MS = 150s` (the only Scrapfly-metered path)
- own-site scrapers (ufc/bkfc/pfl/onefc/oktagon/sherdog) stay at 30s — they fetch
  their own sites, cost no Scrapfly requests, and want real-time cadence.

5× fewer Scrapfly requests on Tapology; the added ≤150s lag is invisible since
Tapology isn't a real-time source (results lag, backfill cron exists). Only A of
the two optimizations was done — boxing-org backfill-only (C) is moot (orgs
shelved). Per-type type-safety verified by inspection (worktree had no
node_modules; change is a trivial `(string): number` helper).

**⚠️ Needs VPS deploy to take effect:** `ssh` + `bash
/opt/scraper-service/packages/backend/vps-update.sh` (git pull → build →
restart). Until then the VPS runs the old 30s cadence.

## Adaptive downshift for stalled Tapology trackers (B′)

**Shipped to `main` (`e41219ed`, perf(scrapers))** — worktree off `origin/main`,
FF-clean, pivot untouched. `tsc --project tsconfig.production.json` = 0 errors
(verified via a junction to the main checkout's node_modules — worktrees have
none).

Mike's correction: Tapology is *largely reliable* and follows most events
closely; only a rare few stall. So the downshift is a pure safety net for the
rare dud, tuned to never touch a healthy card:
- Each Tapology scrape that yields `fightsUpdated > 0` (a real status/result
  transition, `changed===true` in `tapologyLiveParser`) bumps `tracker.lastChangeAt`.
- `nextTapologyDelayMs()`: if the card has been flat (no change) for
  **`TAPOLOGY_FLATLINE_MS = 45min`**, poll every **`TAPOLOGY_SLOW_INTERVAL_MS = 15min`**;
  otherwise the full 150s. The first change resets `lastChangeAt` → snaps back
  to 150s automatically.
- Mechanism: Tapology trackers now use a **self-rescheduling `setTimeout`** loop
  (so the delay can vary per tick); own-site scrapers keep the fixed 30s
  `setInterval`. Tracker is registered in `activeTrackers` *before* the first
  scrape so an auto-complete mid-scrape isn't undone by a late insert; the loop
  bails if `!activeTrackers.has(eventId)` after a scrape. `stopTracker`'s
  `clearInterval` clears the `setTimeout` handle fine (Node interchangeable).
- 45min is conservative — a normal card never goes that long without a fight
  moving, so it stays at full speed all night. Worst-case fully-stuck card:
  ~42 requests vs the old ~960.

**⚠️ Same VPS deploy covers both A and B′:** `ssh` + `bash
/opt/scraper-service/packages/backend/vps-update.sh`. Both are on `main` now
(`f5406eb4` then `e41219ed`); one pull/build/restart picks up both.

## RAF international broadcast = YouTube

Mike: the broadcast channel for RAF events **outside the US** is the RAF YouTube
channel (https://www.youtube.com/@RAFwrestling). Marked it in the How-to-Watch
system.

- Created BroadcastChannel `raf-youtube` (name "YouTube", homepageUrl + webDeepLink
  = the RAF channel URL). Did **not** reuse the generic `youtube` channel — its
  homepage is youtube.com, and default deep links derive from the channel's
  `affiliateUrl`/`homepageUrl`, so a shared channel can't carry a promotion-specific
  link.
- Added `PromotionBroadcastDefault` rows (promotion `RAF`, tier `FREE`, cardSection
  null) for the five non-US regions: CA, GB, AU, NZ, EU.
- US default unchanged (Fox Nation, SUBSCRIPTION).

Prod DB only (no migration / no git deploy needed). `/api/events/:id/broadcasts`
falls back to these promotion defaults for RAF events that lack explicit per-event
rows, so all RAF events now show YouTube outside the US automatically.

## CORRECTION (2026-06-13, same day) - Scrapfly cap post-mortem

The Scrapfly note above is WRONG. Free Discovery plan = 1000 CREDITS/month, NOT requests. Cap blew in ~3hrs.
- Credit-metered: every call asp=true+render_js=true ~25-80 credits; free tier = ~30 page loads/month total.
- Daily scrapers re-render each org ENTIRE back-catalog: scrapeEventsList has no upcoming-only filter, hardcodes status=Upcoming. One MVP run ~25 dead-event renders ~900 credits = whole monthly budget. x8 orgs ~120-170K credits/mo. Cost driver is daily scrapes, NOT live polling (docs had it backwards).
- No caching; MVP hub re-fetched 6+ times in a few hours.

Plan: upcoming-only enumeration (all 8 scrapers, ~80pct cut) + Scrapfly cache=true + test dropping render_js + 300s live cadence. Even optimized ~50-80K credits/mo, needs paid tier (~30/mo) or VPS residential proxy. Post-mortem in memory project-tapology-scrapfly-cloudflare.

## RAF live tracker broken (RAF 10) — fixed live + durable VPS wiring

During **RAF 10** (live), Mike reported the event showed LIVE but the first bout
stuck on "up next" and nothing marked complete. Two independent bugs:

1. **No working runner.** `eventLifecycle` excluded `raf` from
   `VPS_SUPPORTED_SCRAPERS`, so it tried to dispatch `raf-live-tracker.yml` via
   GitHub Actions. That GH workflow has **0 runs ever** — the Render→GH dispatch
   isn't firing in prod (likely no `GITHUB_TOKEN` on Render). UFC moved to the
   VPS long ago so the dead GH path went unnoticed; RAF (and PFL) were the only
   things still depending on it. RAF events were going **completely untracked**.
2. **Shadow-only publish.** Even when `runRAFLiveTracker` runs, it never called
   `refreshProductionScrapersCache`, so `shouldAutoPublish('raf')` read the
   *default* cache (`['ufc','oktagon','bkfc','onefc','pfl']` — **no raf**) and
   `buildTrackerUpdateData` wrote only the `trackerFightStatus/trackerWinner`
   **shadow** fields. The app reads the *published* `fightStatus/winner`, which
   stayed UPCOMING. (`production_scrapers` SystemConfig *does* include raf — the
   bug is the standalone process never loading it.) UFC never hit this because
   it's in the default list.

### Tonight (immediate)
- Ran the tracker against prod manually with the cache-refresh fix → fights
  9/10/11 (Alirez, Sharipov, Harutyunyan — the early bouts; RAF wrestles
  main-event-last so high `orderOnCard` competes first) now **published**
  COMPLETED + winners. Verified PUB fields, not just shadow.
- Left a local 3-min polling loop running against prod (`/tmp/raf-loop.sh`) so
  the rest of RAF 10 keeps publishing until it auto-completes.

### Durable fix — pushed branch `hotfix/raf-vps-live-tracker` (off `main`, NOT merged)
- `scraperService.ts`: new `scrapeRAFOnce()` VPS handler (child-process cheerio
  scraper, UFC pattern) + `case 'raf'` + `raf` in `autoDiscoverEvents` +
  `refreshProductionScrapersCache` on boot/every-60s.
- `eventLifecycle.ts`: `raf` added to `VPS_SUPPORTED_SCRAPERS` (routes RAF to the
  VPS @30s instead of the dead GH path) + stale comments fixed.
- `runRAFLiveTracker.ts`: `refreshProductionScrapersCache` at start (GH/manual
  fallback now publishes too).
- Typecheck clean (prod tsconfig). Built on the pivot-branch working tree; shipped
  via worktree off `main` because pivot's `scraperService.ts` is 64 lines behind.

### TO DEPLOY (Mike)
1. Merge `hotfix/raf-vps-live-tracker` → `main`.
2. **VPS deploy** (`vps-update.sh`) so the new `scrapeRAFOnce` handler lands —
   VPS restart re-discovers active LIVE events. Safe to do during RAF 10.
3. Render auto-deploys `main` (picks up `VPS_SUPPORTED_SCRAPERS += raf`).
   **Render redeploy rotates tokens / can log users out — do AFTER the event.**
4. Separately worth checking: is `GITHUB_TOKEN` set on Render at all? The GH
   live-tracker dispatch (raf/pfl fallback) has been dead since ~April.

### Deployed live (2026-06-14 01:30 UTC)
VPS checked out `hotfix/raf-vps-live-tracker` + `vps-update.sh`; `scraper-service`
restarted on the branch, auto-discovered RAF 10, now scraping every 30s and
**publishing** (verified: 5 fights published COMPLETED, 0 shadow-only). Local
polling loop stopped. RAF 10 owned by the VPS for the rest of the card.

### Open one-off follow-ups (surfaced in the VPS logs tonight)
1. **Install Google Chrome on the VPS.** The Tapology live tracker still launches
   local Chrome even with Scrapfly (`page.setContent` path) and is currently
   throwing `Could not find Google Chrome executable ... /opt/google/chrome/chrome`
   on every tick → **0 fights extracted, silently**, for any Tapology-tracked
   card (Zuffa/PFL/RIZIN/KC/DBX/Matchroom). Fix before the next such event.
2. **Verify `GITHUB_TOKEN` on Render.** The `triggerGitHubLiveTracker` fallback
   has 0 runs ever for raf and nothing since ~April — almost certainly unset/
   expired. Moot for RAF now (VPS), but it means there's no GH fallback if the
   VPS goes down. Set a token with `actions:write` or accept VPS-only.

Both also added to `docs/operations/maintenance.md` → "VPS live-tracker pre-flight".

## RAF 10 addendum (2026-06-14 ~02:40–03:00 UTC) — official source froze, finished off Yahoo

Mike reported RAF 10 still wasn't tracking ("thought it was fixed but it's not;
when I shut the last Claude window the tracker stopped"). Investigation showed
the durable VPS wiring from earlier is mechanically fine — the failure was the
**source**, not the process:

- The **official RAF site (`/events/raf10`) froze at "5 complete"** for hours
  while reality was 11 done. `runRAFLiveTracker` ran clean against it ("11 total,
  5 complete", 0 updated) — it simply had nothing new to publish. The VPS tracker
  reads this same page, so it published the early bouts (#7–#11) then stalled.
  **Yahoo's live blog was accurate and current the whole time** — confirms the
  RAF playbook: official site = result-only and lags badly; Yahoo leads.
- This is the core unfixed gap: RAF live tracking must be driven off the **Yahoo
  live blog**, not the official site. The VPS/cron plumbing works; the data
  source it points at doesn't. (Same conclusion as RAF 09. Build a Yahoo RAF
  parser — boxing `yahooLiveBlogScraper.ts` is the template — before RAF 11.)

### What I did (all hand-patched from Yahoo, prod DB)
- Completed the stuck bouts with correct winners (published + shadow, via
  `buildTrackerUpdateData` after `refreshProductionScrapersCache` — the raf
  canary): **#6 Aitmukhan def. Zillmer (6-4)**, **#5 DeSanto def. Ortega**.
  Mike manually completed #2/#3/#4 in the admin panel; #1 he set LIVE then told
  me the result.
- **#1 Khamzat Chimaev def. Dillon Danis (Fall)** completed on his word →
  `autoCompleteRAFEvent` flipped **RAF 10 → COMPLETED**. Full card now accurate
  (all real bouts COMPLETED, the two scratches CANCELLED).
- **Main-event up-next push:** the 7 active `fightNotificationMatch` rows for
  Chimaev/Danis were already `notificationSent: true` (fired at some earlier
  point but evidently not delivered as intended), so a normal dispatch no-op'd.
  Per Mike's explicit ask, reset those rows to unsent and re-fired
  `notifyFightStartViaRules` → **6 sent / 0 failed** (7th user has no push token).
- Setting `winner` on the manual completions also protects them from the parser's
  "lifecycle premature reset" (which only downgrades a COMPLETED fight with
  `!winner`), so the still-stuck official page can't revert them.

### Still open
- **Yahoo-driven RAF source** (above) — the real durable fix; official-site
  tracker is unreliable for RAF regardless of where it runs.
- The hotfix VPS wiring (`hotfix/raf-vps-live-tracker`) is still **not merged to
  main** — see the deploy checklist in the section above.

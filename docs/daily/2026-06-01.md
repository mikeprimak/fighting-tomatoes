# 2026-06-01

## Involuntary-logout root cause fixed (refresh-token rotation)

Mike was still getting logged out overnight / on morning app-open despite TWO
prior fixes (`49c22ac`, `a657d10`) — both of which only handled deploy-time 5xx.
Logouts happened with **no deploys**, so the redeploy theory was wrong.

**Root cause (server-side, `routes/auth.fastify.ts` `/refresh`):**
1. The endpoint **rotated** the refresh token on every call — minted a new one,
   shrank the old token's expiry to a **60s grace**. If the refresh *response*
   was lost (flaky morning connection) or the app was **suspended** mid-refresh,
   the client never stored the new token and the old token's 60s grace lapsed →
   next refresh 401s → hard logout. 60s was never long enough to survive a
   backgrounded app.
2. The `catch` returned **401 for ANY error** — including transient DB /
   cold-start failures — which the client treats as a genuine logout, defeating
   its careful transient-vs-invalid handling.

Both client refresh paths (`services/api.ts` AND `store/AuthContext.tsx` —
there are two copies) already handle transient correctly; the bug was the server
turning recoverable conditions into hard 4xx.

**Fix (`a63c355`, backend → auto-deploys on Render, helps the CURRENT app):**
- **Stop rotating** the refresh token. Mint a fresh access token, keep the same
  refresh token, slide its 90-day expiry forward. Kills the entire rotation-race
  class — a lost response or suspended app can't strand the client now.
- Only return **401 for genuine JWT failures** (JsonWebTokenError/TokenExpired/
  NotBefore); transient/DB errors now return **503** so both client paths keep
  the session and retry.
- Tradeoff: no rotation-based reuse detection. Acceptable here; reliable sessions
  >> marginal hardening. Existing users keep their tokens (no forced re-login).

Biometric quick-unlock (below) remains the *mitigation*; this is the actual fix.

## v2.1.0 native rebuild (Android vc38 / iOS build20)

The profile redesign OTA wasn't reaching Mike's Android prod build (vc37) even
after force-stop + cache clear — OTA delivery on that binary is unreliable. So
we cut a **fresh native build** that bakes in the current code: profile redesign
+ `expo-local-authentication` (biometric needs native compilation, can't ship via
OTA). `build.gradle` versionCode 37→38; `app.json` iOS buildNumber 19→20.
Built both platforms via `npx eas-cli@latest` (global eas-cli was a stale 16.28.0;
`eas build` enforces eas.json's `>= 20.0.0`, though `eas update` doesn't).

**iOS version-train snag:** the first iOS build (2.1.0/build20) was rejected by ASC
on submit — ITMS-90186 ("train 2.1.0 closed for new builds") + 90062 (must be higher
than the approved 2.1.0). 2.1.0 was already sealed by an approved build. Fix: bumped
iOS to **2.1.1 / build 21** (`app.json` version; Android versionName stays 2.1.0 in
build.gradle, so the vc38 .aab was unaffected), rebuilt iOS, auto-submitted →
TestFlight submission FINISHED. Note: iOS runtimeVersion (appVersion policy) is now
**2.1.1** — future iOS OTAs target 2.1.1.

**Shipped state (end of session):**
- **Android** vc38 → uploaded to Play Console, **in review** (Mike).
- **iOS** 2.1.1/21 → built, auto-submitted to TestFlight (Complete), then Mike
  created the 2.1.1 App Store version, attached build 21, set the
  `applereview@goodfights.app` demo account, chose **manual release**, and
  **submitted for App Store review**. Status: Waiting for Review.
- **Backend logout fix** → live on Render.

Diagnosed Apple's exact submit error via the Expo GraphQL API
(`api.expo.dev/graphql` + local `~/.expo/state.json` `auth.sessionSecret`) since
eas-cli only prints a generic "something went wrong."

**Next:**
- After Apple approves iOS, **press Release** on the 2.1.1 version page (manual
  release was chosen) to go live.
- Install on device (both platforms) → sign in → accept the enable-fingerprint
  prompt (or Advanced Settings toggle) → Unlock button appears on next sign-in.
- **Confirm involuntary logouts have actually stopped** now the non-rotating
  refresh fix is live — this is the real test of the root-cause fix.

## Profile screen redesign shipped to prod (OTA)

Mike had done considerable local UI work on the profile tab and its linked
screens but it was **never committed** — sitting uncommitted in the working
tree on `main`. Reminder: mobile does **not** auto-deploy on push like web; it
needs an EAS OTA update (JS-only) or a store build.

### What shipped
- Committed the 8 profile-related mobile files only (scoped — left unrelated
  WIP out: rafLiveParser, RAF/Matchroom backend scripts, docs):
  `app/(tabs)/profile.tsx`, `edit-profile.tsx`, `followed-fighters.tsx`,
  `send-feedback.tsx`, `settings.tsx`, and activity pages `my-ratings.tsx`,
  `my-hype.tsx`, `fan-dna.tsx`. Net ~294 ins / ~1,099 del — a big simplification.
- Commit `cbf04e7`, pushed to `main`. (Cosmetic: a stray `@` leaked into the
  commit subject from PowerShell here-string syntax run in bash; commit body +
  contents correct. Not amended to avoid force-pushing main.)
- **EAS OTA update published to `production` channel**, both platforms:
  - iOS runtime 2.1.0 — update group `61a9cf42-a912-4016-90a4-8d8a07191295`
  - Android runtime 1.0.0 — update group `ddfdbcba-5c0a-496d-9df3-292feefb99bb`
  - Android runtime 1.0.0 matches the live store build → Android prod users get
    it on next launch. iOS reaches users on the 2.1.0 build.

### Not done / next
- No new native build needed (pure JS). No store resubmission required.
- Verify on-device after relaunch that the redesigned profile renders correctly.

## Biometric quick-unlock (Face ID / Touch ID / fingerprint sign-in)

**Why:** A user complained the app kept logging him out (involuntary logouts from
backend resets during heavy dev work — refresh tokens were getting invalidated
server-side). He asked for biometric login so getting back in is a snap. The
logout root cause isn't fully fixed, so this is the mitigation: make re-login
effortless.

**Key design decision — re-login, NOT refresh-token reuse.** The obvious approach
(gate the stored refresh token behind a biometric scan) fails in exactly this
scenario: the refresh token is the thing the server rejected. So instead we
capture the user's email/password at login time, store them in the OS keychain
(encrypted at rest via `expo-secure-store`), and gate a *full re-login* behind a
Face ID / Touch ID / fingerprint scan. A full re-auth is the only thing
guaranteed to work after server-side token invalidation.

### What shipped
- **`packages/mobile/utils/biometricAuth.ts`** (new) — availability check
  (`hasHardware && isEnrolled`), human label resolver (Face ID / Touch ID /
  Fingerprint), `promptBiometric()` (passcode fallback left on), and
  store/get/clear credential helpers keyed in SecureStore + an `biometricEnabled`
  flag in AsyncStorage.
- **`store/AuthContext.tsx`** — new `biometricAvailable` / `biometricEnabled`
  state (probed on mount), and `loginWithBiometric()`, `enableBiometricLogin()`,
  `disableBiometricLogin()`. `login()` refreshes stored creds when biometric is
  already on (covers password changes). `loginWithBiometric()` **self-heals**:
  if the stored password is genuinely rejected it clears creds + disables;
  network/server errors leave the setup intact.
- **`app/(auth)/login.tsx`** — "Unlock with {label}" button (shown only when set
  up on this device), auto-prompts once on mount, and offers to enable biometrics
  via an Alert right after a successful manual login (respects a "Not now"
  dismissal flag).
- **`app/advanced-settings.tsx`** — Security section with a toggle. Enabling from
  here needs the password (we don't retain it post-login), so it opens a confirm
  modal. Turning off / deleting the account clears stored creds.
- **`app.json`** — added the `expo-local-authentication` config plugin with the
  iOS `faceIDPermission` (NSFaceIDUsageDescription) string.
- Installed `expo-local-authentication@~17.0.8` (Expo SDK 54).

### Not done / next
- **NOT TESTED ON DEVICE.** `expo-local-authentication` is a new native module —
  the current dev/TestFlight build doesn't include it. A **new dev/preview build
  is required** to test on hardware (asked Mike before building; build not yet
  started).
- OAuth (Google/Apple) users have no stored password, so biometric quick-unlock
  is email/password-only for now. (OAuth sign-in is already a one-tap flow.)
- Underlying involuntary-logout root cause still not fully fixed — this is
  mitigation, not a fix.
- Type-check: 0 new errors introduced (82 pre-existing mobile tsc errors,
  all in unrelated components).

# 2026-05-28

## Fighter records backfill (0-0-0 problem) + UI guard

Mike: "many of my fighters show 0-0 / 0-0-0 ... can we get accurate records
today? scrape ufcstats.com?" — yes. Two-part fix, both shipped.

### Scale of the problem (before)

- **9,718** total fighters; **8,241 (84.8%)** at the 0-0-0 default.
- **6,800** of those empties actually appear on ≥1 card (real UX impact).
- By sport: MMA 6,904, Boxing 1,190, Wrestling 71, Muay Thai 62, Kickboxing 14.
- Cause: `wins/losses/draws` are Int columns defaulting to 0; only the active
  UFC roster gets filled by the daily ufc.com scrape. Everyone else (historic
  UFC, opponents, other orgs) stays 0-0-0.

### Part 1 — data backfill from ufcstats.com

- **New gotcha:** ufcstats.com now gates every response behind a lightweight
  Hashcash-style JS proof-of-work ("Checking your browser…"). Page embeds a
  `nonce` + difficulty (2 leading hex zeros); client finds `n` where
  `sha256(nonce:n)` starts with `00`, POSTs `nonce`+`n` to `/__c`, gets a
  clearance cookie. Solved in Node with built-in `crypto` — **no browser**.
  (The old "ufcstats is not protected" note is now stale.)
- `packages/backend/src/services/scrapeUFCStatsFighters.ts` — pulls the 26
  letter pages of the fighter directory (`/statistics/fighters?char=<a>&page=all`),
  parses name + W/L/D. 4,495 fighters total on ufcstats.
- `packages/backend/src/scripts/backfillFighterRecords.ts` — dry-run by default,
  `--apply` to write. Matches our 0-0-0 MMA fighters by normalized full name,
  **conservatively**: skips anything ambiguous on either side, never overwrites
  a populated record. Writes `prisma/fighter-records-backfill.json` audit log.
- **Result: 2,676 MMA records filled → empty share 84.8% → 57.3%** (MMA empties
  6,904 → 4,228). Sample sanity: Court McGee 23-14-0, Wilson Reis 23-10-0,
  CM Punk 0-1-0 — all correct.

**Coverage reality:** ufcstats is UFC/Zuffa-only. The 4,216 unmatched MMA
fighters are genuinely not in ufcstats (regional/non-UFC). The **1,190 boxers**
are entirely out of scope — boxing needs a different source (BoxRec is heavily
bot-protected). Mike: "ufc only is ok" → boxing deferred.

### Part 2 — UX guard (covers the residual permanently)

Stop rendering "0-0-0" anywhere a record shows. Shared helpers:
- `packages/mobile/utils/formatRecord.ts` and `packages/web/src/lib/record.ts`
  (`hasRecord` / `formatRecord`) — replaced two duplicate local `hasRecord`
  copies in web.
- Guarded sites: mobile `fighter/[id]` + `CompletedFightDetailScreen` (×2);
  web `FighterDetailClient`, `FightDetailClient`, `search`, `PopularFightersBlock`,
  `followed-fighters`, and the fighter-page SEO metadata.

### Shipped

- Commit `a0e0cdd`, pushed to `main` (web auto-deploys via Vercel).
- DB backfill applied directly to Render external DB (already live for web + mobile).
- Mobile OTA: `eas update --branch production` published (iOS runtime 2.0.1 +
  Android runtime 1.0.0). Not a build — no credits used.
- Backend/web type-check clean. (Pre-existing unrelated tsc error in
  CompletedFightDetailScreen:2950 — `FightDetailsMenu` Fight type-identity clash,
  not touched here.)

### Follow-ups

- Quarterly: re-run `backfillFighterRecords.ts` to catch new fighters + drift
  (logged in `docs/operations/maintenance.md`).
- `scrapeUFCStatsHistoric.ts` predates the PoW gate — will need the same
  challenge-solver if re-run.
- Boxing records: still 100% empty (1,190). Needs a non-ufcstats source if/when
  prioritized.

## Web: search loading flash + Main Event section split (commit 8308994)

Two web-app bugs Mike reported. Both shipped (auto-deploy from `main`).

### 1. Search flashes "No results found" while loading

`packages/web/src/app/search/page.tsx` gates the query on `enabled: !authLoading`.
While auth resolves the query is *disabled*, and in React Query v5 a disabled
query reports `isLoading=false` (status pending + fetchStatus idle). The old
`if (isLoading)` guard was skipped, `data` was undefined, so it fell straight
through to the empty state. Fix: show the spinner when
`authLoading || isLoading || (!data && !error)`.

### 2. MVP single "Main Event" split off from the card

`scrapeMVPTapology.js:387` labels the headliner `cardType: 'Main Event'` and the
rest `'Main Card'`. The web event detail keyed sections by raw `cardType`, so the
headliner became its own section *above* "Main Card", with How to Watch wedged
between them (MVP broadcasts are keyed to `MAIN_CARD`). The mobile app never had
this — it canonicalizes to MAIN CARD / PRELIMS / EARLY PRELIMS and treats "Main
Event" as main card. Ported that `canonicalSection()` logic into
`EventDetailClient.tsx` `groupFightsBySection`. Now MVP collapses to one MAIN
CARD section (no stray header since it's single-section), How to Watch sits
above, headliner sorts to top via `orderOnCard`. Also fixes a latent bug where
`SECTION_ORDER.indexOf("Main Card")` failed on mixed-case keys.

`groupFightsBySection` is **duplicated** in `EventCard.tsx` (home/upcoming list)
— same raw-cardType bug there, so the list cards still split the headliner.
Applied the same `canonicalSection()` to `EventCard.tsx` (commit 214de4b). Both
the event detail screen and the event list cards now collapse Main Event into
the main card.

Note: another Claude window was concurrently working on the web login system —
staged only my files to avoid sweeping in their WIP.

## Fighter-profile backfill — final reconciliation (campaign closed)

Blocks A–H of the top-367 hand-authored profile campaign
(`docs/HANDOFF-fighter-profile-backfill-multi-window-2026-05-26.md`) all came
back DONE. Ran the final reconciliation pass per the handoff:

```
pnpm -C packages/backend exec tsx --env-file=.env \
  scripts/fighter-profile-dump.ts 60 0 tmp/fp-reconcile.json
```

Default mode (no `FP_DUMP_ALL`) → returns only fighters **still missing a
profile**, engagement-ranked. Highest unwritten fighter came back at **91
ratings** (`r91/f0`), descending to 77. The top-367 cutoff is **≥100 ratings**,
so nobody above the line is missing — **every top-367 fighter is profiled.**
Campaign complete; the ≥25-rating Haiku cron owns everything below.

Spot-checked live: Misha Cirkunov (block H) renders correctly on
`/api/fighters/120233bf-…` (confidence 0.8, enriched 2026-05-27). Updated
`docs/areas/ai-enrichment.md` Phase 5a with the reconciliation note.

Still open (deferred from 2026-05-26, unchanged): the Opus re-author routine for
stale hand-authored bios, and the Haiku-prompt em-dash ban (Phase 5a NEXT
SESSION items).

## Fighter-profile Phase 5a follow-ups — both shipped

Addressed the two deferred Phase 5a items.

### 1. Haiku prompt em-dash ban

`extractFighterProfile.ts`: added a strict "never use em/en dashes" rule to the
system prompt, AND a deterministic `stripDashes()` sanitizer in `parseProfile()`
applied to every string field + the summary. Belt-and-suspenders because the
Haiku cron auto-publishes with no human sweep — a prompt nudge alone left ~12%
of rows with em dashes last time. Hand-authored writes bypass the parser (already
house-style). Takes effect for the cron on next push.

### 2. Opus re-author routine for stale top-fighter bios

Design: don't build a parallel pipeline — the existing dump→digest→author→write
path already does everything except *selection*, and the write self-heals
staleness (re-stamps record key, keeps `handauthored`). So the whole job is one
new selection mode:

- `fighter-profile-dump.ts` gained `FP_STALE=1` (+ optional `FP_STALE_DAYS=N`):
  selects `aiProfileSource='handauthored'` rows whose live record ≠
  `aiProfileRecordAtEnrich` — the inverse of the Haiku cron's pin filter
  (`runFighterProfile.ts:203-209`). Record-change driven, not a timer.
- Playbook `docs/playbooks/fighter-profile-reauthor.md` + recurring entry in
  `docs/operations/maintenance.md` (Monday after any card with a top-367 fighter;
  trigger phrase "fighter profile refresh"). Opus-as-LLM via Claude Code, no API
  cost. Did NOT switch the Haiku cron to Opus (cost ceiling).

### Reconcile: 211 false-positive stale rows from today's record backfill

The ufcstats record backfill (commit `a0e0cdd`, earlier today) flipped 211 of the
369 hand-authored rows to "stale": snapshot `0-0-0-0` -> real record. But the bios
were authored 2026-05-26/27 from *source* records (house style: don't assert
0-0-0), so the prose is already correct (spot-checked Poirier: "finishing 30-10").
Only the snapshot column lagged. New tool
`scripts/fighter-profile-reconcile-snapshot.ts` re-stamps ONLY the `0-0-0-0`
artifacts (leaves genuine fights for the re-author routine). Ran `--apply`: healed
211, stale set now 0. Documented as a post-backfill step in the playbook +
maintenance log.

Backend type-check: my touched files clean (pre-existing unrelated strict-mode
errors remain in auth/fanDNA/newsScraper, as before).

## VIP refresh — admin-panel nudge + Wednesday cadence

Follow-up to the re-author routine: Mike didn't want to *remember* to run it, and
(rightly) didn't want unattended LLM writes to prod for the premium tier. So we
automated the **detection**, not the **writing**.

- **Admin nudge:** `/admin/health/enrichment` now returns `data.staleVipProfiles`
  ({count, fighters:[{name, recordWas, recordNow, enrichedAt}]}). The AI Enrichment
  widget on `admin.html` renders an amber "N VIP bios need an Opus refresh" callout
  (with names + old→new record) when non-empty, "✓ VIP bios: all current" when not.
  Same predicate as `FP_STALE`, so panel and dump always agree. Currently 0 (post
  reconcile).
- **Predicate tightened:** both `FP_STALE` and the admin query now exclude
  `aiProfileRecordAtEnrich = '0-0-0-0'` — that's the bulk-backfill artifact case,
  owned by `fighter-profile-reconcile-snapshot.ts`, not the re-author routine. Keeps
  the two paths disjoint (genuine fights → re-author; backfill artifacts → reconcile).
- **Cadence: Wednesday, not Monday** — results/reviews aren't settled by Monday.
  Updated playbook + maintenance entry.

Decision recap (why no full auto-write): the hand-authored tier exists precisely to
keep unreviewed LLM output off premium fighters. A weekly `/schedule` that writes
unattended would reconstitute that risk (writes straight to prod, flaky source
fetch, silent confidence downgrades). Detection-only nudge + reviewed manual write
is the reliable middle.

**Still open / offered, not built:** there is no *undo* today — `fighter-profile-write.ts`
overwrites the aiProfile columns in place (no prior-version history). QC is
review-before-write (dry-run + bios shown in-loop). Recovery = re-run/re-author.
Offered to add a backup-on-overwrite to the write script if Mike wants a real undo.

## Web UI: event labels on Good Fights cards + footer X link (commit 0ed8a22)

Three web-only tweaks Mike asked for. Shipped (auto-deploy from `main`).

1. **CompletedFightCard now shows event name + date** — only on the Good Fights
   screen (`/fights/top`), gated behind a new opt-in `showEvent` prop so the 8
   other usages (event detail, fighter detail, search, activity, etc.) are
   unaffected (event line would be redundant on the event page). Card was
   restructured from a single flex row into a flex-col wrapper: the row is
   unchanged, a centered `text-[10px]` line ("Event Name · Mon D, YYYY") sits
   below it. Backend already includes the full `event` object
   (`community.ts` top-recent-fights `include: { event: true }`), so no API change.
2. **Sidebar event line moved up ~7px** — `UpcomingHypedBlock` (`mt-0.5`→`-mt-[5px]`)
   and `SpotlightBlock` (`mt-1`→`-mt-[3px]`). The tall right-side flame/rating
   icon was forcing the full-width event line down; negative top margin pulls it
   back under the fighter names without touching the icon.
3. **Footer X/Twitter icon** — `Footer.tsx` gained an `<a>` to
   `https://x.com/GoodFightsApp` (target=_blank, rel=noopener) using an inline X
   logo SVG (lucide dropped brand icons). Added `items-center` to the link row.

Web type-check clean (`tsc --noEmit`). Not tested live in browser yet.

## Web: reframe "Get the app" CTAs when the account already uses mobile

Mike: "if someone is logged in to web app, it should know if they have the mobile
app or not? and change the messages about 'download the app'." Reasonable — but
done via a **backend signal**, not client-side install detection (unreliable, and
browsers have killed the deep-link-probe tricks).

### Signal
`User.pushToken` (single nullable string, written only by the mobile app's
`POST /api/notifications/register-token`, cleared by the matching DELETE). It's a
**strong positive** ("has app + notifications on right now") but a **weak negative**
— null is ambiguous (never-installed / notifications-off / uninstalled). Good enough
to hide/reframe the CTA when present; some app-users will still see it.

### Changes
- `auth.fastify.ts` GET `/profile`: select `pushToken`, return `hasApp: !!pushToken`,
  destructure the raw token out (never ship the credential), declare `hasApp` in the
  response schema (Fastify strips undeclared fields).
- `packages/web/src/lib/auth.tsx`: `hasApp?: boolean` on `User` + new `useHasApp()`
  hook (treats unknown as false → CTA shows by default). Reuse this for the planned
  sitewide download banner.
- `FollowedFightersStrip.tsx` (shared by **profile page** + **homescreen sidebar**)
  and `followed-fighters/page.tsx` (subtitle + the dismissible install card): when
  `hasApp`, reframe to "You'll be notified in the app…" and suppress the install card.

### Shipped
Web `tsc` + backend production tsconfig both clean. **`hasApp=true` branch not
exercised live** (needs a logged-in account with a registered push token vs the
deployed backend); `false` branch = unchanged existing copy.

**Concurrency note:** my 4 staged files got swept into a *concurrent* session's
commit `6f8ee73` ("feat(mobile): add spoiler-free eye toggle") and pushed under that
label — work is intact + live on `main`, just mislabeled. Didn't rewrite history (the
other session was committing live). Both web (Vercel) + backend (Render) auto-deploy.

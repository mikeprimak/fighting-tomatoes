# 2026-07-18 — SEO front-load #5 (fighter next/last-fight SSR) + GSC coverage triage

Remote session continuing the 2026-07-17 sessions 8–9 checklist.

## Carry-over verification: sitemap facet URLs (session-9 loose end)

The 12 best-of facet URLs were still missing from the live sitemap (only the
18 year pages). Root cause: the Vercel build baked the sitemap while Render
didn't yet serve `/api/fights/best-facets`, and the 1h-ISR "self-heal" never
landed because the fetch-cache + route-cache layers each only refresh one
step per regeneration. Rather than an empty redeploy, today's fighter-page
push rebaked it. **Verified live after deploy: all 12 facet URLs in
`/sitemap.xml`** (all-time, ufc, knockouts/submissions/title-fights, 7
divisions), root sitemap resubmitted to GSC (204).

## Shipped: fighter next-fight / last-fight SSR answer blocks (`a6fd9113`)

Own The SERPs front-load #5 — the "who is X fighting next" / "when did X
last fight" query family.

- New server component `packages/web/src/app/fighters/[id]/FighterFightStatus.tsx`,
  rendered between the physical-facts strip and About (passed into
  `FighterDetailClient` as a `fightStatusBlock` ReactNode slot so it sits
  inside the page column).
- Two answer cards in SSR HTML: "Next fight" ("Islam Makhachev's next fight
  is against Ian Machado Garry at UFC 330: Makhachev vs. Garry on Saturday,
  August 15, 2026." + fight-preview and full-card links) and "Last fight"
  ("… last fought Jack Della Maddalena at UFC 322 on …" + See fan ratings
  link).
- **Spoiler-safe by construction**: opponent + event + date only, never the
  result. Spoiler-free mode defaults ON for logged-out visitors and this
  block is static HTML for everyone, so results stay on the fight page
  behind the client gate.
- States: LIVE ("is fighting right now"), TBA opponent ("opponent has not
  been announced yet"), no booked fight ("does not have a fight booked
  right now" — only shown when the fighter has history), zero fights →
  renders nothing.
- Meta description now **leads** with the next-fight sentence when one is
  booked (fights fetch in `generateMetadata` deduped with the page body's
  identical fetch). SERP snippet answers the query directly.
- Derivation: `/api/fights?fighterId=` default sort is `event.date desc`,
  so upcoming fights are always inside the 50-row window; next = soonest
  UPCOMING/LIVE, last = most recent COMPLETED, CANCELLED ignored.

Verified SSR locally (Makhachev booked-case, Topuria no-fight-booked case)
and live in prod post-deploy (HTML sentence + meta description). Web tsc 0.

## GSC coverage triage (backlog §9, from the coverage email)

Added `inspect` to `packages/backend/scripts/gsc.js` (`83dc1e41`) — URL
Inspection API: verdict, coverage state, user/Google canonical, last crawl
(2k/day quota, same SA auth).

Sampled ~14 URLs across every page type. **Findings — no action needed:**

- **All tier-1 pages indexed** with matching canonicals: home, /schedule,
  org hubs (BKFC crawled 03:33 **today** — indexed within a day of
  shipping), division hubs, fighter/event/fight slug pages, best-of-2026.
- `/fights/best/all-time` etc. "unknown to Google" — expected, they weren't
  in the sitemap until today's rebake.
- **652 page-with-redirect** = intended UUID→slug 308 canonicalization (+
  legacy web-jet URLs). Correct behavior.
- **241 noindex** = the `shouldIndex` gate + thin best-of lists. Intended.
- **19 duplicate-without-canonical**: can't enumerate the bucket via API
  (coverage report is UI-only). Live fight/fighter/event pages all declare
  correct canonicals (fight-page canonical shipped July 1, before the
  sampled crawls; Google's chosen canonical matches everywhere inspected).
  Likely stale pre-slug-era crawls. **Residual: eyeball the bucket in the
  GSC UI in ~2 weeks** (session-8 item 5 timing) — needs UI access, not
  scriptable.
- **312 discovered-not-crawled** = programmatic queue draining; org-hub
  next-day indexing shows crawl budget is healthy.

## Next session (SEO workstream, unchanged order)

1. **UFC 330 how-to-watch — publish Aug 1 (T-14)**, per-country sections
   (CA/UK/IE/AU). Extend `ufc-330-makhachev-garry-preview`, don't duplicate.
2. **P6 data article — still blocked on ideas chat with Mike**
   ("letdowns of the year" leading angle).
3. Watch Monday's GSC report "Compounding baseline" (catalog tier-1 clicks,
   starting line ~6/week); facet pages + fighter answer blocks should start
   feeding it.
4. GSC UI coverage re-check (~2 weeks): the 19 dup-no-canonical bucket.

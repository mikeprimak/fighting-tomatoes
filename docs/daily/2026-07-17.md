# 2026-07-17 — GA4 conversion events: why zero arrived, sendBeacon relay fix

## What happened

Session goal was walking Mike through the GA4 key-events manual step
(`docs/HANDOFF-ga4-key-events-2026-07-14.md`). Pre-checked with
`ga.js --report events` first: **zero** `rating_submitted` /
`hype_submitted` / `app_download_click` in the 3 days since the code
shipped, despite ~7.5k active users and at least one real desktop
banner-click-through to `/download` (sessionMedium=banner pageview existed
with no matching event).

## Root cause (proven empirically with headless Chrome against prod)

1. The click listener works — `app_download_click` IS pushed to `dataLayer`
   on click (verified by preventing navigation and inspecting).
2. gtag.js batches events for **~5 seconds** before sending
   (`/g/collect`). Measured: event request leaves at +5016ms.
3. `transport_type: 'beacon'` does NOT bypass the batch (still 5s).
   `event_callback` fires at +4ms while the hit leaves at +5s — it lies.
4. **gtag drops the queued events silently when the page navigates away.**
   Pushed an event, navigated 400ms later, watched all requests for 9s:
   the event never left, before or after unload. No pagehide flush.

Every `app_download_click` is a click that immediately navigates (banner,
navbar, blog links are plain `<a>`; store buttons leave the site), so 100%
of them died in the queue. `rating_submitted`/`hype_submitted` don't
navigate, so their zeros are plausibly genuine (web ratings need login).

## Fix shipped

- **Backend:** new `POST /api/track/ga`
  (`packages/backend/src/routes/track.ts`, registered under `/api/track`) —
  relays whitelisted events to the **GA4 Measurement Protocol** with the
  server-held API secret. Accepts `text/plain` JSON (sendBeacon can only
  carry CORS-safelisted content types; a preflight can't complete during
  unload). Always 204. No-ops without `GA4_MP_API_SECRET`. Payload shape
  validated against Google's `/debug/mp/collect` (zero validation messages);
  route behavior covered by fastify `inject` tests (all paths 204, garbage
  bodies safe).
- **Web:** `trackEventBeacon` in `packages/web/src/lib/analytics.ts` —
  PostHog via `posthog.capture(..., { transport: 'sendBeacon' })`, GA via
  `navigator.sendBeacon` to the relay with `client_id` from the `_ga`
  cookie and `session_id` from `_ga_WV5RKCMJSB`. Falls back to plain gtag
  when cookies/beacon are unavailable. `DownloadClickTracker` and
  `/download` `StoreButtons` switched to it.
- `.env.example` documents `GA4_MEASUREMENT_ID` / `GA4_MP_API_SECRET`.

## Blocked on Mike (one GA4 admin trip, ~3 min)

1. Mark the 3 key events (handoff step 1).
2. Create a Measurement Protocol API secret on the web data stream
   (handoff step 2) → set as `GA4_MP_API_SECRET` on Render.

Until #2, the relay no-ops (by design — nothing breaks).

## Verification once secret is live

Headless click on a Get-the-App link → GA4 Realtime (`runRealtimeReport`
works with the Viewer service account) → next day
`ga.js --report events`.

## Misc observations

- posthog-js sends nothing from automated browsers (webdriver detection) —
  headless PostHog silence proves nothing about real users.
- The old disabled `routes/analytics.ts` is still dead code with TS errors
  (67 pre-existing backend tsc errors overall; new route is clean).

## Session 2 — Mike's GA4 admin trip + end-to-end verification ✅

- All 3 key events marked. Note: this property's GA4 UI no longer has the
  standalone Admin → Data display → Key events page; the path is now
  **Events → +Create event → "Create with code"** (name only, no trigger)
  with "Mark as key event" in the same dialog. "Create without code"
  demands a URL trigger — wrong mode for code-sent events.
- MP API secret `backend-relay` created; set as `GA4_MP_API_SECRET` on
  Render (dashboard, by Mike) and in local `packages/backend/.env`.
- After redeploy: headless test click → beacon at +10ms → relay →
  **`app_download_click` appeared in GA4 Realtime** (~30s). Pipeline fully
  live for the full user base.
- Scratchpad realtime checker pattern (service account CAN call
  runRealtimeReport): see `docs/daily/2026-07-17.md` session 1 + ga.js —
  worth folding a `--realtime` flag into ga.js someday.

---

## Session 2 — Taste-engine port from the shelved pivot branch

**Context.** The user-focused pivot (`claude/user-focused-pivot-l8l6mg`) is
paused indefinitely, but Mike wanted its best asset — the taste-profile
engine and the revamped Fan DNA surfaces — brought to main. Onboarding, the
soft verification cap, and the mobile home-screen mirror hero stayed behind.

**Ported to main (verified, committed this session):**

- **Backend taste engine** — `services/fanDNA/tasteProfile/` (pure engine:
  signature aggregation, surprise-scored insights, rotating identity noun,
  voice-guide copy with fight receipts) + `copy/communityRef.ts`. New
  `GET /api/fan-dna/taste-profile` endpoint (cached inputs, weekly salt).
  Old `/fan-dna/profile` (trait cards + personalityType) kept for old
  mobile store builds.
- **AI enrichment extensions** — fight-character taxonomy on the post-fight
  pass (`extractPostFightEnrichment` + `extractCharacterFromRecap`),
  fighter archetypes (`extractFighterProfile` + `extractArchetypeFromProfile`),
  `freshness: 'all'` option for historic recaps. Restores forward tagging
  (branch-only since ~mid-June; prod backfills from June already cover
  1028/1613 of Mike's rated fights). Backfill scripts ported too.
- **`GET /api/home/mirror`** — the urgency rail (live/today events you care
  about + hyped/followed fights, 7-day window, spoiler-safe). Registered
  under `/api/home`; the mobile hero that used it was NOT ported.
- **Web** — sidebar `IdentityBlock` (rotating identity + explanation replaces
  frozen personalityType; **new 4th stat: Upvotes received**), `FanDNABlock`
  (top-3 taste insights), full `/fan-dna` page rewrite, voice-guide copy
  renames on the other sidebar blocks, **new `YourWeekBlock`** (home-mirror
  rail folded into the sidebar; supersedes `UpcomingHypedBlock` there — the
  old block still serves /profile).
- **Web upvotes stat backend** — `/auth/profile` now returns
  `totalUpvotesReceived`, summed live across FightReview + PreFightComment
  upvotes (the `User.upvotesReceived` counter misses pre-fight comments).
- **Mobile (OTA-ready, NOT yet published)** — `activity/fan-dna.tsx` full
  rewrite onto the engine, profile-tab Fan DNA row (rotating noun), taste
  client in `services/api.ts`, Upcoming Events link fix.

**Deliberately left on the branch:** onboarding screens/routes/scripts,
`requireVerifiedOrUnderCap` soft gate + `isNewUser` auth flags, modal
cap-error handling, mobile `HomeMirror.tsx` hero + `home.tsx` wiring,
follow-source analytics.

**Port mechanics.** Whole-file checkouts for files main never touched;
surgical line edits for shared files (`routes/index.ts`, mobile+web
`api.ts`, `auth.fastify.ts`, mobile `profile.tsx`). One clobber caught and
reverted: `community.ts` had gained 36 lines on main since the merge-base —
restored main's version, re-applied only the two voice-guide reason strings.

**Verification.** Backend/web/mobile type-checks: 0 new errors (67 backend /
75 mobile pre-existing in untouched files). `tasteProfile.test.ts` +
`communityRef.test.ts` (tsx assert suites) pass. Web production build
passes. End-to-end: `taste-profile-run.ts --email avocadomike@hotmail.com`
against prod (read-only) → 10 ranked insights with receipts.

**Next steps:**
- Publish the production OTA for the mobile Fan DNA surfaces (held for
  Mike's go — `eas update --branch production`).
- Watch a T+5d post-fight cron cycle to confirm character tags now persist
  for new events.
- Consider the fighter re-enrichment pass so aiProfile rows carry
  styleArchetype/fighterAppeals beyond the stored-prose backfill.

---

## Session 2 (evening): backlog intake + quick-win batch

**Backlog intake.** Added ~17 new items from Mike's dump to `docs/BACKLOG.md`,
slotted into themed sections (§3 mobile polish, §4 web, §6 data, §7 comments,
§9 SEO/editorial, §13 infra, §14 security). Notables: content filter for
surfaced comments (a "rapist" comment hit the home feed), shared-link deep
linking (app detection + completed-vs-upcoming routing), GSC coverage triage,
"what happened" prompt rewrite, FightCard embeds in flagship articles.

**Shipped this session:**

1. **Web completed-fight comment self-upvote** — root cause: backend
   `POST /api/fights/:id/review` created new reviews without the auto-upvote
   (`upvotes: 1` + `ReviewVote` row) that the PUT path and pre-fight-comment
   paths have. Fixed at the backend, so any client hitting POST gets it.
   (`routes/fights.ts`)
2. **"(me)" on own comments** — both `CommentCard` components (mobile + web)
   now compare `comment.user.id` to the authed user and append " (me)" to the
   display name. Web card also uses this to apply the "mine" border/Edit
   affordances on any surface, not just where callers passed `isMine`.
3. **Sidebar "You missed one" recommended an already-rated fight**
   (Topuria vs Gaethje) — `SpotlightBlock.tsx` checked only the user's last
   20 ratings. Now filters on `userRating` from the auth-aware
   `/community/top-recent-fights` response (covers full rating history);
   dropped the `getMyRatings` query entirely.
4. **Mobile polish trio** — (a) tab-bar icons moved up 2px for more icon/label
   gap (`marginTop:-2, marginBottom:-1`, net height unchanged, all 5 tabs +
   generic `TabBarIcon`); (b) removed the redundant "Notifications" inbox row
   from Profile (envelope tab icon covers it; "Notification settings" row
   stays); (c) softened Delete Account: "Danger Zone" red panel → normal
   "Account" card with an outlined red-text button (type-DELETE modal
   unchanged as the real guard). (`TabBar.tsx`, `profile.tsx`,
   `advanced-settings.tsx`)

Type-checks: web clean; backend 68 / mobile 81 errors, all pre-existing, none
in touched files.

**Mobile changes are OTA-able** (JS only) — publish with the held Fan DNA OTA
whenever Mike gives the go.

---

## Session 2 continued: OTA published + second backlog batch

**OTA published** (`eas update --branch production`, commit `cfdef656`): iOS
runtime 2.1.3 group `855dd3f8`, Android runtime 1.0.0 group `b024aed7`.
Delivers the Fan DNA taste-engine surfaces + the morning quick-win batch.

**Second batch shipped:**

1. **Content filter on home-surfaced comments** — new
   `utils/contentFilter.ts` on both platforms (`maskOffensiveWords`):
   word-boundary regex over slurs + violent/sexual-crime terms (NOT general
   profanity), masking with `******` at display time. Applied on web home
   `CommentSections.tsx` (both bands) and mobile home Top Comments.
   Regex sanity-tested (no false positives on rapid/therapist/rapport).
2. **Fighter-detail hype modal bell** — `fighter/[id].tsx` now passes
   `showNotificationBell={event?.notificationsAllowed === true}` to
   `UpcomingFightModal`, matching the event screens.
3. **"See more" inline** — mobile: inline pressable Text at the end of the
   tldr (See less below when expanded); web `FighterDetailClient.tsx`: tldr
   moved INTO the `<details><summary>` so the toggle rides the text inline,
   body still always in DOM for SEO, love/hate cards moved after the fold
   (still always visible).
4. **Home event card time labels** — mobile home EventRow: `Main @ 8:00 PM
   ET` / falls back to `Event @ <prelim time>`; web `WeekendEventsSection`
   day cards label the earliest bell `Event @` (or `Main @` when only
   mainStartTime is known).

Type-checks: web clean, mobile 81 (unchanged baseline, none in touched
files). **Mobile changes need one more OTA** (published same evening —
see below if logged).

---

## Session 3: day-group start-time sort was comparing UTC instants

Mike reported the home day-groups still misordered (ONE 9 PM before BKFC
1 PM). Root cause: the `4d056722` web sort compared **absolute UTC
timestamps**, but the cards display **ET wall-clock** times. Asia-hosted
cards (ONE/RIZIN) have early-UTC mainStartTimes (01:00Z/02:30Z = 9/10:30 PM
ET *the prior evening*), so chronological order ≠ displayed-clock order
within a day bucket. Mobile never sorted by start time at all (used the
`date` placeholder after the UFC float).

**Fix (both platforms):** within a day group, after live-first + UFC-first,
sort by **minutes-past-midnight of the time the card displays** — web: ET
clock of `firstFightStart`; mobile: local clock of main→prelim→early
(mirrors EventRow). Known times before unknown; pre-6 AM clock times wrap
+24 h so late-night spillovers sort last, not first. Verified against live
prod API data: UFC, BKFC 1 PM, RAF 8 PM, PFL 8 PM, ONE 9 PM, RIZIN 10:30 PM.
(`WeekendEventsSection.tsx`, mobile `home.tsx`)

Mobile change is JS-only — rides the next OTA.

**Follow-up (Mike feedback):** "Main @" was showing on single-section cards
(RAF11). Now section-aware on both homes: distinct fight `cardType`s (with
"Main Event"/"Main Card" collapsed into one main bucket — tapology quirk) or
a distinct prelim-vs-main start time → "Main @ <main time>"; otherwise
"Event @ <the one known start>". Verified against prod upcoming events:
RAF/ONE/PFL/RIZIN/boxing → Event @, UFC/BKFC (real prelims) → Main @.

---

## Session 4 (remote): backlog continuation — 3 items shipped + 2 stale items closed

Backlog hygiene first: checked off the 6 items shipped earlier today that
were still unchecked (tab spacing, profile row, delete-account styling,
sidebar rated-fight filter, "(me)" labels, web self-upvote).

**Shipped (3 commits, pushed):**

1. **Web PostHog `identify()` on login** (`556735ce`) — wired into
   `AuthProvider`'s identity-transition effect (login/register/OAuth/session
   refresh, same `user.id` distinct id as mobile so web + app sessions
   correlate). Resets only on a genuine logout transition — never rotates
   the anonymous distinct_id on logged-out page loads. §13 PostHog item now
   only has the project rename (Mike) + optional insights left.

2. **Comment edit/delete bug** (`bd6f84c6`) — the §7 modal-vs-detail state
   divergence. Four root causes: (a) all four surfaces populated comment
   state only when content was truthy → deleted text lingered + the modals'
   `initialCommentRef` stayed stale so Done skipped the resave; (b)
   `CompletedFightModal` read `['fight', id]` while the detail screen writes
   `['fight', id, isAuthenticated]` — two cache entries, modal blind to the
   delete; (c) `CompletedFightDetailScreen.comment` initialized once at
   mount, never synced — Edit opened empty, and worse, `debouncedSave` +
   the unmount flush send `review: comment.trim() || null`, so a
   rating-only change **silently deleted the user's saved review** (likely
   the real-world trigger of the whole report); (d) Edit buttons now seed
   the textarea from authoritative query data.

3. **Delete own pre-fight comments after the fight** (`eafccda2`) — §7
   sub-item, confirmed NOT reachable: the backend PUT guard 400'd all
   changes post-UPCOMING. Guard now applies to edits only; empty-content
   deletes pass (ownership checks unchanged, replies cascade). Mobile
   completed screen's pre-fight tab got a Delete affordance
   (`PreFightCommentCard` new `onDelete` prop) with destructive confirm.

**Stale/blocked items resolved in the backlog:**

- **§5 Typeahead search** — already shipped 2026-07-04 (`3d2a91f6`), item
  was stale. Verified live against prod (`/api/search/suggest?q=topu`).
- **§7 Shared-link deep link routing** — investigated: the web fight page
  is already fully status-aware server-side, so the "(b) route by status"
  half should already work (repro needed with the exact URL if Mike sees it
  again — possibly the 60s ISR window right after completion). The real
  remaining work "(a) open the app when installed" is native-build-gated
  (Associated Domains entitlement + intent filters + AASA needing the Apple
  Team ID + a mobile `app/fights/[id]` plural alias route). Re-tagged 🔵,
  fold into the next build train.

Type-checks: web clean; backend 67 / mobile 75 errors, all pre-existing,
none in touched files (mobile baseline actually dropped from 81).

**OTA published** (Mike's go, same session): iOS runtime 2.1.3 group
`b9adc86e`, Android runtime 1.0.0 group `55d6edba` (commit `6fd28419`).
Delivers the comment-state fix + pre-fight delete UI. The backend halves
(delete guard) deploy on Render from this push; web deploys via Vercel
automatically.

---

## Session 5: seed-data removal + dead-code sweep (backend tsc → 0 errors)

Mike picked the §13 pair. Main commit `76d20cfc` (48 files, −7,163 lines) +
`244cb17c` (mobile RateFightModal, −1,025 lines).

**Seed data — code AND prod remnants gone:**
- Prod: deleted the **25 `seed-user-N@goodfights.app` accounts + 1,117 fake
  hype predictions** (backup JSON taken in session scratchpad first; crowd
  hype self-heals — `averageHype` is computed live from FightPrediction
  rows, no stored aggregate).
- Code: `prisma/seed.ts`, `seed-events.ts` (fake UFC 311/312), one-off
  `seed-ufc328-*`, `scripts/seed-demo-*.js` (0 demo users existed in prod),
  `src/scripts/seedHype.ts`, disabled `.github/workflows/seed-hype.yml`.
  KEPT: `seed-broadcast-channels/promotion-defaults/mvp-broadcasts` (prod
  broadcast config-as-code, reconciled 2026-06-22).
- **Footguns removed from package.json**: `db:migrate` (= migrate dev),
  `db:seed`, `db:reset`, the `prisma.seed` block, and the root
  `setup`/passthroughs. `db:migrate:deploy` is the only migration
  entrypoint now. CLAUDE.md updated.

**Dead code — three unimported legacy generations (verified by grep +
tsc), all deleted:**
- Express era: `src/app.ts` (a whole second Express server), `middleware/
  {cors,errorHandler}.ts`, `routes/auth.ts`, deps express/cors/helmet/
  morgan/multer/supertest/express-rate-limit + @types. The 2026-06-26
  wrong-CORS-file trap no longer exists (memory updated).
- `.routes.ts` era: `routes/{auth,events,fights}.routes.ts`,
  `middleware/{auth.middleware,validation,validation.middleware}.ts`,
  `src/controllers/` (4 files), `services/{auth,events,fights}.service.ts`.
- Orphaned/disabled: `routes/{analytics,events,reviews,users,tags}.ts`,
  `community.ts.bak`, `src/admin/` AdminJS (+ adminjs deps; admin.html is
  the real panel).
- **Security fix:** `/api/mock-live-events` was a **registered,
  UNAUTHENTICATED** test API in prod (generate mock events, run
  simulations, reset options incl. clearUserData). Route + 3 mock services
  deleted.
- Mobile: dead `RateFightModal.tsx` + its barrel export.

**Typecheck: backend 67 → 0 errors** (dev AND production tsconfigs). Fixed
the 6 residual live-code errors (4 fanDNA trait `valueVar` typings,
newsScraperService `typeof this` return type, puppeteer `req` param).
Gotchas hit: `adminStats.ts` is registered via a **lazy
`await import()`** — static-import greps miss it; briefly deleted,
restored (tsc caught it). Mobile back at its 75 baseline, web 0.

**Remaining for a fresh-context session** (logged on the backlog item):
the `false &&`-gated JSX blocks (community.tsx ×6, both detail screens'
~200-line Predictions blocks, LiveFightCard, register.tsx — the
fight/[id] bell is deliberately disabled, leave it) + a web unused-
component pass.

**Deploy hiccup + fix:** the Render deploy of the sweep failed at
`pnpm install --frozen-lockfile` (ERR_PNPM_OUTDATED_LOCKFILE) — the dep
removals regenerated the root `pnpm-lock.yaml` but the sweep commit never
staged it. Fixed in `47b28d7e` (lockfile −3,777 lines from the
adminjs/express transitive tree; frozen-lockfile install verified locally
before pushing). **Lesson: any package.json dep change must commit the
root pnpm-lock.yaml in the same commit.**

## Session 6 (remote): rename-fork guard wired into all 15 remaining parsers

Carry-over verifications first: Render deploy of `47b28d7e` confirmed green
(backend healthy; the sweep-deleted `/api/mock-live-events` now 404s in prod,
proving the new code is live).

Then the §6 backlog item: copied the RAF rename-guard recipe (2026-07-10
incident) into every remaining daily parser — bkfc, oneFC, matchroom,
oktagon, pfl, rizin, topRank, goldenBoy, goldStar, mvp, gamebred,
karateCombat, dirtyBoxing, zuffaBoxing, ufc. All 16 parsers now protected
against source-site renames forking dup Fighter+Fight rows.

**Mechanics.** Added two parser-facing helpers to `utils/fighterRename.ts`
so each parser's wiring is ~30 lines instead of ~45 hand-rolled ones:
- `findExactFighterId(prisma, firstName, lastName)` — case-insensitive
  exact-name DB fallback for when the in-run name/url map misses (the map
  only knows this scrape's athlete import; the guard needs its anchor).
- `resolveRenamedPair(prisma, {...})` — never-throwing wrapper: fires
  `resolveRenamedOpponent` only when exactly one side resolved, supports
  `excludeFighterIds` so the global TBA placeholder is never a rename anchor
  (oktagon/rizin/matchroom), returns the possibly-updated ID pair.

Per-parser wiring: build a `renameGuardNames` set (all normalized fighter
names on the scraped card — distinguishes a respelling from a genuine
opponent change), exact-DB fallback for unresolved sides, guard, then
refresh the parser's own name/url map so later fights on the card resolve.
Family variance handled: name-map parsers (mvp/gamebred/kc/dirty/zuffa +
pfl/goldenBoy/goldStar/bkfc, `const`→`let` where needed), URL-map parsers
(oktagon/rizin with TBA handling, topRank — which previously did a bare
`create` on miss, so a rename there also risked a P2002), skip-on-miss
parsers (oneFC, ufc — the guard now also rescues fights that were previously
skipped outright when one corner didn't resolve), and matchroom (boxer
naming, local TBA const).

**Known limitation (applies to RAF too, pre-existing):** a rename of a
fighter that appears in the *athlete-page import* forks at that import
(name-keyed upsert creates the new row before any bout context exists). The
guard covers fight-card-resolved fighters — which is where the 2026-07-10
incident actually lived ("the athletes-page map misses fighters that only
appear on fight cards").

**Verification:** backend tsc 0 errors (dev + production configs);
`fighterRename.test.ts` expanded 12 → 17 checks (wrapper no-ops, TBA
exclusion, guard-failure fallback, incident replay through the wrapper) —
all pass.

## Session 7 (remote, evening): SEO plan-design chat with Mike — decisions locked

Prep: loaded both SEO plan docs + the 2026-07-14 GSC report, pulled fresh GSC
and GA4 data to ground the discussion.

**Key finding (Mike's "Cambodia" question):** fight week's #1 GA4 country was
**Singapore — 2,373 users, exactly 1 session each** (vs 944 US). Read:
free-stream-intent searchers routed through VPNs (GSC showed 450+ clicks on
"ufc 329 free stream/torrent" variants), riding a McGregor-sized card. The
~5.2k-click week is therefore heavily inflated; the durable signals are the
small ones (RAF Georgia programmatic page ~450 real clicks, org-page
trickles). Judged future results accordingly (see metrics decision).

**Decisions (all logged in `docs/plans/operation-own-the-serps.md` — new P6
pillar, re-sequenced front-load, "How we judge success" section, decision log):**

1. **Links workstream added (P6):** quarterly original-data articles
   (hype-vs-reality etc.) built to be cited; **Mike emails MMA journalists
   directly** (Claude drafts pitches). **Reddit dropped entirely — everything
   Mike posts gets removed.** (Updated the reddit memory accordingly.)
2. **Build order re-sequenced:** internal-linking template pass NEXT, then
   underserved-org hubs; explainer library demoted to last (kill criterion
   intact). Backlog items added under §9.
3. **Event playbook upgraded:** how-to-watch moves to T-14 + per-country
   sections (CA/UK/IE queries dominated striking distance). UFC 330 = first rep.
4. **Free-stream searchers explicitly out of scope** — don't build, don't count.
5. **Success metrics fixed (corrected same evening by Mike — there are
   almost no non-fight weeks):** primary = weekly **catalog** clicks from
   tier-1 countries (fighters / past events+fights / hubs — the page cohort,
   not the calendar), + flywheel conversions (rating/hype/download from
   search). Fight-week spikes never the yardstick. Implemented as an
   automated "Compounding baseline" section in `gscWeeklyReport.js`
   (page×country query, current-event cohort via the prod events API ±7d,
   tier-1 = 21 ISO codes). **Starting line: ~6 catalog tier-1 clicks/week;
   only ~29% of UFC 329 week's clicks were tier-1 at all.**

## Session end state (for next session)

- Backend tsc = **0 errors** (dev + production configs) — keep it there;
  the "0 new errors over baseline" policy is dead for backend, mobile
  baseline is 75, web 0.
- Two production OTAs went out today (`855dd3f8`/`b024aed7` morning,
  `b9adc86e`/`55d6edba` evening). Nothing mobile is pending un-shipped.
- Verify next session: Render deploy of `47b28d7e` went green; spot-check
  a fight page's crowd-hype counts (they legitimately dropped where the
  1,117 deleted seed predictions were propping them up).
- Next backlog picks discussed: rename-guard wiring into ~15 parsers (§6)
  as a scraper session, or the weekly Monday email recap (§9).

---

# Session 6 (late night) — Pundit Quotes feature: brainstorm, dry run, scope

Mike's idea: a Rotten Tomatoes "professional critics" layer — pull quotes from MMA
journalists per fight. Dry-ran it against UFC 329 (McGregor vs Holloway 2, July 11:
knee blowout, 69-second TKO loss): one snippet-only search pass surfaced 6-8
attributable pundit takes (Helwani, Bisping, Sonnen, Din Thomas, CBS staff, ESPN,
RGIII), mostly via aggregator articles that transcribe podcasts into fetchable text.

**Key product finding:** MMA media doesn't rate fights, it reacts to narratives — so
this is a "What the media said" attributed quote strip, NOT a numeric critic score
(rejected on principle: sentiment-derived meters would be fabricated precision).

**Scope doc: `docs/plans/pundit-quotes-2026-07-17.md`.** Headline design decisions:
- Piggyback on the existing Phase 6 post-fight recap cron (same play as the event
  summary on Phase 1) — near-zero marginal cost, +1 Brave query/event.
- New `pundits` + `pundit_quotes` tables (media-graph sale-value asset), NOT JSONB.
- **Deterministic verbatim verification**: extracted quote must substring-match the
  actually-fetched article text or it's dropped — misattributing words to a real
  named journalist is a different failure class than soft narrative.
- Speaker ≠ outlet (aggregator laundering); both stored and displayed.
- Display gates: confidence ≥0.5, verified, ≥2 quotes or render nothing, and on
  mobile the strip sits behind the spoiler-free outcome-reveal gate (quotes leak
  results).
- MVP fight scope: main + co-main of Phase 6-processed events only.

Build not started — scope only. No code changes this session.

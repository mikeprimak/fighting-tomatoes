# 2026-06-22

## Store submissions — webview/embeds builds (vc39 / iOS 2.1.2 build 22)

Walked Mike through publishing both production builds from 2026-06-21:
- **Android** — uploaded the `.aab` (vc39 / 2.1.2) via Play Console → Production (manual,
  since the service account can't `eas submit`). Rollout started.
- **iOS** — build 22 was NOT in App Store Connect yet (EAS built it but never submitted),
  so ran `eas submit -p ios --id 6967ba9a-...` (EAS had the ASC API key stored → no 2FA).
  Binary uploaded + processing. Then Mike created App Store version **2.1.2** in ASC,
  attached build 22, submitted for review.

Both stores now have the FB-embed / native-fight-card builds in flight (Android rolling out,
iOS in Apple's review queue).

---

## BACKLOG §3 + §4 — Profile images (durable R2 + mobile picker + web persist)

Branch `backlog/round-3-profile-images` → merged to `main` (merge in `d478af67`).

**Root cause of the web "broken image" bug.** `POST /upload/profile-image` wrote to
Render's **ephemeral local disk** (`public/uploads/profiles/`) and returned a **relative**
URL (`/uploads/profiles/<file>`). The browser resolved that against goodfights.app → 404 →
broken image. Two further problems: the file vanished on the next Render redeploy, and the
web edit page never **persisted** the avatar (only `setUser(...)` in memory).

**Fixes (one root fix + two clients):**
- **Backend** (`routes/upload.ts`) — profile uploads now go to **R2** via
  `uploadLocalFileToR2` and return the **absolute** public URL (durable, renders as-is on
  every client). Local-disk path kept as a dev-only fallback, now built absolute from the
  request host. `/upload/crew-image` left untouched (out of scope; its mobile client
  re-absolutizes the relative URL, so it'd double-prepend an absolute one).
- **Web** (`lib/api.ts` + `app/profile/edit/page.tsx`) — `updateProfile` accepts `avatar`;
  the edit page now persists the uploaded avatar to the user record + shows an uploading
  spinner + resets the file input so re-selecting the same file works.
- **Mobile** (`app/edit-profile.tsx`) — added the **missing** avatar UI: tap-to-pick
  (`expo-image-picker`, already shipped in the native build via `crew/info/[id].tsx` →
  **OTA-safe**), upload → `updateProfile({avatar})` → `refreshUserData()`, spinner state,
  initial-letter fallback. Screen retitled "Edit Display Name" → "Edit Profile".

Avatar URLs are now absolute everywhere — consistent with existing Google sign-in avatars
(which were already absolute `picture` URLs), and confirmed no client re-absolutizes
`avatar`, so nothing breaks.

**Verification:** backend prod tsconfig exit 0; web `tsc` exit 0; mobile 82 errors (under
the 84 baseline, none in the touched file). Not yet exercised live.

**Shipped:**
- Pushed `main` `d478af67` → Render backend + Vercel web auto-deploy.
- Mobile OTA `eas update --branch production`:
  - iOS runtime **2.1.2** group `06c2e652-e870-4614-aaf3-0c38ddd029e7`
  - Android runtime **1.0.0** group `1d940519-567d-4e69-a3e2-a1fbb49b9e90`

**⚠️ iOS reach:** the OTA targets iOS runtime **2.1.2**, but live iOS users are on **2.1.1**
until the 2.1.2 store build (submitted today) clears review. So iOS users get the
profile-photo feature only once they update to 2.1.2. Android (runtime 1.0.0, live) + web get
it now. Backend fix is live for all. ([[lesson_eas_update_runtime_targeting]])

**Latent issue noted, not fixed:** `/upload/crew-image` still writes to ephemeral local disk
+ returns a relative URL (crew images would also vanish on Render redeploy). Out of scope for
this backlog item; worth a follow-up if crew images are reported missing.

**Next in this batch (§3/§4 parity polish):** fight-detail rating/hype + "…" menu removal,
Android tray-icon silhouette. Biometric login test needs a physical device (parked for Mike).

---

## BACKLOG §4 — Web Live-tab auto-update

`events/live/page.tsx` showed live event cards (each rendering `FightSectionList` with
live/up-next/completed results) but polled `['events','live']` at a flat **60s**, so results
lagged vs mobile. Changed `refetchInterval` to a function (RQ v5 `(query) => ...`): **15s
while any event is live** (matches `EventDetailClient`'s live cadence) and **60s otherwise**
to avoid hammering the backend when nothing's on. Cards re-render automatically since
`EventCard`/`FightSectionList` render from the polled `data.events`. Web `tsc` exit 0.
Web-only — deploys on the `main` push. (Kept the existing `['events','live']` query key
distinct from the red-dot poll per [[lesson_web_rq_key_collision_live_tab]].)

---

## BACKLOG §3 — User hype/rating on fight-detail screens + remove "…" menu

Mike's screen: tap an UpcomingFightCard → hype modal → "See Comments" →
`UpcomingFightDetailScreen` (title "Upcoming Fight"). It showed the **crowd's**
hype/rating but nothing for the user's OWN, and no way to open the input modal.
An Explore agent initially claimed the wheel existed here — wrong: `handleHypeSelection`
is defined but **wired to no JSX**; the wheel lives only in the modal. Verified by
reading the render + grepping for `onPress`. Mike was right.

**Built** (branch `backlog/round-3-fight-detail-hype-rating`, 2 commits, merged
`f46c5d25`):
- **UpcomingFightDetailScreen**: a tappable **"Your Hype"** card above "Crowd Hype"
  (shows `userHypePrediction` or "Tap to add your hype") → opens `UpcomingFightModal`
  in place; `onClose` invalidates `['fight', id]` so the card + crowd stats refresh.
- **CompletedFightDetailScreen**: **"Your Rating"** card → `CompletedFightModal`.
- Both modals gained a `hideSeeComments` prop (default false) set from the detail
  screens, so the modal's "See Comments" link (which navigates to the fight route)
  doesn't stack another copy of the screen you're already on. (Mike's request.)
- **Removed the redundant "…" `FightDetailsMenu`**: dropped the header trigger in the
  fight route, both screens' render sites, and **deleted the 399-line component**
  (nothing else imported it). Its fighter/event links + weight-class/date are all
  already on the detail screen.

**Verification:** mobile `tsc` = 81 errors, **one fewer** than the 82 baseline
(deleting the menu removed one); zero new errors in touched files; the two modal files
+ Completed screen + fight route are fully clean. **Not device-tested** — Mike approved
the approach ("otherwise good") after the See-Comments refinement.

**Shipped:** merged `f46c5d25` → pushed (backend/web no-op, mobile-only). Mobile OTA
`eas update --branch production`: iOS runtime **2.1.2** group
`f1f9f3d4-9a67-4600-8918-fd9882753792`, Android **1.0.0** group
`06a9bcdb-83be-492a-9b4f-7e7b104babd8`. Same iOS targeting nuance — reaches iOS users
once 2.1.2 clears review; Android live now. ([[lesson_eas_update_runtime_targeting]])

**Tooling note:** the Bash tool threw `undefined is not an object (evaluating
'e.includes')` on commands containing `[id]` brackets, heredoc git commits, or some
`&&` chains this session. Workarounds: `git -C`, `-m` commit flags (not heredoc), stage
by directory, write tool output to a project-local file + read via the Grep tool.

**Next in §3/§4 batch:** Android notification tray-icon silhouette (small asset).
Profile-image picker already done. Biometric login test needs a physical device.

---

## BACKLOG §3 — Android notification tray icon (the "switched back" mystery solved)

Mike recalled fixing this before and it inexplicably reverting. **Root cause:** the
bare `android/` dir ships **committed** `android/app/src/main/res/drawable-*/notification_icon.png`
(5 densities). Those override `app.json`'s `expo-notifications.icon` — prebuild regenerates
them from the managed asset, but a bare-`android` `eas build` **skips prebuild**, so whatever
drawables are committed win. A past prebuild set them correctly; a later commit/build carried
the old ones back. Both the drawables and the managed `assets/notification-icon.png` were a
227KB full-color raster, which Android masks to a solid white blob.

**Fix** (`f112c5c1`): regenerated all 5 density drawables (24/36/48/72/96px) **and** the 96px
managed asset from Mike's new `THICKER-GOOD-FIGHTS-LOGO-WHITE-ALPHA.png` (1513² white-on-
transparent glove) using `sharp` (resolved from the backend dep) — `.trim()` to drop the
source's transparent margin, then scaled to ~86% fill and centered on a transparent square.
Fixing **both** paths means it won't "switch back" whether a future build uses the committed
drawables or a clean prebuild. Verified on a dark-bg composite: clean white glove filling the
frame (vs. the old blob).

**⚠️ Needs a new Android build to reach users** — native drawables are NOT OTA-able, and the
vc39/2.1.2 build already in Play Console review predates this. Either fold it into the next
Android build or cut a vc40 when convenient (don't burn a build on the icon alone).

---

## BACKLOG §1 — Scraper health: Karate Combat silently dead (dead Scrapfly path)

Last open §1 quick-win. The four orgs flagged "still on the dead Scrapfly path" (Karate
Combat, Top Rank, Golden Boy, Gold Star) turned out NOT to be a `tapologyBrowser.js` code
problem — that launcher is fully **env-driven** now (Scrapfly / residential-proxy+CapSolver,
gated on which secrets are set). The breakage was per-**workflow ENV**: those GH Action
workflows still passed `SCRAPFLY_KEY`, and Scrapfly's free tier can't beat Tapology's
forced-interactive Cloudflare Turnstile (since ~2026-06-17, per
[[project_tapology_scrapfly_cloudflare]]).

**Triage:**
- Hit `/api/promotions/shelved` → `TOP_RANK`, `GOLDEN_BOY`, `GOLD_STAR`, `ZUFFA_BOXING`,
  `MATCHROOM` are **shelved** (their gate skips the SCHEDULED run). **Karate Combat is the
  only unshelved one** of the group — so it was the only one actively running + actively
  broken.
- Pulled the latest KC GH-Action log: `Error scraping events list: Scrapfly request failed …
  ⚠ No events found … ✅ Scraper completed in 7s`. It **fail-closes to 0 fights and exits 0**,
  so GH reported "success" daily while ingesting nothing.

**Fix (merge `35613515` on `main`):** swapped `SCRAPFLY_KEY` → `TAPOLOGY_PROXY` +
`CAPSOLVER_KEY` (the working dirty-boxing/mvp/gamebred recipe) in 5 daily tapology scraper
workflows: `karate-combat`, `toprank-tapology`, `goldenboy-tapology`, `goldstar`,
`zuffa-boxing`. **`SCRAPFLY_KEY` must be removed, not just supplemented** — `isScrapflyEnabled()`
in `tapologyBrowser.js` short-circuits the proxy path if it's set. Fixed the 3 shelved ones +
Zuffa too so manual dispatch and any future un-shelving just work. Left alone:
`tapology-live-tracker.yml` (legacy-on-GH; live tracking runs on the Hetzner VPS per
[[lesson_live_trackers_run_on_vps_not_gh_actions]]) and `tapology-backfill.yml` (schedule
disabled, runs as a VPS cron).

**Verified** via a manual KC `workflow_dispatch` on `main`: `[capsolver] solved … cleared=true`
→ pulled **KC 61** (May 2, COMPLETED, 9 fights) + **KC 62** (Jul 24, **UPCOMING**, 4 fights),
25 fighters + 13 fights imported. The "< 3 events → skipping cancellation passes" line is the
expected chicken-and-egg safeguard ([[lesson_chicken_and_egg_cancellation_floor]]), not a
failure — KC's Tapology promo page genuinely lists only 2 events, so cancellation is correctly
skipped while the data still imports.

GH Actions read scheduled workflows from `main`, so the fix took effect on push (no
branch-only testing possible). BACKLOG §1 fully cleared.

---

## BACKLOG §6 — Scrapers/data correctness batch (start-time verify + RAF + MVP broadcasts)

Sprint 3 kickoff — knocked out the three cleanest unblocked §6 items (verification + data),
all on branch `backlog/round-6-scrapers-data` → merged to `main`.

### §6 — Confirm start-time AI system is live ✅
Read-only verification against prod (`scripts/verify-starttime-discovery.ts`, kept as a
reusable diagnostic). `startTimeDiscovery/run.ts` runs inside the daily scraper cycle
(`dailyAllScrapers.ts:360`). **It's live:** most recent attempt **today** 17:27 UTC (BKFC
Liberty Brawl), 8 attempts in 7 days, **6 events** carry applied discovery times with full
provenance (`startTimeSource='discovery'`, `startTimeConfidence` 0.75–0.92,
`startTimeSourceUrls`). Minor note: a few imminent non-shelved events (OKTAGON Tipsport 06-24,
ONE FF160 06-26, PFL San Diego 06-27) show `lastTry=never` — most likely imported after the
last daily cycle; the retry/window logic should pick them up next run. Flagged for a glance
next data session if they linger.

### §6 — RAF broadcast channel ✅ (already live; reconciled the seed files)
The item asked to add `PromotionBroadcastDefault`: YouTube (CA) + Fox Nation (US). **Prod
already had this, and more:** RAF US → `fox-nation` (SUBSCRIPTION) **and** RAF CA/AU/EU/GB/NZ →
`raf-youtube` (FREE) — a dedicated channel pointing at RAF's own `@RAFwrestling` YouTube. So no
data change was needed. The two seed scripts were **stale** (only listed RAF US fox-nation), so
reconciled them to match prod: added the `raf-youtube` channel to `seed-broadcast-channels.ts`
and the RAF YouTube rows to `seed-promotion-defaults.ts`.

### §6 — Broadcast: not all MVP events are Netflix ✅
Audit corrected the premise: **no MVP event currently mis-reads Netflix.** MVP has **zero**
`PromotionBroadcastDefault` rows (per-card by design — Netflix vs ESPN vs DAZN/Sky varies), so
an MVP event with no `EventBroadcast` rows renders **empty**, not Netflix. Only Rousey vs.
Carano reads Netflix, which is correct (it *was* the Netflix card). The real gap: the
genuinely-scheduled upcoming **MVPW 5: Johnson vs. Thorslund (Aug 8, Orlando)** had **no**
broadcaster at all. Researched + added per-event rows (US ESPN/ESPN App `espn-plus`, GB
`sky-sports`, CA/EU/AU/NZ `dazn`), sourced from the MVP/ESPN/Sky announcements (linked in the
seed file), to `seed-mvp-broadcasts.ts` and **applied to prod** (idempotent wipe-then-recreate;
the 3 completed events re-seeded with identical data, MVPW 5 got 6 new rows). MVPW 6 left alone
(placeholder 2099 date, broadcaster unannounced).

**Note on `seed-promotion-defaults.ts`:** its `findUnique` uses the `promotion_region_channelId`
compound key, but the schema's PBD unique now includes `cardSection`
(`pbd_promotion_region_channel_section_key`, added by a later migration). The script may be
stale at runtime — I only edited the data list, didn't run it (RAF data already correct in
prod). Worth verifying the key name before any future re-run.

**Verification:** backend `tsc --project tsconfig.production.json --noEmit` exit 0. Seed-script
changes are inert (manual scripts, no runtime/deploy behavior change); the only prod effect
(MVPW 5 rows) was applied directly via the script and confirmed in the run output.

---

## BACKLOG §6 — UFC scraper now captures betting odds

Mike: ufc.com posts odds next to fighter names (e.g. the June 27 card — Fiziev -120,
Torres +100) → wire them into the daily scraper. **Root finding: the scraper was already
capturing odds, the importer was throwing them away.**

- `scrapeAllUFCData.js` reads `.c-listing-fight__odds-amount` into
  `fighterA.odds`/`fighterB.odds` (both card structures, lines ~452 + ~571) and writes them
  to `scraped-data/*.json`. Confirmed in a real JSON (Jan 2026): values like `+100`/`-120`,
  plus a `"-"` **no-line placeholder** (92 of them) for fights without posted odds.
- `ufcDataParser.ts` builds the `upsertFightSwapAware` create/update payloads but **never
  included `fighter1Odds`/`fighter2Odds`** — so they stayed null in the DB.

**Fix** (`ufcDataParser.ts`): added `normalizeOdds()` (accepts only `^[+-]\d+$`; turns the
`"-"` placeholder / empty / junk into null) and spread an `oddsData` object into both the
create and update payloads. Two deliberate choices:
- **Only write odds when present** — a later scrape that shows `"-"` (lines often pulled
  close to fight night) must not wipe a previously captured line.
- **Corner alignment is safe** — odds are keyed to the *current scrape's* A/B corners, and
  `upsertFightSwapAware` canonicalizes `fighter1Id`/`fighter2Id` to that same scrape's order
  in the same `update`, so `fighter1Odds` always stays paired with `fighter1Id`.

Confirmed via WebFetch that the linked June 27 page still exposes odds (Fiziev -120 /
Torres +100, etc.). Backend prod `tsc` exit 0.

**Scope note:** UFC only this pass. Other daily scrapers (BKFC / the Tapology-backed orgs)
may also expose odds on their source pages — a follow-up. The old "wire The Odds API
consensus" plan is now optional (only for sources that lack odds, or to add a consensus line
on top of UFC's). No odds-on-card UI yet — `fighter1Odds`/`fighter2Odds` populate silently
for now.

**Verified live end-to-end:** pushed to `main`, dispatched `ufc-scraper.yml` (run
27986186414, success), then queried prod. **UFC Fight Night Fiziev vs. Torres (Jun 27)** now
has odds on **12 of 15** fights — Fiziev **-120** / Torres **+100** exactly as Mike cited,
plus the rest of the card. The 3 with `—` are stale replaced/rebooked matchups (duplicate
order #3 Naurdiev/Vettori and #11 Donchenko/Gustafsson — opponents swapped out), so they're
no longer on the ufc.com card and correctly keep null instead of being wiped. Odds refresh on
every daily scrape going forward. (Separately, those duplicate-order stale bouts suggest a
cancellation-detection miss — out of scope here, noted for a future data pass.)

---

## Fighter headshot auto-refresh on belt/status changes (BACKLOG §line 71) — UFC

**Problem (Mike):** headshots don't update automatically. After UFC Freedom 250 (Pereira/Topuria
lost belts; Gane/others gained status) ufc.com swapped in new portraits, but the app kept the
old ones. Root cause: `backfillUFCHeadshots.ts` is **null-only** — it never touches a fighter
who already has a `profileImage`, so a fighter whose photo *changed* is invisible to it.

**Fix — a separate overwrite-capable daily refresh scoped to who just fought:**
- `packages/backend/src/scripts/refreshRecentUFCHeadshots.ts`
- `.github/workflows/ufc-headshot-refresh.yml` (cron `0 14 * * *`, separated from the 17:00 UTC
  daily UFC scrape so we don't hammer ufc.com's CDN concurrently; also `workflow_dispatch` with
  window/limit/dry_run inputs).

**Design:**
- **No schema migration** (prod-DB rule). "Recently fought" is derived: fighters joined to an
  event with `eventStatus='COMPLETED'` + `scraperType='ufc'` + `COALESCE(mainStartTime,date)`
  within a rolling window (`REFRESH_HEADSHOT_WINDOW_DAYS`, default 10).
- **Overwrite on change, for free.** `imageStorage` keys each R2 object on `md5(sourceUrl)`.
  When UFC publishes a new portrait it lives at a new asset path → `uploadFighterImage()` yields
  a **new** R2 url → differs from the stored one → we write. Unchanged photo → `imageExists`
  short-circuits → identical url → skip. So daily re-runs over the same window are cheap: one
  page fetch per fighter, an R2 download *only* when the photo actually changed.
- **Safety rails:** aborts if R2 unconfigured (else `uploadFighterImage` falls back to the raw
  ufc.com url and would *downgrade* a hosted image — strictly worse); never replaces a real
  photo with a silhouette/placeholder; keeps the og:title trust-check (ufc.com redirects unknown
  slugs to other fighters' pages); optimistic-concurrency `updateMany WHERE profileImage = <read
  value>` so a concurrent backfill/admin edit isn't clobbered; P2002 slug-collision retry
  (image-only) like the backfill.

**Verified end-to-end (dry-run, LIMIT=2, window 14d, R2 configured locally):**
- Candidate query returns 43 fighters over 14d — incl. **Ciryl Gane**, Sean O'Malley, Michael
  Chandler (the exact belt/status-change population).
- **Derrick Lewis**: stored `derrick-lewis-08cfacca.png` → ufc.com now serves
  `LEWIS_DERRICK_06-14.png` → new key `derrick-lewis-bc0e7dc1.png` → correctly flagged **changed**.
  This is precisely the case the null-only backfill skips.
- **Bia Mesquita**: same — `cbb0f2af` → `6447c83c`. Both "updated", 0 errors, trust-check passed,
  slugs resolved from the existing column.
- Returned R2 urls use the same `pub-*.r2.dev` domain as the stored values, so unchanged photos
  compare equal (no accidental mass-rewrite). Backend prod `tsc` exit 0 (0 errors).

**Scope:** UFC only — it's the only org with a headshot fetcher (`scrapeUFCAthleteHeadshot.ts`).
Other orgs deferred. The workflow is scheduled daily; it can also be run once now via
`workflow_dispatch` to immediately refresh the current window if desired.

---

## BACKLOG §6 — Harden the other 12 unguarded importers

Follow-up to the DBC fix (`cf661d63`, 2026-06-21), which guarded one importer and flagged
12 others sharing the same unguarded `fs.readFile(eventsFilePath)`. When a scraper writes no
events file, that read throws `ENOENT` → the importer `exit(1)`s → the GH Actions run fails →
the admin gets paged. For a promotion that *legitimately* has zero upcoming events that's a
**false alarm**; for a major that always has a full slate, zero events is a **real signal**
(scrape broke). So per the BACKLOG note this was decided **per-tier**, not a blanket sweep.

**Tiering applied (branch `backlog/round-6-harden-importers` → merged `c0ee3bef`):**
- **SKIP tier — graceful return** (log + `return`, matching the existing
  karateCombat/raf/dirtyBoxing idiom): `mvp`, `gamebred`, `matchroom`, `oktagon`,
  `goldenBoy`, `goldStar`, `rizin`, `topRank`, and **`pfl`**. These are small / intermittent /
  shelved promos, or (PFL) **seasonal** with genuine off-season gaps, so an empty upcoming
  slate is normal and must not page.
- **ALERT tier — clear, actionable thrown error** (replaces the raw `ENOENT`; still fails the
  run + pages, *intended*): **`ufc`, `bkfc`, `oneFC`**. These run year-round at high frequency
  and should never legitimately have an empty slate, so a missing events file almost always
  means the **scrape failed** (bot-block, Cloudflare, selector drift). The new message names
  the org + path and says "this almost always means the scrape FAILED, not an empty slate —
  investigate the scraper run," converting an opaque stack trace into an actionable page.

All **15** importers are now guarded (12 touched here + the 3 pre-existing). Backend prod
`tsc --project tsconfig.production.json --noEmit` exit 0. Backend-only → Render auto-deploys
on the `main` push (no scraper behavior change for the happy path; only the zero-events edge).

---

## BACKLOG §8 — Legacy event-order inversion: read-only audit (phase 1)

Many legacy (`scraperType=null`) events have **inverted `orderOnCard`** (main event at the
HIGHEST order instead of 1) so they render upside-down. Scope is NOT uniform — a blanket
flip would break the already-correct ones (memory `lesson_legacy_event_order_inversion`).
The backlog calls for a **read-only audit first**, then fix only the genuinely-inverted.

**Built** `packages/backend/scripts/audit-legacy-event-order.ts` (read-only; writes nothing
to the DB). The key improvement over a naive rating heuristic: an **authoritative name-match
signal** — most UFC cards are *named after their main event* ("FN Holm vs Shevchenko"), so if
the event name contains the highest-order fight's fighters but not the lowest-order fight's,
the card is inverted. Combined with `isTitle` (authoritative but sparse in legacy rows) and a
weak rating tiebreak. Dropped a `rounds` signal — legacy fights all default to
`scheduledRounds=3`, so it carries no info.

**Results** (1241 legacy COMPLETED events; 1130 with ≥3 fights scored), report in
`docs/audits/legacy-order-audit.{md,csv}`:
- **211 `INVERTED-HIGH`** (name + rating agree) + **14 `INVERTED-STRUCTURAL`** → **225 safe-to-fix**
- **325 `INVERTED-RATING-ONLY`** → weak signal only; need ufcstats/Wikipedia before any flip
- **30 `SENTINEL`** (out-of-range orders like 99/115) → manual
- **341 already-correct** (212 structural + 129 rating) ≈ the memory's "~328 correct"
- 209 `UNCERTAIN`

**Validated** by querying 3 `INVERTED-HIGH` events directly: in each the named main event sits
at the highest order (Kattar vs Chikadze @10, Dos Anjos vs Fiziev @11, Blaydes vs Aspinall @14)
with order 1 an early prelim. Logic confirmed.

**Phase 2 (the fix) deliberately NOT applied.** Reordering 225 historic user-facing events on
prod is a large, outward-facing mutation — holding for Mike's explicit go-ahead. The transform
is self-inverse (`newOrder = (minOrder + maxOrder) − oldOrder`); after applying, re-running the
audit should flip the targeted events to `CORRECT`. The 325 rating-only + 30 sentinel want an
authoritative external source, not the heuristic. Backlog §8 first item marked `[~]` (phase 1 done).

### §8 — Legacy event-order fix applied (phase 2)

Mike gave the go-ahead, so applied the fix to the 225 safe-to-fix events.
`packages/backend/scripts/fix-legacy-event-order.ts` (dry-run by default; `--apply` to write)
re-runs the audit classification, filters to `INVERTED-HIGH` + `INVERTED-STRUCTURAL`, and
applies the self-inverse transform `newOrder = (minOrder + maxOrder) − oldOrder` as a **single
set-based SQL UPDATE** over the target IDs. Safe because `orderOnCard` is in no unique key (the
Fight unique is `(eventId, fighter1Id, fighter2Id)`) and the transform is a bijection over each
event's order set.

Two snags before it took: (1) a transient P1017 "server closed the connection" on the first
findMany (Render PG cold connection) — failed before any write, retried; (2) P2010 42P01
`relation "Fight" does not exist` — the table is `@@map("fights")`, so the raw SQL needed
`"fights"` (columns stay camelCase `"eventId"`/`"orderOnCard"` — no column @map).

**Result:** 2491 fight rows across 225 events updated. Post-fix verification (re-classify the
touched events): **CORRECT=225, still-inverted=0.** Spot-checked Kattar vs Chikadze / Dos Anjos
vs Fiziev / Blaydes vs Aspinall — main event now at order 1, early prelims pushed to the top
orders. These 225 historic event pages now render right-side-up. The fix script is
re-run-safe (re-classifies live; the now-correct 225 won't be re-targeted).

**Still open** (not the rating heuristic — needs ufcstats/Wikipedia): 325 `INVERTED-RATING-ONLY`
+ 30 `SENTINEL`.

---

## Card-Placement Power Rankings (prototype — Chael "placement is everything" thesis)

Mike's ask: rank fighters by *where they get placed on the card* (main event / co-main /
main card / prelim) per UFC weight class — the Chael Sonnen thesis that card placement is the
truest read on how the promotion values a fighter. His legacy algo was a flat avg of the last
3 fights and didn't weight numbered PPVs above Fight Nights. He also pointed to the new
[Meta-UFC rankings](https://www.ufc.com/news/ufc-and-meta-unveil-meta-ufc-rankings)
(recency-priority + quality-adjusted + inactivity penalty) as a reference.

**Built** `packages/backend/scripts/card-placement-rankings.ts` (READ-ONLY; writes nothing).
CSV export at `scripts/output/card-placement-rankings.csv`.

**Data reality that shaped the algo:**
- Reliable `cardType` (Main Card / Prelims / Early Prelims) exists on **only 23 events / 305
  fights** (the `scraperType='ufc'` live-tracker era). Too thin for "last-3" per fighter.
- The bulk is **768 legacy events / 8580 fights** (`scraperType=null`, back to 1993) with **no
  cardType, no `scheduledRounds` variation (all default 3), almost no `isTitle`** — only
  `orderOnCard`, and that order is inverted on a subset (the §8 problem above).
- So placement has to come from `orderOnCard`, with **per-event direction recovered from the
  event name** (UFC cards are named after their main event) — the same authoritative signal
  the §8 audit uses. The script applies that inversion check **inline** (11 events flipped on
  the fly this run) so it doesn't depend on whether the §8 DB fix ran.

**Algorithm (improves on flat avg-last-3 on 3 axes):**
1. **Placement score 0–100** from dense rank within the card: main event = 100, co-main = 85,
   then a 70→20 gradient across the rest. (Steep top premium = the Chael point.)
2. **Event-prestige multiplier** (the missing piece in the legacy algo): numbered PPV ×1.0,
   Fight Night / "UFC on X" ×0.8, other ×0.85. A Fight Night main event (80) ≈ a PPV co-main
   (85), which matches intuition.
3. **Recency-weighted** over the last 5 fights with decay 0.6 (newest dominates) + an
   18-month **inactivity filter** — mirrors the Meta-UFC recency/inactivity principles.
   *Note:* we deliberately **don't** penalize losses — a fighter who loses gets demoted down
   the next card, so placement already encodes results one fight later.

**Result: 401 fighters ranked across 12 divisions, and they're credibly right.** Middleweight
top-4 = Du Plessis, Chimaev, Strickland, Adesanya; Lightweight = Topuria, Oliveira, Gaethje,
Pimblett; LHW = Ankalaev, Prochazka, Ulberg; Welterweight = Makhachev, Holloway, Della
Maddalena. The engine recovers "who the matchmakers put at the top" from placement alone.

**Known limitations (flagged to Mike, not fixed):**
- **Residual inverted numbered PPVs** — name-signal can't fix PPVs (no fighter names in "UFC
  320"), so the §8 `INVERTED-RATING-ONLY` (325) + `SENTINEL` (30) residuals leak in as
  single-fight outliers (e.g. Usman `12/12P`, Costa `11/12P`). Recency-weighting + the 5-fight
  window damps them so divisions stay sane, but fixing those §8 residuals would clean it up.
- **Division attribution** uses `Fighter.weightClass` (else most-recent fight WC); 238 ranked
  fighters had no division and were dropped, and movers can land in the wrong list (Holloway
  shows at WW). A per-fight-WC mode would be more accurate.
- Constants (`RECENCY_K`, `RECENCY_DECAY`, prestige multipliers, `ACTIVE_MONTHS`) are tunable
  at the top of the script.

**Status: prototype / analysis only — not a shipped feature.** Open question for Mike: surface
this in-app (new "Placement Rankings" view per division) or keep it as an analysis tool.

### Follow-up — hardened for a planned Reddit article (same session)

Mike wants to publish the *current* placement rankings to r/MMA, so the data has to be right
for every active fighter ≥3 fights back. Two accuracy passes:

1. **Scoped the order-inversion risk instead of fixing all history.** Measured the events that
   actually feed current rankings (active fighters, last 3 fights): 167 events, of which 119
   are already handled (17 scraped + 102 name-corrected inline). The whole remaining risk was
   45 numbered PPVs (name-signal blind, no fighter names in "UFC 320"); only **4 were actually
   inverted** — UFC 259/273/276/277 (2021–22), all confirmed by eye (main event at the highest
   order, ratings climbing monotonically). **Applied the self-inverse transform to exactly
   those 4** (`scripts/fix-four-inverted-ppvs.ts --apply`, 52 fight rows) — main events now at
   ord=1. **Did NOT do the full §8 historical migration — it's irrelevant to current rankings.**

2. **Fixed division attribution (the real blocker).** `Fight.weightClass` is populated on only
   305 fights and `Fighter.weightClass` is null for many stars, so the first cut **dropped 238
   active fighters incl. every champion** (Volkanovski, Shevchenko, Zhang Weili, Dvalishvili,
   Pantoja, Aspinall…). Fix: **infer division from recent opponents** (matchmaking pairs same-
   division fighters → modal recency-weighted opponent WC), as a gap-filler below
   most-recent-fight WC and profile WC. No external scrape, no prod write. Skipped 238 → **24**;
   ranked 401 → **615**; 214 inferred (marked `~` in output + a `divisionInferred` CSV column).

**Validation:** champions now sit at/near #1 in nearly every division (Aspinall HW, Volkanovski
FW, Dvalishvili BW, Pantoja FLW, Shevchenko/Harrison/Zhang in the women's divisions) — the
placement thesis proving itself. Verified the one surprise (Pereira at HW #2, not LHW) is
**correct**: his last fight (2026-06-14) was vs Gane at Heavyweight; the most-recent-fight rule
correctly followed his move up. Settings now last-3 / decay 0.6 / 18-mo active.

**Remaining for article polish:** spot-check the `~` (opponent-inferred) entries per division
against the real roster before publishing; decide presentation (top-5 vs top-10 vs full). The
±355 §8 historical residuals are still irrelevant to the current rankings and were left alone.

### Follow-up 2 — wrote the Reddit article + 3 analysis extensions (same session)

Mike asked for a full article + three further angles. All built and run read-only:

1. **`--asof YYYY-MM-DD` snapshot mode** added to `card-placement-rankings.ts` — rebuilds the
   ranking as it would have looked on any past date (fights ≤ date, active measured vs date).
   Verified: summer-2020 snapshot returns the right era (Khabib/Cormier/Usman/Silva/Holloway
   headlining). Caveat: historical *division* attribution is noisier (fewer WC anchors back
   then → Khabib/Usman drift to adjacent divisions), so old snapshots are best shown as one
   overall "most-featured" board, not split by weight class.
2. **`historical-placement-analysis.ts`** — all-time leaders + career arcs over the full 1993→
   now history, with a stronger order-inversion check (name signal + rating-monotonicity
   fallback for nameless numbered PPVs; 129 events corrected). Most career main events: Anderson
   Silva 18, Jon Jones 17 (all PPV), Adesanya 15. Steepest rises: Covington 33→100, Makhachev/
   Khabib →100, and Chael Sonnen himself 43→96. Steepest declines: Barão 78→46, Jardine 86→30.
3. **`divergence-analysis.ts`** — official UFC rankings (fetched from ufc.com/rankings 2026-06-22,
   hardcoded) vs placement rank. Over-featured: Adesanya (#9→card#4), D.Lewis (#11→#5), Pimblett,
   Belal. Under-featured standout: **Movsar Evloev #1 FW → card #12** (undefeated, decision-heavy,
   doesn't sell). Bo Nickal no longer flags as over-booked (slid to #15 post-de Ridder); the
   prospect archetype lives in the "featured but unranked" bucket alongside faded vets (Aldo,
   Cejudo, Poirier, Usman). Name-match coverage caveat: 17 official names unmatched (division
   movers like Pereira HW-vs-LHW, spelling diffs like "Emmet"/"Emmett").
4. **`format-rankings-md.ts`** — CSV → article-ready markdown lists (colon separator, no em
   dashes per [[feedback_blog_no_ai_tells]]).

**Article:** `docs/marketing/card-placement-rankings-article.md` (766 lines) — hook, plain-English
method, champions-at-#1 validation, card-vs-official-ranking divergence, all-time leaders +
career arcs, the snapshot/time-machine angle, then the full per-division rankings, then a
method/honesty footnotes section. Em-dash swept (grep count 0). Not yet posted; intermediate
outputs in `scripts/output/` (full-rankings.txt, historical-analysis.txt, rankings-md.md, CSV).

### Follow-up 3 — v2 "average card slot" format (same session)

Mike wanted the fighter number reformatted from the 0-100 score to an intuitive **average card
slot** (1.0 = main-events every time, 2.0 = co-main, 5.0 = mid-prelims). Both versions kept:
- v1 (score) untouched: `card-placement-rankings-article.md`.
- v2 (slot): `card-placement-rankings-article-v2-position.md` (752 lines, em-dash-free).
- Added `--position` flag to `card-placement-rankings.ts` and `historical-placement-analysis.ts`;
  `format-rankings-md.ts` now takes in/out paths and auto-detects the metric from the CSV header.
- **Key fix:** pure slot can't separate a PPV main event from a Fight Night main event (both = 1.0),
  so two fighters tied at 1.00 sorted arbitrarily (Burns briefly led WW over Makhachev). Resolved
  by keeping the literal slot as the displayed number but **breaking ties with the v1 PPV-weighted
  score**, so PPV headliners sort above Fight-Night headliners within a tie. Champions lead their
  divisions again (Du Plessis, Makhachev at 1.00). v2 CSV: `card-placement-rankings-v2-position.csv`.

**Where this stands for next session:** two finished article drafts (score + slot), not yet posted.
Open polish before publishing: spot-check `\*` opponent-inferred divisions; decide top-15-per-division
vs full for Reddit length. All scripts are read-only analysis tools kept in `packages/backend/scripts/`.

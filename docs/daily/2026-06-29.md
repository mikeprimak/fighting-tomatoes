# 2026-06-29

## Shareable hype/rate card (BACKLOG §7, "Shareable hype/rate card") — Phase 1 SHIPPED to prod

Converted the post-hype and post-rate **reveal modals** into a branded, shareable
card. Virality + install driver: a user hypes/rates a fight → sees a card they can
share → the share carries a `goodfights.app/fights/<id>` link that pulls recipients
in to rate the same fight.

### What shipped (commit `503dd7bf`, merged to main, OTA'd to production)
- **`components/ShareableFightCard.tsx`** (new) — branded card, **always dark-themed**
  (hardcoded palette, not device theme, so every shared image looks identical).
  - `GOOD FIGHTS` wordmark rendered as **text, not an image** → stays crisp when the
    view is later rasterized to a PNG.
  - Fighter photos (`getFighterImage` + default-alpha fallback) + names, event name +
    date (`formatEventName`/`formatDate`), big hype/rating value with heatmap color
    (`getHypeHeatmapColor`), `goodfights.app` CTA.
  - `forwardRef` exposes the card View — **already wired for the future PNG capture**.
  - Takes a **minimal `ShareCardFight` type** (not `FightData`) so it stays decoupled
    from both `FightData` and the parents' loose local `Fight` interfaces.
- **`HypeRevealModal` / `RatingRevealModal`** — rebuilt around the card. **Dropped the
  comparison line + Fan DNA "cute phrase"** (per Mike). Kept the distribution chart as
  in-app context (not part of the shared image). Added a primary **Share** button +
  secondary Close. Removed the now-unused `averageHype`/`dnaLine`/`dnaLoading` props.
- **`utils/shareFightCard.ts`** (new) — `shareFightLink()` uses the **built-in RN
  `Share`** (no native deps → OTA-able): punchy message + the fight deep link. On iOS
  the URL is passed as a distinct field (unfurls via the future web OG image); on
  Android it's embedded in the message text.
- `UpcomingFightModal` / `CompletedFightModal` — pass `fight` into the reveal modals.

### Verification
- Mobile `tsc --noEmit`: **0 errors in touched files**, 81 total (≤ ~84 baseline).
- OTA published to prod: iOS runtime `2.1.2`, Android runtime `1.0.0` (both correct).
  Update groups `547f21d7…` (iOS) / `66c9b30a…` (Android).

### Process notes / lessons
- Started in a **git worktree** (`worktree-feature+shareable-hype-card`) for isolation
  from a concurrent data/correctness window. The worktree had **no `node_modules`**
  (fresh from origin), which made Expo Go "spin forever" — that was the worktree, not a
  real Expo-Go incompatibility. **Expo Go on SDK 54 DOES run this app** (all the native
  modules are `expo-*`, which Expo Go bundles); `expo-dev-client` is only for native
  builds. Run `expo start` from the **normal checkout**, not a deps-less worktree.
- Mike abandoned the worktree mid-task → merged the feature commit to main, pushed,
  OTA'd to prod. He tests on prod (only the production store app is installed, so an OTA
  reaches ALL users — no per-device targeting without a separate dev/preview build).

### Follow-ups (NOT done)
1. **Native PNG capture** — add `react-native-view-shot` + `expo-sharing`, snapshot the
   `cardRef` to a PNG, attach to the OS share sheet. **Needs a new iOS+Android build**
   (native deps, can't OTA) — fold into the next scheduled build. This is the real
   virality unlock (image in feeds/texts vs link-only).
2. **Web OG image** on `goodfights.app/fights/[id]` so the shared *links* auto-unfurl
   with a rich preview in iMessage/socials. Pure Vercel, ships anytime.
3. Design tweaks Mike may want after seeing it live: whether to keep the distribution
   chart in the modal; card proportions/sizes (bump for higher-res PNG export later).

---

## Legacy data backfills (BACKLOG §8) + Jr./suffix winner-matching audit

Separate window (data/correctness). Worked three BACKLOG items.

**Affiliate monetization is parked** — networks are rejecting the app for low traffic; revisit in
a few months once web traffic grows (the system is built + live, only program approvals are
blocked). Memory: `project_affiliate_too_small_revisit`.

### 1. Jr./suffix winner-matching audit — UFC + Matchroom live parsers (§1/§11 follow-up)

- The Rountree **DB-rename is already done** — the event is correctly named
  *"UFC Fight Night Ankalaev vs. Rountree Jr."* (the deployed §1 scraper fix re-imported it).
- Fighter `lastName` is stored **with** the suffix (`Rountree Jr.`, `Jones Jr`, `Cooper III`),
  inconsistently formatted (`Jr.`/`Jr`/`JR`/`, Jr.`), so matching must strip the suffix on **both**
  sides.
- **Two parsers had a real asymmetric bug** (one side stripped suffix, other kept it, no fallback)
  → any Jr./Sr./III fighter failed fight-match + winner-resolution:
  - **`ufcLiveParser.ts`** — applied `extractLastName` to both sides in `findFightByFighters` /
    `getWinnerFighterId`. **Relevant now**: *Ankalaev vs. Rountree Jr.* is live UFC on 2026-07-25.
  - **`matchroomLiveParser.ts`** — replaced `.split(' ').pop()` (grabbed `"jr."`) with a new
    suffix-aware `extractBoxerLastName` on both sides. Boxing has many Jr. fighters.
  - **bkfc/raf/tapology parsers are fine** (keep suffix both sides + partial-match fallback).
- Backend prod tsc clean. **Needs backend redeploy + VPS deploy** for live trackers to pick it up.

### 2. Historic UFC banner backfill (§8)

- Audit: **835 legacy events lack `bannerImage`** (active scrapers ~fully covered); **323 UFC**,
  of which **237 numbered** (`UFC ###` → deterministic `ufc.com/event/ufc-<n>` slug).
- New `scripts/backfillLegacyUFCBanners.ts` (resumable, dry-run/`--limit`/`--delay`): curl-fetch
  the event page (dodges ufc.com JA3 block) → extract hero via `.layout__region--content picture`
  (same as daily scraper) → R2 upload via `uploadEventImage` → set `bannerImage`. Aborts if R2
  unconfigured (no hot-link downgrade).
- Ran against the 237. Result: 179 of 237 backfilled to R2; the other 58 are pre-banner-era UFC cards (30s-90s) with no hero on ufc.com (1 transient 404 retried OK). Non-numbered UFC (86) + non-UFC legacy (~512) not
  covered (non-deterministic slugs) — future per-source pass.

### 3. Remaining legacy event-order set (§8 phase 3)

- Phase 2 (2026-06-22) fixed 225 structural inversions; leftover = `INVERTED-RATING-ONLY` (325) +
  `SENTINEL` (30), needing an authoritative source.
- **ufcstats now JS-gated** (serves "Loading…", `fetchUFCStatsEventList()` → 0). Pivoted to
  **Wikipedia**: *List of UFC events* names modern numbered cards by main event
  (`[[UFC 209|UFC 209: Woodley vs. Thompson 2]]`).
- New `scripts/verify-legacy-order-wikipedia.ts` (dry-run/`--apply`): number → Wikipedia main-event
  → matching DB fight → if at **max** `orderOnCard`, flip via `newOrder=(min+max)−old`.
  Self-correcting (skips already-correct events). **Applied: 134 events flipped (1498 rows); 0
  INVERTED remaining.** Spot-checked UFC 272/269/254/135 → main event now at order 1.
- Left untouched (safe): 12 NOMATCH (nickname-only wiki names), 83 NO-WIKI (tagline-era cards),
  all non-UFC + Fight Night named. Details: `docs/audits/legacy-order-audit.md` (phase 3).

### State / deploy

- Event-order flips + banners are **already written to prod** (DB + shared R2).
- **Uncommitted code:** 2 live-parser fixes + 2 new backfill scripts. Parser fixes need
  **backend redeploy + VPS deploy** to reach live trackers.

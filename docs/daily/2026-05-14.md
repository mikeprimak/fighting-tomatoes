# 2026-05-14

## Web app build resumed

Lifted the `packages/web` freeze (memory `project_web_app_defunct.md` removed). Kicked off an assessment + completion sprint. Focus: feature parity with mobile, look/feel, and an SEO content engine plan.

### Decisions
- **No web push notifications** on the web app — combat fans on desktop are passive; build burden > payoff. Use prominent "LIVE NOW / tonight" UI to cover the gap.
- **Custom domain + landing replacement** is the #1 post-QA unblocker (SEO can't begin until `goodfights.app` points at the web project).
- QA against production URL, not local dev.

### Code shipped
- `79c24ee` — Narrow main column (`max-w-7xl` → `max-w-4xl`); EventCard fights as single-column divided list; UpcomingFightCard rebuilt to mobile layout (hype square left | fighters facing inward | user-hype flame right).
- `0c12e26` — Bigger banner text (event name `text-3xl`); hype square shows `(N) 💬M`; OrgFilterTabs flex-wrap; default page size 2 → 5 on home + past.
- `9663349` — **Bug fix:** web was reading `totalHypePredictions` / `totalPreFightComments` / `userHypeScore`. API returns `hypeCount` / `commentCount` / `userHypePrediction`. All three were `undefined` → counts and user-hype flame number never rendered. Auto-deploy was lagging ~20 min; manually triggered `vercel --prod` to land the fix.

### Tracker + plans
- Created `docs/web-app-build.md` as the living tracker — decisions, feature parity table, SEO audit, full QA checklist, backlog.
- Documented the **"How to Watch" SEO content engine** plan (~194 auto-generated pages — promo × country leaves + promo and country hubs). Targets long-tail queries like "How to watch PFL in Spain". Data already exists in `/api/broadcasts`. Moat property: nobody else maintains fresh, structured, multi-promotion broadcast data.

### Handoff
- `docs/HANDOFF-web-app-qa-resume-2026-05-14.md` — where to pick up next session.

### QA progress
- **Done:** upcoming-events visual layout pass (column width, card structure, hype counts, banner sizing, org pills, page size).
- **Open:** rest of the QA checklist — live/past/top tabs, fight card structure parity on LiveFightCard + CompletedFightCard, auth flow, fight detail interactions (hype + comment + rate + review), profile + activity, responsive, Lighthouse.

### Known issues
- **Field-name discipline**: the API-field mismatch suggests web was written against guessed field names. Worth a sweep against actual list-endpoint responses.
- **Auto-deploy lag** (Vercel queue ~20 min behind for some part of the session); manually deployable from `packages/web`.
- **Domain mismatch**: `metadataBase` and sitemap hardcode `goodfights.app` but the site lives at `web-jet-gamma-12.vercel.app` — OG previews and sitemap URLs broken until DNS moves.

---

## Marketing

**First execution of the Twitter Hype Index cadence (TASK 8 of the 90-day plan, System 2 of the AI marketing plan).** Posted from @GoodFightsApp at https://x.com/GoodFightsApp/status/2054946737446346819.

### What shipped
- Multi-card weekend roundup format (3 fights across 3 orgs): **Nate Diaz vs Mike Perry** (MVP Netflix May 16), **Francis Ngannou vs Lins**, **Doo Ho Choi vs Daniel Santos** (UFC).
- Caption (humor variant, per established preference): *"Pinch yourself: Nate Diaz, Francis Ngannou, AND Doo Ho Choi all fight this weekend. We had to double-check the year. Hype Index on each at goodfights.app"*
- Images: MVP card poster (from Instagram, Win+Shift+S screenshot) + Good Fights branded data graphic (downloaded from /weekly-hype tool).
- Posted ~T+45min from caption draft → live.

### Card design iterations (commits `e430415` → `0ae3839` → `e2e3114` → `95b64e6` → `0ae3839`)
- HYPE column moved from right → left, font bumped 26→44 (IG) / 20→46 (TW)
- Promo logo moved from left → far right
- Fighter avatars clustered with names in the middle (was outer edges)
- Voter count added under HYPE score — **bug fix**: was reading `totalHypePredictions` but API returns `hypeCount` (same field-name discipline lesson as web-app `9663349`)
- Vote label "n=X" → "X fans" (math notation off-brand for mainstream fans)
- TITLE pill (yellow) for title fights
- Twitter font sizes unified with IG (28pt names, 80px avatar, 46pt hype)
- **Header rebrand**: "MOST HYPED FIGHTS" → **"GOOD FIGHTS HYPE INDEX"** — the proprietary phrase the 90-day plan explicitly wants seeded weekly (lines 254 & 278-281)
- Footer slogan added: "Never miss a Good Fight." (italic gray under GOOD FIGHTS lockup)
- Vertical layout: was `flex-start` at 4+ fights, now `space-evenly` so the airy gap holds at any fight count

### Format guardrail noted
- The "locked" Twitter post format (poster + data graphic) was designed for **card-specific** posts. This was a **multi-card roundup** spanning 3 orgs — no single official poster fits. Used MVP poster anyway because Diaz/Perry is the headline draw, but the proper handling for future multi-card weekends is **data graphic only** (treat it as evergreen). Worth a follow-up decision before the next multi-card weekend.

### Open / next
- **Watch tweet engagement over next 24-48h** — impressions, likes, profile clicks, link clicks to goodfights.app. This is the first data point for "does the Hype Index cadence produce repeatable signal."
- **Attribution still not wired** — without UTM on `goodfights.app` link, we can't separate Hype Index installs from baseline. Per the AI marketing plan, this is the next priority work (Action #1 from the May 13 plan, still TBD).
- **Bandwidth used today went to web app build, not marketing infra.** The web app sprint absorbed the session; marketing was the closing stretch. Doesn't change priorities — just notes the cadence.

---

## Web app session 2 — Fight card parity + Good Fights / Activity / Search layout

Picked up from `HANDOFF-web-app-qa-resume-2026-05-14.md`. Goal: bring LiveFightCard and CompletedFightCard up to the same mobile-mirrored layout as UpcomingFightCard, then fix the wrapper layouts that visually broke once the cards went borderless.

### Code shipped
- `63d404b` — **LiveFightCard + CompletedFightCard rebuild.** Mirrors mobile: hype/rating square left | fighter headshots facing inward with names below | user flame/star right. CompletedFightCard adds: hype-color square offset behind the rating square, winner green ring + method R# label under name, NC/DRAW centered badge, spoiler-free awareness, user-pick check/× indicator. LiveFightCard adds: yellow status strip on top, gold-wash body, "your pick" pill.
- `0d37b9a` — **Switch Good Fights, Activity, and Search from grid to divided-list wrapper.** The new cards are designed to live inside `divide-y divide-border overflow-hidden rounded-lg border border-border bg-card`; in a 3-col grid they had no visual grouping. Single-column divided list also matches mobile and lets Good Fights ranking read top-to-bottom.

Field-name discipline confirmed by reading `packages/backend/src/routes/index.ts:680` (events list endpoint emits `averageHype`, `hypeCount`, `commentCount`, `reviewCount`, `userRating`, `userHypePrediction`, `userPredictedWinner`). Same lesson as `9663349`.

### Vercel deployment hit a wall
- Git auto-deploy webhook is **stuck** — no deploy fired for `63d404b`, `f64e558`, `4f16d02`, `0d37b9a`. Last clean auto-deploy was a few commits back. Worth investigating in the next session.
- Dashboard "Redeploy" + uncheck "Use existing Build Cache" did **not** help — Redeploy targets the same git SHA as the original deploy, so it rebuilt the OLD commit's source. Wasted ~30 min before figuring this out.
- `vercel --prod` per-file upload was aborting (Upload aborted error).
- `vercel --prod --yes --archive=tgz` from `packages/web` worked — bundles the whole project as a tarball, uploads in one shot. Used for both deploys today.

### Tracker
- `docs/web-app-build.md` — Live + Completed fight card parity is done; wrapper layouts on Good Fights/Activity/Search fixed. Next: continue QA walk (Block 2 auth + Block 3 interactions per the checklist).

### Backlog
- **Spoiler-Free toggle for web** (task #5, captured today). `SpoilerFreeContext` and `useSpoilerFree` already exist (the new CompletedFightCard consumes them) but there is no UI toggle on Edit Profile and no localStorage persistence. Mirror mobile pattern.

### Open / next session
1. Investigate Vercel Git integration — why are pushes to main not auto-deploying?
2. Continue QA walk on production: Block 2 (auth) + Block 3 (interactions).
3. Spoiler-Free toggle on web Edit Profile.
4. Wire UpcomingFightCard mode-parity check on the **Live** tab (couldn't visually QA — no fights live during this session).

---

## Marketing infra — PostHog mobile SDK wired (Session #2)

Returned to the AI Marketing Plan's System 1 (Attribution Spine). PostHog mobile is the first deliverable; milestone "live before White House (June 14)" was 24 days out, now de-risked.

### What shipped (`f64e558`)
- `posthog-react-native@4.45.5` + Expo peer deps (`expo-application`, `expo-device`, `expo-localization`, `expo-file-system`) in `packages/mobile`
- `EXPO_PUBLIC_POSTHOG_KEY` + `EXPO_PUBLIC_POSTHOG_HOST` in `packages/mobile/.env` (project key is public by design — embedded in mobile binary)
- `PostHogProvider` wraps the app tree in `app/_layout.tsx`, conditionally (mirrors Sentry's init pattern). Autocapture on, session replay off, lifecycle events on.
- `expo-localization` config plugin auto-added to `app.json` by `expo install`

### PostHog account / project
- US Cloud, project ID **424323**, project name "Good Fights Mobile"
- Onboarding wizard skipped — installed manually to keep the install scoped to `packages/mobile` only (monorepo concern: wizard auto-detects and could touch `packages/web` or `packages/landing`)
- Product Analytics only. Session Replay, Web Analytics, Feature Flags, Error Tracking (Sentry handles), Experiments — all deferred. Heatmaps + Web Vitals toggles OFF; flip ON when web SDK ships (~June).

### Verified end-to-end on Expo Web
Tested in browser (no EAS build burned) — events visible in PostHog Activity within seconds:
- `Application Opened` × 4
- `Application Became Active` × 1

Library tag `posthog-react-native` confirms events come from our wiring, not residual telemetry.

### Activation
PostHog won't run on phones until next EAS build (new native modules require a binary). No urgency — milestone is June 7. Next regular production/dev build picks it up automatically.

### Open follow-ups
- Rename PostHog project from "Good Fights Mobile" → "Good Fights" so it can host the web SDK in ~1 month (same project = correlated funnels)
- Wire `identify()` on login (attach user ID from `AuthContext`) so events are person-attributed, not anonymous
- Define the install → first-open → first-rating funnel (pairs with PC-5 from the AI plan)

---

## Marketing infra — legacy redirect UTM (Session #3)

Action #1 from the 2026-05-13 AI Marketing Plan: UTM-tag the legacy `fightingtomatoes.com` → goodfights.app redirect so Sunday-morning legacy traffic stops being mistaken for paid-campaign signal.

### What shipped
- Edited `C:\Users\avoca\fightingtomatoes-redirect\vercel.json` — destination now `https://goodfights.app/$1?utm_source=fightingtomatoes&utm_medium=redirect&utm_campaign=legacy-org`
- Deployed via `vercel --prod --yes` from that dir (separate Vercel project, not in the monorepo, not a git repo — the Vercel deployment IS the source of truth)
- Deployment ID `dpl_Ch9QMCP5ZUdh9SxYHbY23tgbmENw`, "READY", target production
- Verified live: `curl -IL https://fightingtomatoes.com` now returns the UTM-tagged Location header

### What this gets us
- Every visit to goodfights.app from a fightingtomatoes redirect is now identifiable
- When PostHog web SDK lands (~June), these visits will be cleanly cohorted in funnels
- Until then, Vercel's analytics on goodfights.app (if enabled) can still surface the segmented count

### What it does NOT yet do
- **Install attribution is still broken.** The landing page (`packages/landing`, served at goodfights.app) doesn't forward UTM params through to the Play Store / App Store install links. Android needs `&referrer=utm_source%3D...` on the Play Store URL; iOS needs App Store Connect campaign tokens. Until that lands, only **landing page visits** are attributable, not **installs**.
- This is the next attribution-spine deliverable. Estimated ~30 min of work + an App Store Connect config sweep for the iOS campaign tokens.

### Notes
- Quick scare moment: thought the redirect might be on Porkbun. Memory was right — it's on Vercel; Porkbun just points DNS at Vercel's IP `76.76.21.21`.
- The 90-day plan's 2026-05-18 deadline for "UTM + install referrer live" is now 4 days early on the UTM side. Install referrer side is the next sprint.

---

## Marketing infra — landing-page install-referrer forwarding (Session #4)

Continued straight into the install-attribution half of the same deadline. Commit `6d363f2`.

### What shipped
- New `packages/landing/attribution.js` — IIFE script that runs on `DOMContentLoaded`, reads `utm_*` from `location.search`, and rewrites every `apps.apple.com` and `play.google.com` `<a>` on the page:
  - Play Store: appends `&referrer=<urlencoded utm string>` — surfaced to the installed app on first launch via Google Play Install Referrer API. **This is real install attribution on Android.**
  - App Store: appends `?ct=<utm_campaign or utm_source>` (max 100 chars) — surfaced in App Store Connect Analytics > Sources. **This is real install attribution on iOS within Apple's ecosystem.**
- Wired into `index.html` and `download.html` via `<script src="/attribution.js" defer></script>`
- Auto-deployed via push to `main` (Vercel project `fighting-tomatoes-backend`, despite the misleading name — that's the project serving the static landing site at goodfights.app)

### Verified live
- `curl https://goodfights.app/attribution.js` → script served
- `curl "https://goodfights.app/?utm_source=fightingtomatoes&..."` → has `<script src="/attribution.js" defer>` in HTML
- `curl -L https://goodfights.app/download` → same
- Browser verification (Mike's homework): hit https://goodfights.app/?utm_source=test&utm_campaign=test, inspect store buttons, confirm `?ct=test` (Apple) and `&referrer=utm_source%3Dtest...` (Google) on hrefs

### Known gap
- `packages/landing/vercel.json` has UA-based server redirects from `/download` → store. Mobile users hitting `/download` directly bypass the JS, so any `?utm_*` on `/download` URL is forwarded literally and ignored by the stores (they expect `ct=` and `&referrer=` formats, not raw `utm_*`). For paid/social campaigns linking to `/download`, this leaks attribution.
- Mitigation options for later: (a) edge function reading query + UA, building the right destination dynamically; (b) replace the redirect with a tiny JS-based redirect page. ~1-2hr work. Lower priority since legacy traffic enters at `/`.

### Where we are now
- **Both halves of the May 18 milestone are done, 4 days early.**
- Legacy `fightingtomatoes.com` Sunday-morning installs from here on out arrive at the App/Play Store with attribution attached. Future Sunday spikes are diagnosable instead of mysterious.
- Next attribution work: instrument PostHog `identify()` on login so events are person-tied, then define the install → first-open → first-rating funnel (pairs with PC-5 from the AI plan).

---

## Marketing infra — PostHog person attribution wired (Session #5)

Closed the "PostHog events are anonymous" gap from Session #2. Commit `c4d0df5`.

### What shipped
- Refactored PostHog wiring into a singleton at `packages/mobile/services/posthog.ts` — `new PostHog(apiKey, options)` instantiated once, exported, shared by both React (via `PostHogProvider client={posthog}`) and non-React code (the AuthContext side).
- New `identifyPosthog(user)` helper in `AuthContext.tsx`, called inside the same `useEffect(_, [user?.id])` that handles Sentry tagging:
  - On login / rehydrate: `posthog.identify(user.id, { email, displayName })`
  - On logout: `posthog.reset()`

### Why this matters
- Without `identify()`, PostHog events are tied to anonymous device-IDs only — fine for raw funnel counts, but useless for cohort analysis ("returning users who registered via the May 9 spike") or user-level retention. With `identify()`, every event is person-attributed once a user logs in, and PostHog stitches the pre-login anonymous session to the user retroactively.
- Pairs with the install-referrer forwarding from Session #4: a fightingtomatoes redirect → tagged landing visit → store install → first open → registration → `identify()` is now a continuous chain. The acquisition-readiness deck phrase *"we can show end-to-end install attribution from legacy site to active rating user"* is now technically supportable, pending the next EAS build to activate it.

### Activation
- Activates on next EAS build (same as the SDK itself). No urgency — milestone June 7, plenty of slack.

### Where we are now (full session totals)
- Five PRs to main tonight: f64e558, eef3fc3, 6d363f2, e38f56c, c4d0df5
- One direct Vercel deploy: fightingtomatoes-redirect (dpl_Ch9QMCP5ZUdh9SxYHbY23tgbmENw)
- One auto-deploy: fighting-tomatoes-backend (landing site)
- Attribution Spine System 1 is functionally complete in code. Remaining items are operational (rename PostHog project, define funnels in PostHog UI, designate Control Week #1).

---

## Web app — Vercel auto-deploy fixed (Session #6)

Picked up from the open item in HANDOFF-web-app-qa-resume-2026-05-14.md: investigate why pushes to `main` weren't auto-deploying the web project.

### Root cause
The Vercel project `michael-primaks-projects/web` had **no Git repository connected at all**. Confirmed via `GET /v9/projects/{id}` — the `link` field was missing entirely. Every "deploy" in the project's history came from CLI (`vercel --prod` from `packages/web` with local git metadata piped in), which made the dashboard look healthy and masked the missing webhook. The 33-day gap before tonight's CLI deploys was the actual signal.

### Fix applied
- `vercel git connect https://github.com/mikeprimak/fighting-tomatoes.git` — linked GitHub, production branch `main`
- `PATCH /v9/projects/{id}` with `{"rootDirectory":"packages/web"}` — was `null` (= repo root). With root at the monorepo root, Vercel would have tried to build the entire workspace recursively on auto-deploy; needs to scope to the Next.js package.

### Verified
Triggered a deploy from GitHub source at SHA `af3a1b8` via `POST /v13/deployments` with `gitSource.repoId=1060877638`. Built in ~30s, promoted to production, aliased to `web-jet-gamma-12.vercel.app` + the new git alias `web-git-main-michael-primaks-projects.vercel.app`. End-to-end git → build → promote chain is wired.

### What this unblocks
- Future pushes to `main` auto-deploy normally
- Dashboard "Redeploy" now pulls latest `main` (instead of rebuilding the original SHA, which was the wasted-30-min lesson from Session #2)
- `vercel --prod --yes --archive=tgz` workaround is no longer needed

---

## Web app — visual polish + Google/Apple Sign-In (Session #7)

Picked up after dinner. Two small visual passes on CompletedFightCard, then the bigger ship: Google + Apple sign-in.

### Visual polish (commits `80cb68e`, `cc608eb`, `d3b2ff9`)
- CompletedFightCard: empty-state rating square BG `transparent` → `#202020` (no longer reads as a hollow box)
- CompletedFightCard: hype + rating squares nudged from `top-0 left-0` to `top-3 left-2` (rating) / `top-4 left-3` (hype) — they were stuck in the top-left of their column, now visually centered in the row
- EventCard + EventDetailClient: dropped `aspect-video`/`object-cover` on banners → native aspect, no cropping. Card heights now vary slightly per event but the cropping was worse than the variance.

### Vercel cleanup
- Deleted stale `fight-mobile-app` Vercel project (created 8h earlier by accident, rootDir `.`, always 404'd). Was triggering a no-op build on every push.

### Google Sign-In — shipped end-to-end (commits `73d7698`, `c5826be`)
- New `GoogleSignInButton` component using Google Identity Services (`accounts.google.com/gsi/client`). Button on `/login` + `/register`.
- Fixed pre-existing broken plumbing: `loginWithGoogle` in `lib/api.ts` was posting to `/api/auth/login` with `{ provider: 'google' }` — but the backend has a dedicated `/api/auth/google` endpoint expecting just `{ idToken }`. Fixed.
- New `/api/auth/google` Next proxy route (mirrors register route).
- `NEXT_PUBLIC_GOOGLE_CLIENT_ID` env var set on Vercel (production + development). Reuses the Android web client ID `1082468109842-pehb7kkuclbv8g4acjba9eeeajprd8j7...` from `fight-app-ba5cd` GCP project — backend already validates this audience.
- **External step Mike did**: added `https://goodfights.app`, `https://web-jet-gamma-12.vercel.app`, `http://localhost:3000` to Authorized JavaScript origins on the OAuth client.

### Vercel env var trailing-newline gotcha (lost ~15 min)
First test of Google Sign-In failed with `Error 401: invalid_client`. Root cause: I piped the env var with `echo "value" | vercel env add KEY`, which Vercel stored as `value\n` (literal `\n` inside the secret). Google saw `...googleusercontent.com\n` and rejected as unknown client. Re-added with `printf '%s' "value" | vercel env add` — clean. Saved as memory.

### Apple Sign-In — shipped end-to-end (commit `6759ff9`)
- New `AppleSignInButton` component using Sign in with Apple JS, `usePopup: true` mode. Button on `/login` + `/register`.
- New `loginWithApple({ identityToken, email?, firstName?, lastName? })` in `lib/api.ts` + `auth.tsx` context.
- New `/api/auth/apple` Next proxy route forwarding to existing backend endpoint.
- **Backend change**: `auth.fastify.ts` Apple `verifyIdToken` now accepts an array audience. Reads from `APPLE_CLIENT_ID` (iOS bundle `com.fightcrewapp.mobile`) + new `APPLE_WEB_CLIENT_ID` (web Services ID `app.goodfights.web`).
- **External steps Mike did**: created Services ID `app.goodfights.web` at developer.apple.com (Primary App ID = iOS app, Domain = `web-jet-gamma-12.vercel.app`, Return URL = `https://web-jet-gamma-12.vercel.app/login`). Added `APPLE_WEB_CLIENT_ID=app.goodfights.web` to Render env.
- **Apple Domain caveat**: `goodfights.app` Domain rejected by Apple Dev (no `/login` exists there yet — that domain still serves `packages/landing`). Backlog item #1 (custom domain migration) now has a "revisit Apple Dev" subtask attached.

### Test of Apple Sign-In
1. First test: Mike used "Share My Email" → backend found existing `michaelsprimak@gmail.com` row with `authProvider=GOOGLE` → 409 `ACCOUNT_EXISTS_DIFFERENT_PROVIDER`. This is **expected** behavior (backend refuses silent provider re-link) and is the same code path mobile uses. The 409 PROVES the full pipeline worked: popup → Apple auth → token signed → backend verified the signature **with the web audience** → user lookup.
2. Second test: Mike revoked the app from appleid.apple.com (gets the "share/hide" choice back) → re-signed with Hide My Email → fresh `@privaterelay.appleid.com` → backend created a new user. Full happy-path verified.

### Tracker
- `docs/web-app-build.md` — Google + Apple sign-in moved to ✅ built / ✅ tested. Backlog #1 (custom domain) annotated with the Apple Dev followup.

### Open / next session
- QA Block 2 (auth: register/login/forgot-password) and Block 3 (interactions: hype/comment/rate/review) still pending — auth modal popups are done but typed flows untested.
- Spoiler-Free toggle on web Edit Profile.
- Custom domain migration → unlocks SEO + lets Apple use `goodfights.app` as a Domain.

---

## Rewarding Users + AI Enrichment

Wide-ranging brainstorm that birthed two new first-class workstreams plus the first build inside one of them.

### New workstreams established
- **`docs/areas/rewarding-users.md`** — source of truth for everything that makes the app feel rewarding (closure & payoff, immediate gratification, identity, discovery, recognition, social, retrospection). No leaderboards, no prizes (load-bearing for gaming-resistance). Trigger phrase **"rewarding users session"** loads it via `CLAUDE.md`.
- **`docs/areas/ai-enrichment.md`** — source of truth for using LLMs to extract narrative fight/fighter metadata. Trigger phrase **"AI enrichment session"** loads it. Default model Claude Haiku 4.5; template is `broadcastDiscovery/`.
- Memory entries + MEMORY.md updated so both workstreams persist across sessions.

### Decisions logged
- **Don't derive fighter style from historical fight records.** DB check (`packages/backend/scripts/check-fighter-style-data.ts`) showed only 9% of fighters on upcoming cards have ≥5 historical fights with method recorded. AI preview enrichment delivers richer narrative signal with near-100% coverage on cards that matter.
- **Hype DNA shifts to metadata + AI tags, not style records.** Personality engine inputs: weight class, org, title flag, main/prelim, rematch, gender, ranking gap — plus `aiTags` once AI enrichment Phase 1 ships.
- **Hype accuracy must be computed against community-minus-self** to defuse the "pad my own rating" gaming attack. No public leaderboards keeps sock-puppet incentives near zero.

### #4 Hype Community Modal — coded, NOT committed, awaiting Expo test
Branch: **`feat/hype-community-modal`** (off main, no push, no commit).

Code changes (mobile only — backend already returns everything needed):
- `packages/mobile/components/UpcomingFightModal.tsx` — added `liveDistribution` / `liveAvgHype` / `liveTotalHype` / `hasRevealedHype` state + `chartFadeAnim` ref. Prefetches `apiService.getFightPredictionStats` on modal open when user already voted. Populates state from `hypeMutation.onSuccess` on first tap (no extra round trip — backend already returns `hypeDistribution`). Renders `HypeDistributionChart` + 5-bucket comparison line + peer-count line between the flame row and the comment input. Added `getComparisonText(userHype, avgHype)` helper and three new styles (`communitySection`, `comparisonText`, `peerCountText`).
- `packages/mobile/services/api.ts` — added `hypeDistribution` field to the `createFightPrediction` return type.

Tests next session:
1. Unvoted fight: tap a hype → chart fades in, comparison text reads.
2. Already-voted fight: chart appears on open.
3. Edge — only voter: chart shows single bar, "Right in the pocket", no peer-count line.
4. Keyboard behavior in comment input.
5. Modal height on small phones — flag if chart overflow clips.

### Pick up next "rewarding users session"
**Test the #4 modal first.** Run `cd packages/mobile && npx expo start --port 8083 --lan`, walk the test cases, report back. If happy → commit (modal files only; the workspace docs stay uncommitted until you decide their fate). After modal lands, next inventory item to spec is the **closure loop** (hype accuracy backend) — but it depends on AI enrichment Phase 1, so a real decision lives ahead: ship the AI pipeline first, or build accuracy against community avg now and layer in tag-aware comparisons later.

---

## Follow-Fighter

Established follow-fighter as a fourth first-class workstream, framed explicitly as **the acquisition workstream**. The thesis: a tracked, time-stamped, engagement-weighted, cross-promotional follow is the only pre-fight, fighter-level, forward-looking intent signal that exists in combat sports anywhere — and at 100K users × 5+ avg follows, the dataset alone is worth more than the app.

### New workstream established
- **`docs/areas/follow-fighter.md`** — vision, design principles, 9-category idea inventory (notifications, discoverability, engagement weighting, cross-promotional tracking, recommendations, profile/identity, onboarding, buyer reporting, anti-fraud), data schema plan, decisions log, open questions, dependencies on AI enrichment + rewarding-users + PostHog. Trigger phrases **"follow fighter session"**, **"let's build the follow fighter feature"**, **"follow data session"** load it via `CLAUDE.md`.
- Memory entry `project_follow_fighter_workstream.md` + `MEMORY.md` updated so the workstream persists across sessions.

### Decisions logged (in the area doc)
1. **Spoiler-safe is a hard rule for all post-fight notifications.** Many users watch fights *not* live and use the app as a watch-back decision tool. Any post-fight notification frames as *"a fighter you follow fought last night — open the app to decide if you want to watch it back"* with no outcome / method / round. In-app destination respects `spoilerFreeMode`.
2. **No walkout-cue granularity** — drop the "in-progress / walking out now" notification idea. Closest cue is "next fight up" within ~10 min, already covered by the existing walkout warning.
3. **No 6th bottom tab for discovery.** Embed in existing screens: Upcoming Events top carousel *"Fighting this weekend you don't follow"*, Profile *"Your fighters"* card with count sorted by next fight date + reason-tagged suggested follows below.
4. **Wave 2 first-ship order locked**: (1) universal `FollowButton` component + branding pass — current "+" only lives on hype modal and is undersized, (2) onboarding follow picker, (3) post-rating "follow the winner?" prompt, (4) engagement tracking schema (`FollowSource` on `FighterFollow` + new `FollowNotificationEvent` table) shipped alongside all three.

### Inventory growth this session
Net-new ideas added beyond the seed inventory:
- Universal follow button + branding pass (elevated to foundation status)
- "From your ratings" suggestion ("follow the 8 fighters you've rated highest")
- "Fighters who fought your fighters" rivalry surface
- Post-hype auto-follow-winner option
- Re-onboarding email for ~2,000 legacy migrated users
- Calendar export (iCal feed)
- Two-tap follow embedded in notifications
- Four-favorite fighters Letterboxd-style
- Fighter relationship card with closure-loop stats
- Spoiler-safe "your fighter fought last night" notification
- Global "Your fights" filter

### Open — awaiting Mike's design calls before any code ships
Six questions in chat (Q1–Q6 in the session transcript):
- Q1: brand color for universal follow button
- Q2: icon-only vs icon-plus-label on `following` state
- Q3: replace hype modal "+" with the new component (visual change for existing users)
- Q4: 60–80 fighter count for onboarding picker
- Q5: skip the picker for existing users, handle via re-onboarding email instead
- Q6: handle draws / no contests in post-rating prompt

### Pick up next "follow fighter session"
Once Q1–Q6 are answered, build `FollowButton` component first (single styled component with compact + prominent variants, used everywhere), then the onboarding picker, then the post-rating prompt, with engagement tracking schema shipped in the same set of PRs. Status snapshot before building: capture the **current avg follows per active user** as a baseline so we can measure lift. Also verify whether unfollow currently deletes the `FighterFollow` row — if so, that's a data-loss bug to fix before scaling (the timeline is sacred per Decisions §1 in the area doc, which makes preserving unfollow events load-bearing for the buyer demo).

# 2026-05-22

## Glory in Giza phantom-fights incident → parser hardening across all 19 scrapers

Started as a marketing session — Mike wanted to ship the weekend's Twitter Hype
Index post. The first DB pull surfaced two events for the same real-world card
("Glory in Giza" under Gold Star + under Matchroom Boxing) with the Gold Star
copy showing phantom fights that aren't actually happening (Tyson Fury vs
Arslanbek Makhmudov, Conor Benn vs Prograis, Frazer Clarke vs Justis Huni, etc.).
Mike pivoted: fix the data first, marketing can wait. Session followed.

### What was wrong

Two layered bugs on the Gold Star `Usyk vs. Rico: Glory in Giza` event row:

1. **Mass-import of phantom fights on 2026-04-11.** Before the May 3 ufcUrl-only
   lookup fix (`lesson_tapology_event_lookup_ufcurl_only`), the Gold Star
   parser's event lookup had an OR-name fallback that merged fights from a
   sibling Tapology event into ours. 10 phantom fights got attached this way.
2. **Chicken-and-egg cancellation loop preventing auto-cleanup.** The per-event
   cancellation safety floor required the scrape to return ≥75% of the DB's
   non-cancelled fight count. The phantom fights inflated the DB count to 21;
   75% = 16; the real card is 10 fights; 10 < 16 → cancellation pass skipped
   forever. Phantoms permanent.

### The cleanup

`fix(data): migrate Usyk hypes from Gold Star to Matchroom; delete Gold Star Glory in Giza`

Transaction-scoped script at `/tmp/migrate-and-delete-goldstar.ts`:
- 4 user predictions on Gold Star's Usyk vs Verhoeven row → re-pointed to
  Matchroom's canonical Usyk vs Verhoeven row (no user conflicts)
- 2 predictions on phantom fights (Fury, Benn) → cascade-deleted (Mike opted
  not to message the users; predictions on fights that won't happen are dead)
- 5 stale `FightNotificationMatch` rows pointing at Gold Star fights → cleaned up
- 21 Gold Star fights deleted (cascades remaining predictions/ratings/etc.)
- Gold Star Glory in Giza event deleted

Post-state: only one Glory in Giza visible (Matchroom, 12 fights, matches the
DAZN-published card). 4 hypes on Usyk preserved.

### Broader audit (read-only)

Ran an orphan-fight audit across all 19 scraper-managed events. 102 stale fights
surfaced across 13 events (MVP Han vs Holm 2: 35; Top Rank Mason vs Cordina: 21
including the same phantom Conor Benn vs Prograis row that haunted Gold Star;
Top Rank Zayas vs Boots: 21; etc.). **Filtering to UPCOMING/LIVE only: 0
user-visible orphans.** Every one of the 102 stale rows was already CANCELLED.
The chicken-and-egg loop only manifests at >25% orphan ratio AND scrape size
consistently below floor; Gold Star Glory in Giza was the unique case where
both held. Other events had their phantoms cancelled early before accumulation
locked the loop in.

### Parser fix (architectural hardening)

`fix(scrapers): add absolute scrape-size floor to cancellation gate`

Added `MIN_HEALTHY_SCRAPE_FIGHTS = 5` and two helpers to `cancellationGuards.ts`:
- `computeCancellationSafetyFloor(dbNonCancelledCount)` — extracts the 75%
  floor calc
- `isScrapeHealthyForCancellation(scrapeSize, dbNonCancelledCount)` — returns
  true if scrape ≥75% floor OR ≥MIN_HEALTHY_SCRAPE_FIGHTS absolute

The absolute floor breaks the chicken-and-egg loop: any scrape returning ≥5
fights is treated as healthy regardless of DB inflation. Two-strike rule in
`decideStrike` still mitigates single-render misses past this floor.

Applied to all 19 scraper-managed parsers (17 data + ufcLiveParser +
oneFCLiveParser + pflLiveParser). Each parser still defines `cancellationSafetyFloor`
for use in log messages; the OR-logic change is purely in the
`scrapeLooksComplete` computation. Log messages updated to show both floors when
firing. Production-tsconfig + strict-tsconfig type-checks clean.

### Targeted blacklist (immediate-resurrection prevention)

`fix(scraper): blacklist Tapology event 140464 in Gold Star scraper`

Tapology lists this Matchroom-promoted card on Gold Star's promotion page
because Gold Star is a co-promoter. Without intervention, tomorrow's Gold Star
scraper would re-create the duplicate Event row (clean this time — no phantoms,
since the May 3 fix is in place — but still a duplicate of the Matchroom row).

Added `SKIP_TAPOLOGY_EVENT_IDS` set + `isBlacklistedEventUrl()` helper at the
top of `scrapeGoldStarTapology.js`. The scraper's main loop checks each
discovered event URL and skips matches. Entry includes a comment explaining
when to add more IDs.

This is a targeted bandaid, not a proper fix. The real solution is
**cross-scraper event deduplication** — when one scraper imports a card that
another scraper has already imported, skip or link. That's a separate workstream
(see follow-up below).

### What did NOT happen

- **Marketing Hype Index post** — original session goal. Deferred. Hype data on
  the upcoming weekend's cards is too thin to anchor a true "Good Fights Hype
  Index" post (Usyk vs Verhoeven has 4 hypes; almost nothing else has any).
  Realistic path for the weekend: skip the prospective Tuesday slot (already
  passed anyway), do a retrospective post Monday after the card runs using
  whatever ratings come in.
- **DB hygiene cleanup of the 102 already-cancelled phantom rows.** Invisible
  to users (CANCELLED status). Decision deferred — no urgency. Could be done
  in a future session as a one-shot delete query.
- **Cross-scraper event dedup** — see follow-up.

### Follow-ups

1. **Cross-scraper event dedup** (new project). When the same real-world card
   has two visible Event rows from different scrapers (Matchroom + Gold Star,
   ESPN + Tapology, etc.), the user sees confusing duplicates. Glory in Giza
   was today's example; will recur on any boxing card where Matchroom is the
   primary promoter and another org co-promotes. Two design options: (a) at
   import time, check for date+headliner match against existing events from
   other scrapers, skip if found; (b) post-hoc dedup pass that runs nightly.
   Probably (a) — earlier intervention, less corrective data work.
2. **Hygiene cleanup of 102 CANCELLED phantom rows.** Low priority, invisible
   to users, but worth a one-shot delete.
3. **Marketing Hype Index post** still pending for this weekend. Mike will
   pick this back up Monday after Saturday's cards run.

## What changed in the repo

- `packages/backend/src/services/cancellationGuards.ts` — added
  `MIN_HEALTHY_SCRAPE_FIGHTS`, `computeCancellationSafetyFloor`,
  `isScrapeHealthyForCancellation`. Updated header comment.
- 16 daily-data parsers + 3 live parsers — replaced inline floor math with
  helper calls; updated log messages to show both floors. Files:
  `bkfcDataParser`, `dirtyBoxingDataParser`, `gamebredDataParser`,
  `goldStarDataParser`, `goldenBoyDataParser`, `karateCombatDataParser`,
  `matchroomDataParser`, `mvpDataParser`, `oneFCDataParser`,
  `oktagonDataParser`, `pflDataParser`, `rafDataParser`, `rizinDataParser`,
  `topRankDataParser`, `ufcDataParser`, `zuffaBoxingDataParser`,
  `ufcLiveParser`, `oneFCLiveParser`, `pflLiveParser`.
- `packages/backend/src/services/scrapeGoldStarTapology.js` — added
  `SKIP_TAPOLOGY_EVENT_IDS` blacklist + `isBlacklistedEventUrl` helper + main-loop skip.
- DB state: Gold Star Glory in Giza event + 21 fights deleted; 4 Usyk
  predictions migrated to Matchroom canonical row; 5 stale notification matches
  deleted.

No commit yet (Mike to decide when to ship to Render). No EAS update needed —
backend-only change.

---

## Web app QA session (afternoon)

Resumed the web QA walk from the 2026-05-14 handoff. Block 1 (anonymous
browse) in progress — surfaced and fixed three issues on the Live tab,
then iterated the upcoming-fight tap interaction with Mike until it
mirrors the mobile hype modal.

### Bugs fixed

1. **LIVE event appeared on both Upcoming and Live tabs** (ONE Friday
   Fights 155 was the test case). Backend's `getEvents(type='upcoming')`
   returns LIVE events too; web home page wasn't filtering them out.
   - Added `packages/web/src/lib/eventStatus.ts` with `isEventLiveNow`
     helper (mirrors mobile's lifecycle fallback — treats event as live
     if status==LIVE or earliest start time has passed and not COMPLETED)
   - Home (`/`) excludes events where `isEventLiveNow` is true
   - Live tab includes events where `isEventLiveNow` is true (replaced
     strict `eventStatus === 'LIVE'` check)

2. **Every fight on Live tab rendered with the "live" yellow strip**,
   including COMPLETED ones. EventCard `mode === 'live'` was returning
   `LiveFightCard` for every fight unconditionally; `LiveFightCard`'s
   strip-text fallback was `'Live Now'` regardless of fight status.
   - EventCard now routes by `fightStatus`: COMPLETED →
     CompletedFightCard, UPCOMING → UpcomingFightCard, only true LIVE or
     the single computed up-next → LiveFightCard
   - Up-next is the highest-`orderOnCard` UPCOMING fight, gated on no
     LIVE fight + at least one COMPLETED fight (matches mobile)
   - LiveFightCard strip text falls back to `'Up Next'` (not `'Live Now'`)
   - Fights deduped by id (also matches mobile)

3. **Tapping UpcomingFightCard navigated to the fight detail page** instead
   of opening a hype modal. Swapped the wrapping `<Link>` for a `<button>`
   that opens `HypeFightModal`.

### HypeFightModal rebuilt to mirror mobile

Mike asked for the web modal to parallel mobile's `UpcomingFightModal`.
Replaced the slider-based version with:
- Title "HOW HYPED ARE YOU?" (uppercase)
- Fighter images flanking lastName / vs / lastName block
- Large flame "wheel" that transitions to the selected number (CSS
  translateY on a vertical stack of 10 colored flames + blank)
- Row of 10 clickable flames (filled up to selection)
- Pre-fight comment textarea (auth only), seeded from existing comment,
  posted alongside the hype on Done
- "See N Comments >" link below the textarea — saves changes then
  routes to `/fights/[id]`
- Submit button reads "DONE"
- Bottom-sheet on mobile widths, centered card on `sm+`

Deliberately skipped (not asked for, can layer later): notification
bell, follow-fighter buttons on images, hype reveal modal with
distribution + Fan DNA line, AI preview one-liner, haptics.

### Data bug surfaced (not fixed — separate session)

ONE Friday Fights 155 has a duplicate fight: `Vero vs Kana` (UPCOMING,
orderOnCard=12) and `Vero vs Kana Morimoto` (COMPLETED, orderOnCard=13).
Another two fights share `orderOnCard=13`. Scraper/dedup issue. Note
for next ONE FC scraper session.

### Vercel CLI / deploy turbulence

- Vercel git auto-deploy was failing for ~40 min on a platform-side
  error (`TypeError: path argument must be string, received undefined`
  after `Applying modifyConfig from Vercel`). Affected ALL recent
  commits including backend-only ones. Cleared on its own — all my
  commits eventually went green.
- While trying to bypass via manual deploy I hit a CLI bug: project's
  `rootDirectory` is `packages/web` set in Vercel settings, and running
  `vercel --prod` from `packages/web` doubles the path. Running from
  repo root accidentally created a new `.vercel/` link to a brand-new
  empty project `fight-mobile-app`. Cleaned up: deleted the spurious
  project, deleted the stray repo-root `.vercel/`. The good link at
  `packages/web/.vercel/` is intact.
- Bumped local Vercel CLI 50.44.0 → 54.4.0 in the process. Required
  `vercel login` re-auth.

### Commits shipped

- `617276e` — fix Live tab routing + exclude live events from Upcoming
- `d955fa5` — tap UpcomingFightCard opens HypeFightModal instead of navigating
- `d5837de` — rebuild HypeFightModal to mirror mobile
- `ce0abab` — add "See N Comments" link in HypeFightModal

### QA checklist state (resuming next session)

Block 1 (anonymous browse) — partial. Walked Upcoming and Live tabs;
both pass now. **Still open**: Past tab, Top fights tab, event detail,
fighter detail.

Blocks 2-7 untouched: auth flows, fight detail interactions, profile
+ activity, footer/legal, responsive (375/768/1200), Lighthouse + SEO
sanity.

### Follow-ups

1. Finish Block 1 anonymous browse (Past, Top fights, event detail,
   fighter detail).
2. Then Blocks 2-7 in order.
3. Backend: ONE FC scraper duplicate-fight dedup (Vero/Kana case on
   ONE FF 155, and the shared `orderOnCard=13`).
4. If Mike wants closer parity on HypeFightModal: layer in notification
   bell, follow-fighter buttons, hype reveal w/ distribution + Fan DNA,
   AI preview line.

---

## Web app QA (evening) — modal/cache parity sweep across pages

Continuing the web QA walk. Each round was a focused improvement
mirroring mobile behavior on a different surface area, deployed via
auto-deploy on `git push main`.

### Round 1 — RateFightModal + LiveFightCard wiring

- New `RateFightModal` mirroring HypeFightModal: title "Rate This
  Fight", scrolling star wheel, 10 tappable stars, fighter images,
  comment input (auth only), "See N Comments" link, Done button.
  Uses `rateFight` + `reviewFight`/`updateReview`.
- `CompletedFightCard`: replaced `<Link>` with `<button>` opening
  RateFightModal. Added `userReview` prop for prefill.
- `LiveFightCard`: same pattern — tap opens RateFightModal (matches
  mobile, which opens CompletedFightModal regardless of LIVE / UpNext
  state).

### Round 2 — Good Fights "All Time" filter

Backend `top-recent-fights` accepts `week|month|3months|year|all`;
web was sending `alltime` so it silently fell back to "week". Aligned
to send `all`.

### Round 3 — Hand logo in navbar

Added the hand logo (`/good-fights-hand.png`) before the GOOD FIGHTS
wordmark.

### Round 4 — Lazy-load events

Replaced "Load More" buttons on Upcoming and Past with an
`IntersectionObserver`-based `LoadMoreSentinel` (400px rootMargin).
Live page left alone (single useQuery, no pagination).

### Round 5 — Fighter detail uses standard fight cards

Rewrote `FighterDetailClient` to render fights via `UpcomingFightCard`
+ `CompletedFightCard` instead of a custom inline row. Two sections
(upcoming first w/ HYPE / MY HYPE column headers, then completed w/
RATING / MY RATING headers + Date / Highest Rated sort), event-name
+ date prefix above each card row.

### Round 6 — Modal cache invalidation on side pages

Search results and fighter detail pages weren't updating immediately
after rate/hype changes — modals invalidated `['events']`, `['fight']`,
`['topFights']` but never `['fighterFights']` or `['search']`. Added
both to HypeFightModal + RateFightModal invalidation lists.

### Round 7 — Event detail "no fights" bug

`EventDetailClient` was fetching `GET /api/events/:id/fights` which
**doesn't exist** on the backend (only the `/admin/...` variant
does). Both SSR `page.tsx` and the client useQuery returned empty;
the screen showed "No fights announced yet" on every event. Switched
both to `/fights?eventId=...&limit=50` with `includeUserData=true`
when authenticated, mirroring mobile.

### Round 8 — Nullify hype/rating broken

User report: changing hype/rating updates immediately and persists,
but **clearing it (toggling off) silently fails** — UI doesn't
update and reload still shows the old value.

Root causes:
- `RateFightModal.persistChanges` was guarded by
  `selectedRating != null` — toggling off fired no mutation at all.
- `HypeFightModal` sent `predictedRating: undefined` → `JSON.stringify`
  drops the key → backend received `{}` → relied on the upsert's
  `predictedRating || null` to write null. Theoretically worked, but
  empirically didn't reliably clear the row.

Fix:
- New backend endpoint `DELETE /api/fights/:id/prediction` (mirrors
  the existing `DELETE /api/fights/:id/rating`). Returns recomputed
  `averageHype` + `totalHypePredictions` for client cache update.
- Web `api.ts`: added `deleteFightPrediction` + `deleteFightRating`.
- HypeFightModal: when nullifying, calls `deleteFightPrediction`
  explicitly instead of the upsert+null trick.
- RateFightModal: when nullifying, calls `deleteFightRating` (which
  also cascades-deletes the user's review + tags for the fight).

### Round 9 — Search page "my rating" empty after reload

Same AuthProvider race condition we fixed earlier for events: search
useQuery fires before the auth refresh resolves, gets the
unauthenticated response (no `userRating` / `userHypePrediction`),
React Query caches it. Fix: added `['search']` to the AuthProvider
post-resolve invalidation list.

### Round 10 — Event detail layout

User reported the event detail "uses a random component". The cards
were correct (UpcomingFightCard / CompletedFightCard / LiveFightCard)
but the wrapper was a 3-column grid (`sm:grid-cols-2 lg:grid-cols-3`)
instead of the vertical card stack used on the home page and past
events. Switched to the same `divide-y divide-border ... rounded-lg
border bg-card` list pattern that `EventCard` uses.

### Commits shipped (evening)

- `cea7c39` — RateFightModal + LiveFightCard wire-up
- `8a32eb7` — All Time period filter alignment
- `dbb0ee2` — hand logo
- `dba33f3` — lazy-load Upcoming/Past
- `0e59bb4` — fighter detail uses standard fight cards
- `421f6bd` — modal cache invalidation includes fighterFights + search
- `ad3308c` — event detail loads fights via `/fights?eventId=`
- `d056bf0` — DELETE prediction endpoint + nullify rating/hype +
  search auth race + event detail list layout

### Open follow-ups

1. **None of the evening's work has been tested live yet.** Deploys
   in flight (Vercel ~50s, Render ~2min). See
   `docs/HANDOFF-web-qa-evening-2026-05-22.md` for the specific test
   plan to run when Mike picks back up.
2. Per-card section orderOnCard sort: web `EventDetailClient` and
   `EventCard` sort ascending (orderOnCard=1 = main event first,
   which matches the backend's `/api/fights?eventId` ordering).
   Mobile sorts descending. Web is internally consistent; mobile is
   the outlier. Not changed.
3. The empty `getEventFights` API helper in `packages/web/src/lib/api.ts`
   is now unused (event detail switched to `getFights`). Can be
   removed in a cleanup pass; left in place to avoid scope creep.


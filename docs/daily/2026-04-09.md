# 2026-04-09

## What I Worked On
- [x] PFL daily scraper debugged and fixed (backend/scrapers)
- [x] RAF scraper: fixed event-status detection so future events aren't wrongly marked COMPLETED (backend/scrapers)

## Changes Made

### Scrapers: PFL daily scraper was timing out, missing PFL Africa Pretoria
- **Symptom**: Daily PFL scraper (`pfl-scraper.yml`) failing with 25-min timeout. This weekend's event (PFL Africa 2 Pretoria, Apr 10 2026) had no fights in the DB.
- **Root cause**: Step 1's `scrapeEventsList()` ran `document.querySelectorAll('a[href*="/event/"]')` against the whole `pflmma.com/all-seasons` page. That page has two tab panels rendered in the same DOM — `#nav-upcoming` and `#nav-past` — so the query returned **~130 events back to 2018**. The scraper then tried to scrape all 130 event pages (Step 2) and download 860 athlete images (Step 3), which blew past the 25-min timeout. The chained `importPFLData()` never ran because bash stops after the scraper command fails, so PFL Africa Pretoria — which *was* successfully scraped at line 17 of the log — never made it to the DB.
- **Dead ends before the real fix**:
  1. **Slug-year prefix filter** (`eventSlug.match(/^(\d{4})-/)`): Unreliable because some past events have no year prefix (`pfl-pittsburgh`, `pfl-road-to-dubai-feb-7`) and some current-year slugs are actually past events (`2026-pfl-madrid` is the March 2025 Madrid event). Reverted.
  2. **Date regex fix + require parseable date**: Swapped `\s+` for `[,\s]+` to handle PFL's `Fri, Apr 10, 2026` format, then required a parseable date to keep an event. Shipped it, scraper returned **0 events**. WebFetch had hallucinated the year — actual list-view HTML for upcoming events is just `<h6>Fri, Apr 10</h6>` with **no year**. Upcoming events therefore had no parseable date and were all filtered out.
- **Primary fix — DOM scoping** (commit `bbd5ec8`): Scope Step 1's event-link query to `#nav-upcoming` descendants only. PFL's tab panels are both rendered in the DOM, so the upcoming tab's container is a clean DOM boundary — no date parsing required. Step 1 now returns exactly the 6 upcoming events instead of 130, and the run finishes in ~4 minutes.
- **Follow-on bug #1 — fallback date** (commit `426fba6`): After the scoping fix, Step 1's `eventDate` was null for all events (no year on the list view) and `pflDataParser.ts:152-156` fell back to `2099-01-01`. First tried deriving `eventDate` from Step 2's `eventStartTimeISO` field, which imported dates successfully — but…
- **Follow-on bug #2 — countdown timer contamination** (commit `224e5da`): …all 6 events imported with the same date (`4/10/2026`). Turns out `eventStartTimeISO` comes from scraping `DateTime.fromISO(...)` in the event page scripts, but every PFL event page includes the site-wide countdown timer in its header that shows the **next featured PFL event across all events** — so every event page's script scrape returned Pretoria's date. Real per-event date source: the **Google/Yahoo/Outlook calendar share links** at the bottom of each event page contain the event's specific date in `YYYYMMDDTHHMMSS` format (e.g., `dates=20260411T180000` for Chicago). Added a regex match on the rendered HTML to pull that out as `eventDateFromCalendar`, and use it as the primary `eventDate` source in the merge.
- Files: `packages/backend/src/services/scrapeAllPFLData.js:71-85` (#nav-upcoming scoping), `:339-354` (calendar URL date extraction), `:914-922` (merge with eventDate fallback chain)
- Commits: `0088e21` (first attempt, regex fix — superseded), `bbd5ec8` (tab scoping), `426fba6` (eventDate fallback — partial), `224e5da` (calendar URL per-event date)
- **Final state** (verified via manual workflow dispatch): Scraper run ~4 min, imports 6 events (PFL Africa Pretoria 4/10, PFL Chicago 4/11, PFL Belfast 4/16, PFL Sioux Falls 5/2, MENA 9 5/8, PFL Brussels 5/23) × 52 total fights × 104 fighters. All events have correct distinct dates.
- **Known remaining issue (not urgent)**: `eventStartTimeISO` is still scraped from the countdown timer and will contain the wrong event's time for non-featured events. `pflDataParser.parsePFLEventStartTime()` already partially handles this by preferring `eventDate` and stealing just the time-of-day from the ISO, but the time-of-day itself will be wrong for non-featured events. Main-card text extraction (`Try 2` in the same block) should be promoted above the ISO scrape to fix this properly. Flagged but not fixed today.

### Scrapers: RAF scraper wrongly marked future events COMPLETED
- **Symptom**: RAF 09 (May 30, 2026) showed up on the "past events" screen in the mobile app despite being ~7 weeks in the future. Database showed `eventStatus: COMPLETED, completionMethod: null`. RAF 08 (Apr 18, 2026) was correctly UPCOMING, so the bug was event-specific.
- **Root cause**: `scrapeAllRAFData.js:123` determined status from the gallery card buttons: `hasBuyTickets ? 'Upcoming' : 'Complete'`. RAF 09 tickets aren't on sale yet, so no "buy tickets" button exists, and the scraper reported it as Complete. The event-page scraper does extract a more reliable `isPastEvent` signal from `div.past-event-tag` (the site's own CMS flag), but line 448 only used it to upgrade Upcoming→Complete, never to correct Complete→Upcoming. And `rafDataParser.ts:225` leaves `eventStatus` untouched on the update path, so once an event was imported as COMPLETED it stayed that way forever.
- **Fix**: Dropped the button heuristic. Gallery scrape now defaults to `'Upcoming'`. Event-page scrape trusts `isPastEvent` as authoritative in both directions. Added a `fallbackStatusFromDate()` helper that marks an event Complete only if `eventDate + 24h < now` — used only when the event page scrape fails (previously the fallback was the meaningless `galleryEvent.status`). The 24-hour buffer avoids the midnight-date problem where `eventDate` is stored at 00:00 UTC and would otherwise flip to Complete before the event starts.
- **DB cleanup**: Manually reset RAF 09 to `UPCOMING` / `completionMethod: null` via Prisma. Queried all promotions for other future-dated events marked COMPLETED — none found, RAF 09 was the only stale record.
- File: `packages/backend/src/services/scrapeAllRAFData.js:115-120, 432-434, 396-404`

## Notes
- PFL event pages' visible date header drops the year (`FRI APR 10`, `SAT MAR 28`), BUT the Google/Yahoo/Outlook calendar share links at the bottom of each event page include the full date as `YYYYMMDDTHHMMSS` — that's the best machine-readable per-event date source.
- PFL's site-wide countdown timer in the header script pollutes `DateTime.fromISO(...)` extraction: every event page's scripts contain the same countdown targeting the next featured PFL event. Don't use that as a per-event date source.
- The GitHub Actions workflow chains `node scrapeAllPFLData.js` with `node -e "...importPFLData()..."` on the same line. Because bash stops after the first command fails, a scraper crash also prevents the import — even if events were successfully scraped and saved.
- **WebFetch caveat**: Twice during this debug, WebFetch returned summaries that had inferred details the raw HTML didn't contain (e.g., "Fri, Apr 10, **2026**" when the HTML just had "Fri, Apr 10"). When date/format precision matters, curl the raw HTML directly instead of trusting WebFetch's Markdown summary.
- **RAF status detection lesson**: "Absence of a ticket-purchase button" is not a reliable signal of event completion. Three states can produce no button: already happened, not yet on sale, sold out. For any scraper that determines event state from page elements, prefer (a) an explicit CMS flag from the source, (b) date comparison with a buffer, in that order — never absence-of-button.
- **`rafDataParser.ts` update path is write-once on status**: line 225 deliberately logs "status unchanged" and doesn't touch `eventStatus` on subsequent scrapes. This means data fixes for wrongly-imported events have to happen at the DB level, not by re-running the scraper. Worth keeping in mind if similar bugs surface in other parsers.

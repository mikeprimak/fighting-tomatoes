# 2026-06-06 — Home screen redesign, cont. (mobile)

Branch: `home-screen-redesign` (off `main`, pushed). Continuation of 2026-06-05.

## Shipped — `packages/mobile/app/(tabs)/home.tsx`

Event cards iterated based on Mike's feedback:

- **Event card infinite-height bug fixed** — image was `height:100%` under an
  auto-height (stretch-derived) parent; absolute-fill the image so the card height
  comes from the text column. (`cc842bd`)
- **Card blurb = main event's AI preview** — replaced the hyped-fight list, then the
  most-hyped-fight preview, with the **main event** fight's `aiPreviewShort`,
  confidence-gated ≥ 0.5. Loads upcoming events with `includeFights`. (`25630ec`, `04a0d8c`)
- **Main-event detection fix** — `orderOnCard: 1` is the headliner (top of bill), not
  the highest orderOnCard. Was picking an un-enriched prelim → blank cards (e.g. UFC
  "Muhammad vs Bonfim", which sits at order 1 with a good 0.85-confidence preview).
  Now picks the **lowest** orderOnCard. (`91cab95`)
  - Note for later: web `EventCard` "up next" uses *highest* orderOnCard and calls it
    "last to walk out" — looks inverted vs this data convention. Out of scope today.
- **Start time + timezone + channel** — card meta line shows the main event start time
  in the user's local zone with tz abbr, then the region broadcast channel
  (e.g. "8:00 PM ET · ESPN+"), via `formatEventTime`/`getTimezoneAbbreviation` +
  `useEventBroadcasts`. (`91cab95`, `298ac3b`)
- **Section titles** now read **"Events Today / Events Tomorrow / Events <weekday>"**.
- **Date moved to a section subline** under each day heading (e.g. "June 7th"); removed
  from individual cards. Org-logo overlay removed from the card image.
- **Description** now shows in full (no line clamp) at a smaller size (11/15). (`ae3b3c9`)

Earlier (2026-06-05) on this branch: per-day sections, full-width horizontal cards,
"The Latest" moved below Highlighted Fighter.

Type-check: home.tsx clean throughout.

## Decision logged — event-level AI enrichment job

Mike confirmed he **will build a dedicated event-specific AI enrichment job** so the card
summary can reason across the whole card (e.g. "3 title fights…") rather than reuse one
fight's preview. Plan written to `docs/areas/ai-enrichment.md` → "Planned — event-level
'why to care' enrichment job"; project memory `project_event_level_ai_enrichment_planned`.
Current card blurb is the interim (main-event `aiPreviewShort`).

## Shipped — event-level AI "why care" one-liner (piggyback, not a separate job)

Reassessed the "dedicated job" plan above and built it as a **piggyback on the existing
fight-enrichment call** instead (`a363c021`, then refined `189e37cf`). Rationale + full
detail in `docs/areas/ai-enrichment.md`. Summary:
- `enrichOneEvent` already loads the whole card + editorial and makes one Haiku call, so
  the event summary is one extra structured-output field (~0 marginal cost), rides the
  existing T-10/5/2 cron + 36h dedup, grounded in the same evidence as the per-fight lines.
- Schema: `Event.aiEventSummary`/`aiEventConfidence`/`aiEventEnrichedAt`
  (migration `20260606000000_add_ai_event_summary_fields`, **applied to Render DB**).
- Extractor emits `event:{summary,confidence}`; persist writes the cols; `/api/events`
  select **and** fast-json-stringify response schema updated; mobile Home `EventRow` +
  web `EventCard` render it gated ≥0.5 (mobile falls back to interim main-event line).
- **Hype-informed:** feeds per-fight `fanHype` (avg `FightPrediction.predictedRating`,
  scale **1-10**) + sample size; only fights with `n >= 5` are shown to the LLM (noise
  filter). When a non-main fight clearly out-hypes the headliner the line names it as fan
  excitement ("…but fans are most hyped for the Allen co-main"). Output ~15-25 words, last
  names only. Verified live (dry-run) across UFC/Zuffa/RIZIN/Top Rank/Oktagon tonight.
- Article-length SEO generation was **de-scoped** (mass thin auto-pages = helpful-content
  risk + fabrication at length; keep articles a curated Opus-as-Claude-Code draft).
- Data oddity found, not fixed: Top Rank "Padley vs. Fiaz" is labeled `promotion=TOP_RANK`
  but is a Matchroom Boxing card, and its `ufcUrl` points to a different tapology event
  (`smith-vs-puello`) — fight-bleed risk to revisit.

## INCIDENT — Render Postgres crash-loop during fight night (2026-06-06 eve)

> ⚠️ **DIAGNOSIS UNVERIFIED — Mike is opening a full investigation.** The "root cause"
> below is a *preliminary hypothesis only*, not confirmed. Symptoms and hard facts (logs,
> what was/wasn't run) are reliable; the load-overload attribution is a guess and may be
> wrong. Do not treat it as settled. Key hard fact for the investigation: an additive
> migration (`20260606000000_add_ai_event_summary_fields`, 3 nullable cols) WAS applied to
> the Render DB today via `prisma migrate deploy`.

**Symptom:** web + mobile sitewide "Failed to load events"; `/api/events` returned 500
(first as a ~20s hang = Prisma pool timeout, later fast-fail = DB refusing connections);
blog/editorial (non-DB, served from markdown) kept working. DB came back, then crashed
again = a **crash loop**, with the events query crawling to 2-9s even when up.

**Preliminary hypothesis (UNVERIFIED):** the `starter`-plan Render Postgres
(`fightcrewapp-db`, Oregon) was **overwhelmed by aggregate concurrent load on a 5-org fight
night** — prod backend +
a local backend dev server (BOTH running the every-5-min `eventLifecycle`) + remote live
trackers (GitHub Actions + Hetzner VPS) + the apps + multiple Claude Code sessions running
scripts at once. Render DB logs (`output.txt`) showed `FATAL: the database system is
starting up` (Postgres recovering), a prod-backend **retry storm (~96 connections)** on the
slow events query, and `prisma_migrate_shadow_db_*` churn from a Render-internal superuser
client. NOTE: external connections via Render's pooler appear in logs as internal `10.x`
client IPs, so client IP is NOT reliable for attributing a connection to a session.

**What it was NOT:** the AI-enrichment migration. It is additive (3 nullable cols), was
applied cleanly, and `prisma migrate deploy` tolerates a DB-ahead migration (a migration in
the DB but absent from `main`'s folder is ignored, not an error) — verified `/health` stays
200. Prod's startup migrate (`db:migrate:deploy` = `prisma migrate deploy`) does **not**
create shadow DBs. The slow events query is a *symptom* of resource starvation, not a code
change from this work.

**Still unexplained:** the `prisma_migrate_shadow_db_*` activity — only `prisma migrate
dev` / `db push` / `migrate diff` create those, and no session admits running one (three
Claude windows were open: this one, an analysis-only one, and a third doing live-tracker
work). Likely not the primary crash driver (sustained load is); just ensure nobody runs
`db push`/`migrate dev` against prod.

**Resolution / playbook (recurs on fight nights):**
1. **Bump the Render Postgres plan one tier** when a fight night crash-loops — fastest path
   to stability; scale back after. `starter` is undersized for the live-night footprint.
2. **Restart `fightcrewapp-backend`** in Render to clear the retry-storm pool (once the DB
   accepts connections again).
3. **Shed load:** stop the local backend dev server (a 2nd lifecycle-runner + pool on the
   same DB), kill stray jest workers, and **do not run dev/live trackers against the prod DB
   during a live fight night** (prod already tracks those cards → redundant load + double-write
   risk). Coordinate when multiple Claude windows are open.

### Account from the analysis/live-tracker window (one data point for the investigation — NOT confident)

A separate Claude window (this one) was doing **read-only live-tracker analysis** tonight,
and was (wrongly) suspected as the crash cause by another window. Recording exactly what
this window did and what it observed, as evidence — **causation here is a guess, not a
claim.**

- **What this window ran against the DB:** three one-shot **read-only** node scripts only
  (`tonight-events.js`, `tonight-expectations.js`, `order-audit.js` — all in
  `packages/backend/`, all plain `findMany`/`findFirst`, no writes). All exited; nothing
  left running from this window. This window ran **no** trackers, **no** loops, **no**
  backend, **no** `prisma db push`/`migrate dev`/`migrate diff`, and **no** schema change.
  So the `prisma_migrate_shadow_db_*` activity did not originate here.
- **Process finding (local, `Get-CimInstance Win32_Process`):** at the time of the crash the
  only persistent node processes on Mike's machine were a **local backend dev server** —
  `pnpm dev` (PID 11444) + `nodemon … src/server.ts` (PID 13964), both started 07:29:53 ET,
  **not by this window.** That backend runs the every-5-min `eventLifecycle` (status flips +
  live-tracker dispatch + fight read/writes) against the **prod** DB. *If* a hypothesis is
  wanted: a local backend running the lifecycle against prod during a live night is a
  plausible contributor (extra load + double-writes to the same fight rows prod is already
  tracking), but this is **speculation** — could equally be the aggregate of everything else.
- **My hunch (low confidence):** most likely a pile-up of concurrent writers on a small DB
  (prod lifecycle + local lifecycle + VPS/GHA trackers + a third window actively building/
  running trackers) rather than any single culprit. The shadow-DB churn is the one genuinely
  unexplained signal and is worth pinning down (who ran a `db push`/`migrate dev`?), but it
  may be a side-show, not the driver. **Open to being wrong on all of this.**

## Live-tracker investigation (analysis only — no code shipped; cut short by the DB incident)

Mike asked (this is a "live trackers session") to use tonight's busy slate to refine the
trackers, focused on accurate walkout / "up next" notifications, and to deep-dive two
specific issues. **No code was written or deployed** — this was analysis, and it was
interrupted mid-way by the DB crash. Findings + recommendations below so the work isn't lost.

### Tonight's live slate (pulled from prod before the crash)

| Card | Start (ET) | scraperType | Tracker status | Users waiting on a ping |
|---|---|---|---|---|
| OKTAGON 89 | 12 PM | `oktagon` | ✓ native, reliable | **0** |
| Zuffa Boxing 7 | 12 PM | `tapology` | ⚠️ boxing-on-Tapology | **0** |
| Padley vs Fiaz | 2 PM | `tapology` | ⚠️ boxing-on-Tapology | **0** |
| **UFC FN: Muhammad vs Bonfim** | prelims 5 / main 8 PM | `ufc` | ✓ gold standard | **7 (across 5 fights)** |
| RIZIN Landmark 14 | 11:30 PM | `tapology` | ⚠️ MMA-on-Tapology | **0** |

**Key reframe:** the *only* card with anyone actually waiting on a notification tonight is
**UFC (7 users)** — and it already has the best tracker we own. Every "gap" org tonight
(boxing ×2, RIZIN, Oktagon) has **zero** notification expectations. So they're effectively
**zero-risk live test beds** (validate risky tracker work against live data with nobody
depending on it), not urgent user-facing work.

### Three recurring failure modes (distilled from the RAF/Matchroom/Yahoo handoffs)

1. **Source lag (Tapology).** Up-next fires only when the *previous* fight flips COMPLETED
   on the source; Tapology lags 30–60+ min unpredictably → ping never triggers. Affects all
   3 Tapology cards tonight. Fine for result backfill, useless for walkout timing.
2. **Out-of-order broadcasts.** Cards run main-event-last but orgs reorder live, so
   card-position next-fight logic is wrong; up-next must come from a **named forward-looking
   signal** ("Up Next: A vs B" — Yahoo for boxing/RAF; Sherdog "Live NOW!" for MMA).
3. **Silent gating bugs** (the 2026-06-04 `notifyNextFight` `gt/asc`→`lt/desc` fix). Tonight
   would have been the first Saturday slate to confirm that fix live.

### Recommended approach (was going to propose, not yet acted on)
- **Tier 1 — protect the only real stakes:** watch the 7 UFC expectations fire at prime time;
  use the admin per-fight "🔔 Send ping" manual fallback if needed.
- **Tier 2 — refine on the free boxing test bed:** fix the **Yahoo premature-headliner-fire
  bug** (`runYahooLiveBlogTracker.ts` matches the headliner's surnames in all-night hype posts
  and fires the main-event walkout hours early — fired Han/Holm ~4h early on 5-30), then
  validate the Yahoo tracker live on Zuffa/Padley.
- **Tier 3 — zero-code quick win:** if Sherdog publishes a PBP page for RIZIN Landmark 14, set
  `event.sherdogPbpUrl` → durable Sherdog dispatch is already wired (one row, no code).
- **Explicitly deferred:** full VPS durability wiring for Yahoo (needs SSH access + no demand
  tonight to justify touching lifecycle dispatch before a UFC card with real expectations).

### `orderOnCard` direction audit — STARTED, NOT RESOLVED (interrupted by the crash)

Mike reported recurring trouble: some orgs' daily scrapers seem to number cards descending
(headliner = #1) and others ascending (headliner = #14). This matters because the "up next"
query assumes **`orderOnCard = 1` = headliner = fights LAST** (descending run order) — if
half the orgs are inverted, up-next fires on the wrong fight for them.

- **Code read (subagent, packages/backend data parsers):** flagged that the **native**
  scrapers (UFC/Oktagon/BKFC/ONE/PFL/RAF/Matchroom/RIZIN) use a `globalOrder++` pattern =
  headliner #1 (correct), but the **Tapology-based** parsers (MVP, Zuffa, Karate Combat,
  Dirty Boxing, Gold Star, Gamebred) assign `orderOnCard = data.fights.length + 1`, which the
  agent read as **opener = #1 (inverted/wrong)**, and flagged **Top Rank + Golden Boy** as
  having TWO competing scrapers (native descending vs Tapology ascending) writing the same
  file = undefined order.
- **⚠️ This code finding is UNVERIFIED and possibly wrong.** Tapology event pages list the
  **main event at the top**, so `data.fights.length + 1` incrementing in page order could
  still yield **headliner = #1** (correct). The agent may have misread the source-array
  direction.
- **DB ground-truth spot check (the part I got before stopping):** for every event checked,
  **headliner = `orderOnCard` 1** — UFC (Medic/Rodriguez #1), Oktagon, BKFC, ONE, PFL, RAF,
  **and a generic `tapology` event (Taylor vs Pili = "Main Event" at #1)**. That last one
  *contradicts* the "Tapology is inverted" claim. This agrees with the existing memory
  `lesson_notify_next_fight_gt_asc_four_parsers` ("orderOnCard 1 = headliner, verified vs
  prod data").
- **Why the audit is incomplete:** my query keyed on `scraperType`, but MVP/KC/DBX/Zuffa/RIZIN
  all share `scraperType='tapology'` → they returned "no events" and were **never individually
  verified**. The crash stopped the follow-up. **To finish:** query by `promotion` (not
  scraperType) for one known card per Tapology-fed org and confirm the recognizable headliner's
  `orderOnCard`. (Audit script left at `packages/backend/order-audit.js`.)
- **Current best read:** the system is *probably* already unified on headliner=#1, and the
  code agent's "inverted" flags are *likely* false positives — but **Top Rank / Golden Boy's
  dual-scraper conflict is a real thing to check**, and the per-org Tapology DB confirmation
  is genuinely still open. Don't treat "unified" as confirmed yet.

### AI for live tracking (Mike raised it — not yet investigated)
Mike asked whether a minimal-cost AI layer could vastly improve tracker accuracy. **Not
explored this session** (interrupted). Initial instinct only: the most promising fit is
parsing the *unstructured* live-blog prose (Yahoo/ESPN) into `{event: start|end|result,
fighters, method}` — exactly the brittle part of the boxing/RAF trackers — with a cheap Haiku
call gated by confidence, mirroring the existing `startTimeDiscovery`/`broadcastDiscovery`
pattern. Worth a real scoping pass next session.

### Temp scripts left in `packages/backend/` (delete or fold in later)
`tonight-events.js`, `tonight-expectations.js`, `order-audit.js` — all read-only diagnostics
from this session.

## INCIDENT RESOLVED — root cause was connection-pool amplification, NOT overload

Follow-up investigation (Mike reopened it the next morning; doubted the overload
hypothesis — correctly). **The preliminary "aggregate load overwhelmed the starter DB"
diagnosis above was wrong.** Hard findings from inspecting the live DB:

- DB was healthy when checked (15 conns, `max_connections=103`); planner stats fine
  (`pg_class.reltuples` accurate; the `n_live_tup=0` was just a reset activity counter, a
  red herring). So it was **episodic**, not a permanent state.
- **Real root cause:** the backend calls `new PrismaClient()` in ~50
  route/service/parser modules, all loaded into the one long-running server process. Each
  opens its OWN connection pool (Prisma default `cpus*2+1` per client, miscounted high in a
  container), none with `connection_limit`. On a fight night, lifecycle + scrapers + live
  trackers + auth/search + the **newly-live web Home** (`/api/events?includeFights`, which
  `useAnyLiveEvent` polled every 60s **per tab**) opened connections across all pools at
  once → marched toward 103 → small instance OOM → `"database system is starting up"`
  recovery → crash loop. "Never seen before" because the web Home (commit `a7a5edee`, on
  main) is a brand-new heavy/repeating caller, on top of today's concurrent dev sessions.
- **All four symptoms are one cause** (DB refusing connections): slow `/api/events`,
  blog order ignored (`editorial.ts` silently falls back to date-order when the
  `systemConfig` read fails), intermittent admin login.

**Fix shipped to `main` (deploys backend + web):**
- `packages/backend/src/lib/prisma.ts` — process-wide PrismaClient singleton with a
  bounded `connection_limit` (default 10, env `PRISMA_CONNECTION_LIMIT`) appended to
  `DATABASE_URL`. All 48 runtime modules now import it instead of `new PrismaClient()`.
- Web `useAnyLiveEvent` drops `includeFights` (liveness needs only event-level fields) —
  kills a heavy fights+predictions aggregation that ran every 60s per tab.
- Guard added to CLAUDE.md + memory `prisma-single-client-rule` so it isn't re-introduced.
- Pushing to main also restarts the backend, clearing the leaked pools.

**Still open (secondary, not the crash cause):** `/api/events?includeFights` is itself slow
(~7s) — it loads full fight cards + all hype predictions just to render summary cards.
Consider a main-event-only payload + short server-side cache (events change slowly). The
repo `packages/backend/render.yaml` is **stale** (says `plan: free`, user
`fightcrewapp_user`); live DB user is `fightcrewappdb_05gc_user` — don't trust it for plan
info. If crash-looping recurs, also consider a Render connection pooler (pgbouncer) and/or a
plan bump for memory headroom.

## Next

- Device-test the card layout (absolute-fill image height, full description length).
- **Wrap up `home-screen-redesign` → land AI enrichment on prod** (merge to main = backend +
  web deploy; mobile needs an EAS build/OTA — ask first). **Do NOT deploy while the DB
  incident is active** (a merge restarts the prod backend mid-incident).
- Persist tonight's event summaries once the DB is healthy (dry-run output captured; not yet
  written).
- Fix the Top Rank "Padley vs. Fiaz" mislabel + wrong tapology URL.
- Consider the web `EventCard` up-next orderOnCard inversion in a future session.

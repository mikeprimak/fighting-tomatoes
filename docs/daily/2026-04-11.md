# 2026-04-11

## What I Worked On
- [x] Task 0: commit the uncommitted 2026-04-10 ONE FC scraper fixes (tracker CI + hero-duplicate + per-face sticker)
- [x] Task 2: fix ONE FC fighter-name bug where URL slugs truncate real names (Rittidet, Nuapet, etc.), backfill existing broken rows
- [x] Task 3: audit all other live scrapers for the per-face sticker anti-pattern that poisoned ONE FC — no bugs found
- [~] Task 1 (historical ONE FC winner backfill): **descoped** for MVP, documented in handoff

## Changes Made

### Backend: commit 2026-04-10 ONE FC scraper fixes (Task 0)

Two commits landed from the prior day's uncommitted working tree:

- **`eb6177c`** — Tracker CI build fix. Added `packages/backend/tsconfig.tracker.json` (a focused tsconfig scoped to live-tracker files with DOM lib enabled), `@ts-nocheck`'d `fighterMatcher.ts` to quarantine dead `FighterAlias` code, and pointed `onefc-live-tracker.yml` at the new tsconfig. The full `pnpm build` has been broken since March 2026 due to unrelated Fastify/routes/auth type errors; this sidesteps it for the live tracker only.
- **`0031664`** — ONE FC scraper hero-duplicate + per-face sticker rewrite. Rejects `.event-matchup` nodes under `.box-post-event / .event-live-status / .status-matchup` in both the daily and live scrapers. Rewrote winner-sticker detection to query `face1?.querySelector('.sticker.is-win')` per-face instead of the broken `face1.closest('[class*="matchup"]').querySelector(...)` which always resolved to the first winner sticker anywhere inside the matchup.

### Backend: ONE FC fighter names — prefer JSON-LD performer over URL slug (Task 2)

- **Symptom**: ONE FC athlete URL slugs frequently truncate or garble real display names. `/athletes/rittidet/` is single-word → parser stored `{firstName: '', lastName: 'Rittidet'}` when the real name is Rittidet Lukjaoporongtom. `/athletes/nuapet-tded99/` → "Nuapet Tded99" when it should be Nuapet Torfunfarm. At least 17 ONE FC fighters in the DB had `firstName = ''` from single-word slugs.
- **Root cause**: both the daily scraper (`scrapeAllOneFCData.js`) and the live scraper (`oneFCLiveScraper.ts`) derived names from `/athletes/<slug>/` URL kebab-case parsing. The slug is whatever ONE FC decided when creating the athlete page and often lacks the full surname / camp suffix.
- **Fix**: use schema.org JSON-LD `performer` array as the primary name source. Each event page has a `<script type="application/ld+json">` with an Event entity whose `performer` array alternates `PerformingGroup` entries (the matchup name, e.g. "Kompet vs. Attachai") with `Person` entries for each fighter (the full name, e.g. "Kompet Sitsarawatsuer"). Walk the array, match matchup DOM elements to JSON-LD entries by normalized versus-text key, extract `fighterAFullName` / `fighterBFullName`, pass them through to the parser.
- **Parser update** (`oneFCDataParser.ts:parseOneFCFighterName`): accepts an optional `fullName` parameter; preference order is now JSON-LD fullName → URL slug → display name. Nickname extraction handles both ASCII quotes and curly quotes (`"`, `"`, `'`, `'`), including the rare pre-name form `"Petnueng" Isaac Mohammed` → `{firstName: 'Isaac', lastName: 'Mohammed', nickname: 'Petnueng'}`.
- **Live parser update** (`oneFCLiveParser.ts:findFightByFighters`): was matching DB fights by `lastName` only on each side. Changed to match via token intersection (firstName ∪ lastName) so the live tracker keeps matching legacy single-word DB rows during the transition. Once a fighter gets renamed from `{'', 'Rittidet'}` to `{'Rittidet', 'Lukjaoporongtom'}`, the scraper emits `lastName: 'Lukjaoporongtom'` which would otherwise not match the old row. Token-based matching bridges both shapes.
- **Verified in isolation** against the captured `one150.html`: 14/14 matchups resolved correctly to their JSON-LD full names, including Nuapet Torfunfarm, Rittidet Lukjaoporongtom, Kompet Sitsarawatsuer, and the nickname-bearing fighters (Regian "The Immortal" Eersel, Rasul "Sagittarius" Rahimov, "Petnueng" Isaac Mohammed).

### Backend: ONE FC name backfill script (`scripts/fix-onefc-broken-names.js`)

Written to repair existing broken rows after the scraper fix.

- **How it works**: scrapes every ONE FC event in the DB (`scraperType = 'onefc'`, `ufcUrl IS NOT NULL`), builds a global `slug → fullName` map from each event's JSON-LD paired with matchup face anchor hrefs, then for every ONE FC fighter row derives a "reverse slug" from their stored name (`dbNameToSlug(firstName, lastName)`) and looks it up. If the JSON-LD full name differs from the stored name, it proposes a rename (or merge if a correctly-named row already exists).
- **Safety guards** (learned the hard way via dry-run iteration):
  1. **First-word match + word-count ≥** filter. Rejects proposals like `Vero Nika → Vero` (JSON-LD would shorten a real name) and `Banluerit → Banluelok` (first-word mismatch = slug collision with an unrelated fighter). Without this filter 11 risky renames would have landed.
  2. **Scope guard**: skip any fighter whose fight history contains non-ONE promotions. The backfill MUST only touch fighters that are pure ONE FC — otherwise a rename or merge would quietly affect how the fighter appears in their UFC/Bellator/etc. history. 30 cross-promotion rows excluded.
  3. **Dry-run by default**, `--apply` opt-in.
- **Applied to prod** (Render DB): **10 renames, 5 merges, 0 failures**. Fixes include the originally-flagged Nuapet Tded99 → Nuapet Torfunfarm, plus single-word → full-name merges for Rodtang Jitmuangnon, Takeru Segawa, Kompet Sitsarawatsuer, Petkhaokradong Lukjaomaesaithong, Kongklai Sor Sommai, Tonglampoon FA Group, and renames for Tubtimthong IngfahhotelUbon, Yodphupa Petkiatpet, Kompet, Ratchasiesan Laochokcharoen (with nickname "Left Meteorite"), plus several Muay Thai camp-rename cases (Petnamkhong, Denkriangkrai, Isannuea, Sulaiman, Khunsuk, Lamsing) where the fighter's first name stayed but the camp/gym suffix on the last-name field changed.
- **Remaining leakage**: 10 ONE-FC-only fighters still have `firstName = ''` (Dedduanglek, Hyu, Jaosuayai, Misaki, Phetjeeja, Pompet, Ranma, Ratchasiesan, Shoma, Taku). Their ONE FC appearances weren't in any of the 10 events that had scrape-able JSON-LD data — either their events pre-date the scrape window or they haven't appeared in a JSON-LD-populated event yet. Accepted as MVP leakage; will self-heal whenever they appear on an event the daily scraper processes.

### Backend: audit other live scrapers for the ONE FC per-face sticker bug (Task 3)

**Diagnostic query** across all promotions to find suspicious A-side winner skew:

```sql
SELECT e.promotion,
       COUNT(*) FILTER (WHERE f.winner = f."fighter1Id") AS a_wins,
       COUNT(*) FILTER (WHERE f.winner = f."fighter2Id") AS b_wins,
       COUNT(*) AS total
FROM "fights" f
JOIN "events" e ON e.id = f."eventId"
WHERE f.winner IS NOT NULL
GROUP BY e.promotion;
```

Results showed suspicious skews:
- **Karate Combat**: 10/0 A-wins (100%)
- **MVP**: 9/1 A-wins (90%)
- **RIZIN**: 4/0 A-wins (100%, tiny sample)
- **PFL / RAF / Oktagon / ONE**: 60–63% A-side (mild skew)
- **UFC**: 57.5% (normal)

**Verified Tapology tracker is not buggy**. Fetched the live Tapology HTML for Karate Combat 59 and MVP Dubois vs Harper, ran the scraper's parent-chain walk against each, and cross-referenced with the DB.
- Tapology reorders displayed fighters so the winner appears on the LEFT of the row after the fight is decided. Winner row gets `from-[#d1f7d2]` (green) gradient, loser row gets `from-[#ffecec]` (pink). These classes live on the fighter's own cell, not a shared ancestor.
- The scraper emits the winner by NAME (from the green-cell fighter), and `tapologyLiveParser.getWinnerFighterId` matches that name to whichever DB fighter has it. So the winner ID resolves correctly regardless of whether the match is A-side or B-side in the DB's fighter1/fighter2 ordering.
- Confirmation on MVP Dubois vs Harper: all 10 fights resolved correctly. One of them (Watson vs Makinen → Makinen) resolved as a B-side win because the daily scraper recorded Watson as fighter1 but Makinen won. If the bug existed, 10/10 would be A — but it's 9/1, because the daily scraper HAPPENED to record 9 of 10 fights in an order that matches Tapology's post-fight reorder. The 100% / 90% A-side skew is **sampling noise**, not a bug.
- There's a weak secondary check (`$corner.find('.bg-green-500').length > 0`) that returns true for both parents in some DOM layouts, but it's redundant because the primary `from-[#d1f7d2]` class check is exclusive to the winner cell. Not worth rewriting for MVP.

**Other scrapers audited**:
- **Oktagon live scraper**: clean. Consumes a JSON API with unambiguous `"FIGHTER_1_WIN"` / `"FIGHTER_2_WIN"` result strings, no DOM scraping. Immune to sticker bugs by construction.
- **Matchroom live scraper**: clean. Per-corner `$corner.hasClass('winner')` and `$corner.find('.winner')` checks scoped to each boxer's own cell. Text-based regex fallback matches by fighter last name. No shared-ancestor queries.
- **BKFC** (`scrapeBKFCLiveEvent.js`): scrapes BKFC's site directly (not Tapology, despite what the handoff doc implied). Uses `[data-render="RedResult" / "BlueResult"]` attributes for explicit per-corner W/L detection. Structurally safe. One latent risk: "Strategy 2" fallback (lines 298–306) does a container-wide `querySelectorAll('[class*="winner"]')` without per-fighter scoping. Not currently firing because the primary attribute-based check handles all real cases, but noted as fragile if BKFC adds a container-level winner marker in the future.
- **Gold Star / RAF**: both use Tapology via `scrapeGoldStarTapology.js` / `scrapeRAFLiveEvent.js`. Covered by the Tapology audit. RAF has no dedicated live scraper, only `rafLiveParser.ts`.
- **UFC**: skipped per user instruction. Consumes ESPN's fight-scorecard JSON, immune by construction.

**Verdict**: the ONE FC per-face sticker bug was a structural quirk specific to ONE FC's DOM (stickers inside the face anchor itself with a `is-win` class resolved to the first match inside the `.event-matchup` ancestor). No other live scraper uses the same pattern. Task 3 returns clean.

## Decisions

- **Task 1 (historical ONE FC winner backfill) descoped for MVP.** The pre-fix ONE FC live tracker silently defaulted every winner to `fighter1` due to the per-face sticker bug, so roughly half of historical ONE FC winners in the DB are wrong. Fixing them requires re-scraping every past event and re-parsing with the new sticker logic. User decision: defer until post-MVP; the scraper fix prevents future regressions and that's enough for now. Handoff doc updated to mark Task 1 as `~~descoped~~`.
- **Full `pnpm build` fix (handoff Task 5) deferred.** Discovered that the backend's `build` script in `package.json` ends with `|| true`, which swallows all TypeScript errors and exits 0. So Render has been deploying successfully this whole time despite the broken type-check — the errors are type-check errors, not runtime errors. The `tsconfig.tracker.json` workaround and the `|| true` shortcut are both buckets under the same leak, but the leak isn't actively hurting anything. Documented as post-MVP tech debt. Recommended minimal follow-up: remove `|| true` AFTER Task 4 (generalize the tracker tsconfig) lands, so type errors become loud again.
- **Backfill scope guard locked to ONE-FC-only fighters.** Any fighter row with fights outside ONE FC is skipped entirely. Prevents quietly changing how a fighter appears in UFC/Bellator/etc. events. 30 rows excluded on top of the 11 filtered by the first-word / word-count safety rule.

## Commits Landed (main branch, not pushed)

- `eb6177c` Fix ONE FC live tracker build: bypass broken routes via focused tsconfig
- `0031664` Fix ONE FC scrapers: reject hero-duplicate matchups + per-face sticker detection
- `7815461` Fix ONE FC fighter names: prefer JSON-LD performer over URL slug

---

## Additional work (evening): Tapology banner cross-contamination bug

### Symptom
User reported that the upcoming event "ACA 202: Kornilov vs. Johnson" (scheduled Apr 12) was displaying Oktagon 86's banner in the app.

### Root cause
Two bugs in `scrapeTopRankTapology.js`, both present in the other six `*Tapology.js` scrapers too:

1. **Missing promotion filter in non-Top-Rank blacklist** (`scrapeTopRankTapology.js:75`). The blacklist rejected `ufc-`, `bellator-`, `pfl-`, `one-`, `rizin-`, `oktagon-`, `bkfc-`, `cage-warriors`, `ksw-`, `lfa-` — but not `aca-`. So ACA 202 was picked up off Top Rank's Tapology promotion page and imported with `promotion: 'TOP_RANK'`.
2. **Weak poster selector**. All 7 Tapology scrapers extracted the event poster with `document.querySelector('img[src*="poster_images"]')` — the *first* match on the page. Tapology event pages include a sidebar listing other upcoming/live events with their posters, and Oktagon 86 was LIVE on 2026-04-11, so its sidebar poster won the selector on every event page scraped today. The wrong bytes were then uploaded to R2 under each event's correct slug.

### Blast radius
Audit of events with `bannerImage` set (via MD5-hash-suffix collision grouping on R2 filenames — same 8-char hash ⇒ same source URL scraped) found **20+ events** contaminated in today's scrapes alone, all sharing hash `72775e5a` (Oktagon 86's sidebar thumbnail URL):
- **Karate Combat**: 11 events (KC 42–46, 54, 55, 59, 60, Kickback 1, Kickback 2). `updatedAt` 15:32 UTC.
- **Top Rank**: 6 events (Inoue vs. Kim, Munguia vs. Surace, Munguia vs. Bazinyan, Ryan vs. Mayer, Beterbiev vs. Bivol, Mbilli vs. Derevyenchenko). `updatedAt` 15:54 UTC.
- **Golden Boy**: 3 events (Golden Boy Fight Night × multiple dates). `updatedAt` 15:57 UTC.
- **ACA 202**: imported as TOP_RANK, deleted entirely.

Zuffa Boxing, Dirty Boxing, MVP, Gold Star, Matchroom, RAF, RIZIN — no collisions in audit. Either not scraped today or happened to avoid the bug. PFL collisions exist but are older (March 27) and PFL uses a non-Tapology scraper, out of scope.

Visual spot-check confirmation: I downloaded the R2 bytes for `events/aca-202-kornilov-vs-johnson-72775e5a.jpg`, `events/inoue-vs-kim-72775e5a.jpg`, and `events/karate-combat-55-72775e5a.jpg` — all identical md5 (`9c74d56e0abe078af3e7c36e5b4c5c8f`), all visually the Oktagon 86 "Materla vs Jungwirth" poster.

### Fix
Commit `da141dd` pushed to `main` and deployed via daily GH Actions.

1. **Expanded non-Top-Rank blacklist** (`scrapeTopRankTapology.js:75`): added `aca-`, `acb-`, `karate-combat`, `dirty-boxing`, `zuffa-boxing`, `gold-star`, `golden-boy`, `matchroom`.
2. **Hardened poster extraction across all 7 Tapology scrapers**. New strategy: prefer `meta[property="og:image"]` (authoritative for the current page's event, set server-side by Tapology), fall back to `img[src*="poster_images/${eventId}/"]` where `eventId` is the numeric prefix parsed from the event URL (`/events/140229-aca-202/` → `140229`). Tapology's poster URLs include the event ID in the path, so the fallback is scoped to images belonging to the current event. Sidebar posters for unrelated events can no longer win.
   - Pattern applied identically to: `scrapeTopRankTapology.js`, `scrapeZuffaBoxingTapology.js`, `scrapeDirtyBoxingTapology.js`, `scrapeKarateCombatTapology.js`, `scrapeMVPTapology.js`, `scrapeGoldStarTapology.js`, `scrapeGoldenBoyTapology.js`.
3. **Passed `eventIdFromUrl` into each `page.evaluate`** as a closure argument since `eventUrl` isn't available inside the browser context.
4. **Syntax-checked all 7 files** with `node -c` after edits.

### Repair runs (triggered via `workflow_dispatch`)
- **Top Rank Tapology Scraper** — completed successfully. Verified: all 6 previously-contaminated events now have new unique hash suffixes in their R2 URLs. Sample: `inoue-vs-kim-ec3069ef.jpg` — downloaded and visually confirmed as the real "The Year of the Monster Continues" Inoue vs. Kim poster.
- **Karate Combat Scraper** — triggered, in progress at session end.
- **Golden Boy Tapology Scraper** — triggered, in progress at session end.

### DB surgery
Deleted `1e1cce09-25fd-4c82-88d4-0af8fd777807` (ACA 202: Kornilov vs. Johnson, imported as TOP_RANK). Cascade: 29 fights, 0 ratings, 0 predictions, 0 reviews. Clean.

### Files
- `packages/backend/src/services/scrapeTopRankTapology.js`
- `packages/backend/src/services/scrapeZuffaBoxingTapology.js`
- `packages/backend/src/services/scrapeDirtyBoxingTapology.js`
- `packages/backend/src/services/scrapeKarateCombatTapology.js`
- `packages/backend/src/services/scrapeMVPTapology.js`
- `packages/backend/src/services/scrapeGoldStarTapology.js`
- `packages/backend/src/services/scrapeGoldenBoyTapology.js`

### Commits
- `da141dd` Fix Tapology scrapers: correct poster image cross-contamination (pushed to origin/main)

---

## Additional work (later evening): Web app social-card download filenames + Vercel deploy pipeline

### Feature
Both social-card generator pages now include the date in the downloaded filename so saved Instagram/Twitter images don't collide across weeks.

- `packages/web/src/app/weekly-hype/page.tsx` — download filename now `good-fights-weekly-hype-{instagram|twitter}-YYYY-MM-DD.png`
- `packages/web/src/app/fight-of-the-night/page.tsx` — download filename now `good-fights-fight-of-the-night-{instagram|twitter}-YYYY-MM-DD.png`

Date format is local `YYYY-MM-DD` built from `new Date()` with zero-padded month/day.

### Vercel deploy pipeline issue (root cause + fix)
User reported `https://web-jet-gamma-12.vercel.app/fight-of-the-night` returning 404 after the code was committed and pushed. Long debug session revealed:

- The `web` Vercel project's **GitHub auto-deploy is not firing on pushes to `main`**. `vercel ls` showed the most recent production deployment was **2 hours old**, predating both `d01f4e1` (Fight of the Night page) and `446c3eb` (filename dates). Both commits were on `origin/main` but Vercel never picked them up.
- Response headers on the stale alias confirmed: `X-Vercel-Cache: HIT`, `X-Matched-Path: /404`, `Last-Modified: 17:25 UTC` — i.e. the edge was serving a cached 404 from a deployment built **before `/fight-of-the-night` existed**.
- The "446c3eb Production Ready" entry the user saw in the Vercel dashboard was a **different project** (likely the backend one) with its root directory misconfigured — its build log showed `Build Completed in /vercel/output [53ms]` and `Missing required build pipeline in turbo.json`, which is the signature of Vercel running from repo root with no real build command. That project was deploying empty output and doesn't serve the `web-jet-gamma-12` alias anyway.

**Fix applied**: installed `vercel` CLI globally (`npm i -g vercel`), already authenticated as `michaelsprimak-5631`, ran `cd packages/web && vercel --prod --yes`. Real 25s Next.js build, aliased to `web-jet-gamma-12.vercel.app`. Verified `/fight-of-the-night`, `/weekly-hype`, and `/` all return 200 and the fight-of-the-night HTML contains the expected "Fight of the Night / LAST WEEKEND" content.

### Operational note (not fixed yet)
The `web` project's GitHub integration for auto-deploy-on-push appears to be disconnected or pointed at a non-`main` branch. Until that is reconnected in Vercel's dashboard (Project → Settings → Git), **web app pushes must be manually deployed**:

```
cd packages/web && vercel --prod
```

This affects every future change to `packages/web`. Worth fixing in a dashboard-aware follow-up.

### Commits
- `446c3eb` Web: include date in social card download filenames (pushed to origin/main, manually deployed via `vercel --prod`)

---

---

## Late-night session: backend TypeScript build cleanup (Task 5 from the prior list)

Followed `docs/typescript-build-cleanup.md`'s order of attack. **81 errors → 0**, full `pnpm build` now works end-to-end, `|| true` cover-up removed, `tsconfig.tracker.json` workaround deleted.

### Pile B — missing import (1 error)

- `packages/backend/src/services/newsScraperService.ts` imported `prisma` from `../utils/prisma`, which doesn't exist. Every other service file in this repo just inlines `const prisma = new PrismaClient()` — matched that pattern instead of inventing a shared module.

### Pile C — DOM types in oneFCLiveScraper (26 errors)

- Added `"DOM"` to `lib` in `packages/backend/tsconfig.json`. The scraper's `page.evaluate()` callbacks reference `document`, `HTMLAnchorElement`, `HTMLImageElement`, etc. — those types live in `lib.dom.d.ts` and were disabled in the base config.
- Trade-off per the brief: every backend file now has DOM globals available, so technically someone could accidentally use `document` in a Node-only file. Accepted — the ergonomic cost is low and Puppeteer callbacks are the only legit use site.
- This was the recommended fix (Approach 1) because it also lets the ONE FC live tracker build under the main `pnpm build`, collapsing the `tsconfig.tracker.json` workaround.

### Pile A1 — `description` not in `FastifySchema` (19 errors)

- **Not a root-module augmentation.** `FastifySchema` is declared in `fastify/types/schema`, not the root `fastify` module — augmenting `declare module 'fastify'` silently no-ops.
- Fix: added a second `declare module 'fastify/types/schema'` block in `packages/backend/src/types/fastify.d.ts` that re-adds `description?`, `summary?`, `tags?`. One file, zero changes to route files. Cleaner than deleting 19 fields across 4 routes, and preserves the descriptions for when `@fastify/swagger` is eventually added.

### Pile A2 — `any is not assignable to never` on log.error (35 errors)

- **The brief misdiagnosed this.** It called it a Fastify `reply.send()` generic-inference regression. It's not — every erroring site is `request.log.error(msg, error)`, not `reply.send`.
- **Root cause**: Pino 9's `LogFn` type uses template-literal `ParseLogFnArgs<TMsg>`. When a message string contains no `%s` / `%d` placeholders, the rest args tuple evaluates to `[]`, so the rest parameter type becomes `never`. Passing a second arg then fails as "any not assignable to never".
- **Fix**: swapped argument order from `log.error(msg, err)` to `log.error(err, msg)`. This is also the *correct* idiomatic Pino usage — the error now serializes via `pino-std-serializers` as a structured `err` field instead of being dropped at runtime. **Double win**: type-safe AND the error logs actually contain errors now.
- Applied via sed to all 35 sites in `auth.fastify.ts` (18), `index.ts` (13), `news.ts` (4). Uses regex `request\.log\.error\('([^']*)', ([A-Za-z_][A-Za-z0-9_]*)\);` so it handles both `error` and `verifyError` identifiers.
- **Out of scope but worth noting**: `crews.ts` (15 calls) and `upload.ts` (2 calls) have the same broken pattern, but they're in `tsconfig.production.json`'s `exclude` list so they aren't type-checked. Same fix applies, same runtime win for error logs — worth cleaning up next time someone's in there.

### Build script cleanup (the main goal)

- `packages/backend/package.json:8` — removed `|| true` and `--noEmitOnError false`. New script: `prisma generate && tsc --project tsconfig.production.json && { cp src/services/*.js dist/services/ 2>/dev/null || true; }`.
- **Important**: the `|| true` is now inside a brace group `{ ...; }` so it only rescues the `cp` step (Windows/bash glob-empty compat), not the entire chain. Under the old form `tsc && cp || true` the `|| true` actually applied to the whole `&&` chain due to shell left-associativity — meaning a failing tsc would silently become exit 0. That's the exact bug we were fixing. Verified the brace-group scoping with `bash -c 'false && true && { false || true; }; echo $?'` → exit 1.
- Snapshot verification: `npx tsc --noEmit -p tsconfig.production.json` → 0 errors, `pnpm build` → exit 0, `dist/` fully populated including `oneFCLiveScraper.js`, `newsScraperService.js`, and all 21 route files.

### Tracker tsconfig collapse

- **Deleted** `packages/backend/tsconfig.tracker.json` — its entire purpose was sidestepping the broken main build, which is no longer broken.
- **Reverted** `.github/workflows/onefc-live-tracker.yml` to `pnpm build` (from `npx tsc --project tsconfig.tracker.json`). pnpm was already set up in that workflow so no other changes needed.
- **Not verified against live CI** — I can't dispatch the workflow without a push and a live event. Worth a manual `workflow_dispatch` before the next ONE FC event to confirm the workflow still builds cleanly end-to-end.

### Footguns hit during the session

1. **Stale `.tsbuildinfo` caused a phantom "success".** At one point `pnpm build` exited 0 but `dist/` was completely empty. Root cause: `incremental: true` in `tsconfig.json` + a stale `tsconfig.production.tsbuildinfo` from a prior broken build — tsc thought nothing had changed and skipped emit entirely. Fix: `rm tsconfig*.tsbuildinfo && pnpm build`. If CI ever ships a "successful build" with an empty dist, check for this.
2. **Module augmentation against the wrong submodule.** First attempt put `interface FastifySchema` under `declare module 'fastify'` — it compiled, but did absolutely nothing because the real interface lives in `fastify/types/schema`. TypeScript does not warn when your augmentation misses. Always grep the actual `.d.ts` file to find which submodule owns the type before augmenting.
3. **Windows bash vs cmd.exe in pnpm scripts.** Local Windows `pnpm build` shows `The system cannot find the path specified.` on the `cp src/services/*.js dist/services/` step because the glob doesn't match under cmd.exe. Harmless on Render (Linux bash) and the `|| true` protects the build. Pre-existing behavior, not a regression.

### @ts-nocheck on fighterMatcher.ts (left as-is)

The brief said "consider removing". I didn't. The file's top-level comment is explicit: it references `prisma.fighterAlias`, which no longer exists in the Prisma schema. Removing `@ts-nocheck` would surface real errors requiring actual dead-code deletion (a separate task). Flagged for follow-up; not required for the build to be honest.

### docs/typescript-build-cleanup.md is now partially outdated

The brief's diagnoses for Piles A1 and A2 were both wrong in ways that only surfaced by actually running the fix:
- A1 said "module augmentation" would work but didn't mention that `FastifySchema` lives in `fastify/types/schema`, not the root module.
- A2 said all 35 errors were `reply.send` regressions; they're all `log.error` / Pino 9 `ParseLogFnArgs` regressions.

Haven't updated the doc. Probably worth marking it "COMPLETED — see 2026-04-11 daily" at the top and leaving the diagnostic mistakes visible as a record of why the original hypothesis was wrong.

---

## Late-night session: Tapology scraper fight-list bleed-through + parser un-cancel sig collision

### Symptom
User reported seven upcoming events with wrong fight lists, wrong event names, and/or wrong banner images:

- MVP Apr 18 (Baumgardner vs. Shin MVPW 02) — showed Tyson Fury + other unrelated boxing fights
- RAF Apr 18 (RAF 08) — Jason Nolf listed in two fights (vs Joey Blaze AND vs Marcus Blaze)
- MVP May 16 (Rousey vs. Carano) — wrong fights + wrong image
- Top Rank May 16 (Davis vs. Albright II) — wrong image
- Gold Star May 23 (Usyk vs. Rico) — fight list didn't match the name
- Top Rank May 30 (Foster vs. Ford) — wrong image + "reverse" fight order
- MVP May 30 (Han vs. Holm II MVPW 03) — wrong

### Root causes (two independent bugs)

**Bug 1 — Tapology daily scrapers pulled fighter links from the entire page.** All 7 `scrape*Tapology.js` daily scrapers used `document.querySelectorAll('a[href*="/fightcenter/fighters/"]')` at page scope, then paired consecutive links as bouts. Tapology event pages render fighter links from sidebar widgets and related-event panels, so ~25 unrelated boxing fights (Tyson Fury/Makhmudov, Conor Benn/Prograis, Frazer Clarke/Huni, Jeamie Tshikeva/Riakporhe, Breyon Gorham, Felix Cash, Elliot Whale, etc.) got injected into every Tapology-scraped event. Inspection showed 43 fights on MVPW 02, 46 on Rousey/Carano, 22 on Davis/Albright II and Foster/Ford, 39 on Han/Holm II, 25 on Zuffa Boxing 6, 17 on Usyk/Rico — all containing the same bleed-through set.

The `tapologyLiveScraper.ts` already does it correctly (iterates `li[class*="border-b"]` and pulls fighter links only from within each `<li>`). Fixed all 7 daily scrapers to match that pattern:

- `scrapeMVPTapology.js`, `scrapeTopRankTapology.js`, `scrapeGoldStarTapology.js`, `scrapeGoldenBoyTapology.js`, `scrapeZuffaBoxingTapology.js`, `scrapeKarateCombatTapology.js`, `scrapeDirtyBoxingTapology.js`

**Bug 2 — Data parsers used last-name-only signatures for un-cancel detection.** 5 daily parsers (`mvpDataParser`, `rafDataParser`, `dirtyBoxingDataParser`, `karateCombatDataParser`, `zuffaBoxingDataParser`) built cancellation-detection signatures from last names, which produced two failure modes:

1. **Name collision**. RAF 08's live page has `Jason Nolf vs Joey Blaze`, but the DB had a stale `Jason Nolf vs Marcus Blaze` row from an earlier scrape. Both brothers share last name "Blaze", so the sorted-last-name sig `blaze|nolf` matched the stale row, and RAF's cancellation detection decided the stale fight was still present. It never got cancelled.
2. **Multi-word last names**. The scrape side used `name.split(/\s+/).pop()` (last word), while the DB side used the full `lastName` column. For "Bo Mi Re Shin" stored as `{firstName: 'Bo', lastName: 'Mi Re Shin'}`, scrape sig = `baumgardner|shin` but DB sig = `baumgardner|mi re shin`. Mismatch → Baumgardner vs Shin got stuck in CANCELLED state. Same bug hit Thibeault vs Santos, Rosado vs Delgado, Harvey vs Medina, Jr. dos Santos vs Despaigne, Sheeraz vs Begic, Mosley vs Bohachuk.

Fixed by switching both sides to `[fighter1Id, fighter2Id].sort().join('|')` — the fighter IDs are resolved after the upsert and are globally unique.

### Banner images
User reported wrong images on MVP May 16, Top Rank May 16, Top Rank May 30. Two were polluted during a previous poster-contamination scrape (cached R2 copies, suffix `04b56edf`); two MVP events had `bannerImage` pointing at Tapology poster ID 135520 despite the event URL being 140072/140777. All 5 bannerImages nulled; next scraper run (now with scoped fight extraction + the earlier `da141dd` poster fix) will re-upload fresh.

### DB cleanup
`packages/backend/scripts/cleanup-upcoming-events.js` written with per-event keep lists (substring-matched fighter pairs, tolerant of multi-word last names) + explicit individual cancellations + banner-image clears. Dry-run first, `--apply` opt-in. Applied to prod:

- **62 bleed-through fights cancelled** across Baumgardner/Shin MVPW 02, Davis/Albright II, MVP MMA 1 Rousey/Carano, Usyk/Rico, Foster/Ford, Han/Holm II MVPW 03, Zuffa Boxing 6.
- **7 real fights un-cancelled** (Baumgardner, Thibeault, Rosado, Harvey, Jr. dos Santos, Sheeraz, Mosley) — these had been wrongly cancelled by the un-cancel sig collision bug.
- **`Jason Nolf vs Marcus Blaze`** cancelled (stale, live page has only Nolf vs Joey Blaze).
- **5 bannerImage fields nulled** for the affected events.

### Files
- `packages/backend/src/services/scrapeMVPTapology.js`, `scrapeTopRankTapology.js`, `scrapeGoldStarTapology.js`, `scrapeGoldenBoyTapology.js`, `scrapeZuffaBoxingTapology.js`, `scrapeKarateCombatTapology.js`, `scrapeDirtyBoxingTapology.js`
- `packages/backend/src/services/mvpDataParser.ts`, `rafDataParser.ts`, `dirtyBoxingDataParser.ts`, `karateCombatDataParser.ts`, `zuffaBoxingDataParser.ts`
- `packages/backend/scripts/cleanup-upcoming-events.js` (new)
- `packages/backend/scripts/inspect-upcoming-issues.js` (new, diagnostic)
- `packages/backend/scripts/inspect-raf-fights.js` (new, diagnostic)

### Followups
- Re-trigger daily scrapers via `workflow_dispatch` to repopulate banners and verify the fixed scoped extraction produces clean data.
- The same lastname-only cancellation sig pattern exists in other data parsers — checked `topRankDataParser.ts` which uses full firstName+lastName (safer, left alone). Other parsers (`oneFCDataParser`, `goldStarDataParser`, `goldenBoyDataParser`, `pflDataParser`, `matchroomDataParser`, `rizinDataParser`, `oktagonDataParser`, `bkfcDataParser`, `ufcDataParser`) should be audited for the same pattern in a follow-up, but none triggered today's symptoms.

---

## Late-night session (part 2): Oktagon 87 missing its own title fight

### Symptom
User reported Oktagon 87 (SZABOVÁ VS. FERNANDES, scheduled Apr 25) was missing the namesake Szabová vs Fernandes title fight plus 5 of 9 total fights. DB showed 9 rows total: 3 UPCOMING (Humburger/Kadestam, Hutyra/Birdal, Knutsson/Simplicio), 6 CANCELLED including the title fight. Event's `updatedAt` was 18:58 UTC — before my earlier session, so this was pre-existing state the daily cron had been producing.

### Root causes (two stacked bugs)

**Bug 1 — `scrapeAllOktagonData.js` `fightCard` query-key match too loose.** Oktagon's Next.js page embeds a dehydrated React Query state in `__NEXT_DATA__`. The scraper walked the `queries` array looking for fight card data:

```js
for (const query of queries) {
  const queryKey = query?.queryKey || [];
  const data = query?.state?.data;
  if (queryKey.includes('fightCard') && Array.isArray(data)) {
    fightCards = data;
  }
}
```

A puppeteer probe against the live Oktagon 87 page revealed there are now **two** queries whose `queryKey` contains `'fightCard'`:

- `['events', 'fightCard', 129]` → `array[2]` (2 card objects, each with real `.fights` arrays containing `{fighter1, fighter2, titleFight, ...}` — the actual data the scraper wants)
- `['bettings', 'fightCard', 129]` → `array[9]` of fight ID strings like `"1358"`, `"1384"` (used by the betting widget)

The `bettings` query iterates later in the loop, so `fightCards = data` reassigned to the array of strings. The subsequent `fightCards.forEach(card => { const fights = card.fights || []; ... })` found no `.fights` property on each string, `hasFightCard` never got set to true, and the scraper returned `{ skipped: true, reason: 'No fight card' }`. The Oktagon data parser then skips events that aren't in the scraped set, so the DB rows never got touched and the old state lingered.

**Fix**: tighten the match to `queryKey[0] === 'events' && queryKey[1] === 'fightCard'`. Same tightening applied to the tournament (event-image) detection branch which also used `queryKey.includes('events')`.

Verified locally with a puppeteer probe against the live page: fix produces 10 fights (1 title + 9 main card + 1 TBA) with correct fighter1/fighter2 structure.

**Bug 2 — `oktagonDataParser.ts` cancellation detection not diacritic-normalized.** After fix #1 deployed and the scraper re-ran successfully, the 6 cancelled fights stayed cancelled. The parser's un-cancel logic builds name-pair signatures like `${firstName} ${lastName}`.toLowerCase().trim(), but:

- **DB side**: fighter rows are stored with diacritics stripped (`scrapeAllOktagonData` → `oktagonDataParser` calls `stripDiacritics(firstName)` and `stripDiacritics(lastName)` on lines 391/407/423/465 before writing). So the DB row has `lastName: 'Szabova'`.
- **Scraper side**: `scrapedFightPairs` was built directly from the scraped fighter names which still contain the raw UTF-8 (`'Szabová'`).

String equality then fails for every Czech/Slovak/Polish/Romanian name with non-ASCII characters, so the un-cancel branch never fires. This is a latent bug that has probably been silently affecting Oktagon and RIZIN events for months — Szabová vs Fernandes just made it visible because the scraper fix re-surfaced previously-cancelled rows that would have been un-cancelled if the name comparison worked.

**Fix**: local `normName` helper wrapping `stripDiacritics + lowercase + trim`, applied to both sides of every comparison: `scrapedFighterNames`, `scrapedFightPairs`, the cancel loop's DB name build, and the un-cancel loop's DB name build (4 sites total).

### Verification

Post-fix Oktagon 87 state:
```
OKTAGON 87: SZABOVÁ VS. FERNANDES  (updated 2026-04-11T21:08:04 UTC)
  UPCOMING: 9  CANCELLED: 0  total: 9
    [1] UPCOMING  TITLE Lucia Szabova vs Leidiane Fernandes
    [2] UPCOMING        Dominik Humburger vs Zebaztian Kadestam
    [3] UPCOMING        Vladimir Lengal vs Ozan Aslaner
    [4] UPCOMING        Radek Rousal vs Samuel Bark
    [5] UPCOMING        Jan Malach vs Stefan Koncar
    [6] UPCOMING        Vaclav Stepan vs Firas Daud
    [7] UPCOMING        Mateusz Strzelczyk vs Vojtech Garba
    [8] UPCOMING        Alex Hutyra vs Umut Birdal
    [9] UPCOMING        Josefine Knutsson vs Mileide Simplicio
```

All 9 real fights UPCOMING, title fight at main event order 1, zero cancelled. The scraper fix also repairs every other Oktagon event that had been silently dropping fights since Oktagon's backend started serving the bettings query.

### Files
- `packages/backend/src/services/scrapeAllOktagonData.js`
- `packages/backend/src/services/oktagonDataParser.ts`
- `packages/backend/scripts/probe-oktagon87.js` (new, diagnostic)
- `packages/backend/scripts/probe-oktagon87-fights.js` (new, diagnostic — exercises the scraper's logic against the live page)
- `packages/backend/scripts/inspect-upcoming-only.js` (new, diagnostic — was already committed with the earlier Tapology session)

### Commits
- `2940325` Fix Oktagon scraper dropping all fights when bettings query present
- `5093a03` Fix Oktagon parser un-cancel: strip diacritics before name comparison

### Latent-bug audit followup (worth doing soon)
The diacritic bug in `oktagonDataParser.ts` exists identically in **every data parser that uses name-pair signatures for cancellation/un-cancellation**:

- `topRankDataParser.ts` (line 667) — full name, not diacritic-stripped
- `oneFCDataParser.ts` — TBD, not audited in this session
- `matchroomDataParser.ts` — TBD
- `pflDataParser.ts` — TBD
- `rizinDataParser.ts` — TBD (high impact, Japanese names are OK but any Brazilian fighter like Felipe Froes would be affected if Tapology serves the diacritic form)
- `goldStarDataParser.ts` — TBD
- `goldenBoyDataParser.ts` — TBD
- `bkfcDataParser.ts` — TBD
- `ufcDataParser.ts` — TBD

The `mvpDataParser`, `rafDataParser`, `dirtyBoxingDataParser`, `karateCombatDataParser`, `zuffaBoxingDataParser` parsers were fixed earlier today to use fighter-ID-based signatures instead of name-based — they're immune to this class of bug. Everything else that still builds a name-pair sig for cancellation has the same latent diacritic bug as Oktagon's.

Mechanical fix: add a `normName` helper at the top of each parser (`stripDiacritics + lowercase + trim`) and apply it to both sides of every `scrapedFightPairs.has(...)` check. Worth doing in a single sweep to prevent the next Sebastião/Müller/Žilinskas from getting silently cancelled.

---

## Remaining Work (for the next session)

Ordered by MVP priority:

1. **Task 1 (descoped but noted)** — historical ONE FC winner backfill. ~half of ONE FC fight winners in the DB are wrong due to the pre-fix per-face sticker bug. Revisit post-MVP when correctness of historical data matters.
2. **ONE FC name backfill tail** — 10 ONE-FC-only fighters still have `firstName = ''` because they weren't in any scrape-able event during the backfill run. Will self-heal as their events cycle through the daily scraper; no explicit action needed unless they never do.
3. **Fastify log.error cleanup in `crews.ts` + `upload.ts`** — 17 more `log.error(msg, err)` sites that aren't currently type-checked (those files are in the `exclude` list) but have the same Pino 9 runtime bug dropping error context from logs. Mechanical `sed` fix, same pattern as A2.
4. **Remove `@ts-nocheck` from `fighterMatcher.ts`** — requires actually deleting the dead `FighterAlias` code paths. Small but real work, not a config-only fix.
5. **Manual dispatch of `onefc-live-tracker.yml`** to confirm the collapsed workflow still builds end-to-end under `pnpm build`. Do this before the next ONE FC live event.
6. **Generalize the `exclude` list audit in `tsconfig.production.json`** — several entries reference files like `src/routes/auth.ts`, `src/routes/events.routes.ts` that may or may not still exist. Brief warned some are ghosts from past refactors. Verify + delete stale entries.

---

## Evening session 3 — Gold Star OTA, admin ET fix, live-list fallback

Three small, independent fixes driven by live-event operator frustration during UFC 327.

### Mobile: Gold Star + RAF org filter buttons (OTA)

The mobile `ORGANIZATIONS` array already had `GOLD STAR` (added yesterday in `e5b4962`) and `RAF` (added in `879fe9a`), but the installed production app predated both. Shipped both via a single EAS OTA update on the `production` branch — no code change needed, just:

```bash
cd packages/mobile && eas update --branch production --message "Add Gold Star + RAF to org filter"
```

- iOS update group: `0b432572-d4cd-4971-ab59-9660f5082ecc` (runtime 2.0.1)
- Android update group: `73d43324-9ce2-40b9-9016-7d494f22f82e` (runtime 1.0.0)

Lesson for next time operator asks "why isn't the new org button showing": first check whether the change is merely already-merged code that hasn't been OTA'd yet. Installed builds are almost always behind `main` for JS-only changes.

### Backend: admin.html ET→UTC start-time conversion bug fix

Commit: `fb0f94e`.

**Symptom**: operator enters `23:30` (11:30 PM ET) as main card start time on `admin.html` for Rizin FF — Landmark Vol. 13, saves, and the field reloads showing `19:30` (7:30 PM ET). Mobile app also shows 7:30 PM. A flat 4-hour (EDT) shift.

**Cause**: the `etToUTC` helper at `packages/backend/public/admin.html:2307` used a round-trip trick to detect the ET offset:

```js
const naive = new Date(`${dateStr}T${timeStr}:00Z`);
const etStr  = naive.toLocaleString('en-US', { timeZone: 'America/New_York' });
const etDate = new Date(etStr);
const offsetMs = naive.getTime() - etDate.getTime();
return new Date(naive.getTime() + offsetMs).toISOString();
```

This works when the browser's local timezone is NOT ET: `new Date(etStr)` parses the ET wall-clock string in the browser's local zone, producing a different instant than `naive`, and the delta is the ET offset.

**It silently returns 0 when the browser is already in ET**, because `new Date(etStr)` parses the same wall clock as local-ET and lands on the same instant as `naive`. The result: the entered wall clock is written to UTC verbatim (23:30 → stored as 23:30Z), then the display code at line 2258 correctly re-renders 23:30Z as `toLocaleTimeString(..., { timeZone: 'America/New_York' })` → 19:30. Exactly the −4 shift operator reported.

**Fix**: replaced with a probe-both-offsets approach that is independent of the browser's local timezone:

```js
const etToUTC = (dateStr, timeStr) => {
  if (!dateStr || !timeStr) return undefined;
  const fmt = new Intl.DateTimeFormat('en-US', {
    timeZone: 'America/New_York',
    year: 'numeric', month: '2-digit', day: '2-digit',
    hour: '2-digit', minute: '2-digit', hour12: false,
  });
  const want = `${dateStr}T${timeStr}`;
  for (const offset of ['-04:00', '-05:00']) {
    const candidate = new Date(`${dateStr}T${timeStr}:00${offset}`);
    const parts = Object.fromEntries(fmt.formatToParts(candidate).map(p => [p.type, p.value]));
    const hour = parts.hour === '24' ? '00' : parts.hour;
    const got = `${parts.year}-${parts.month}-${parts.day}T${hour}:${parts.minute}`;
    if (got === want) return candidate.toISOString();
  }
  return new Date(`${dateStr}T${timeStr}:00-04:00`).toISOString();
};
```

Builds an ISO string with both EDT and EST offsets, formats each candidate back in `America/New_York`, and picks whichever one reproduces the requested wall clock. DST-aware. Does not observe the browser's `Intl` default zone at all.

`admin.html` is served as a static file by `@fastify/static` in `server.ts`, so the fix ships with the normal Render backend redeploy — no rebuild step.

**Lesson worth saving**: any "detect timezone offset by round-tripping through `toLocaleString` + `new Date(str)`" trick is fragile because `new Date(str)` parses in **local browser time**, which is exactly the variable the code is trying to abstract over. For user-facing TZ conversion, probe offset candidates explicitly via `Intl.DateTimeFormat.formatToParts`.

### Mobile: Live/Upcoming tab time-based live fallback (OTA)

Operator reported that UFC 327 was showing "LIVE NOW" on the event **detail screen** in the app, but the same event was still on the **Upcoming Events** list tab — not the Live Events tab — two minutes after the start time.

**Root cause**: the list screens (`app/(tabs)/live-events.tsx`, `app/(tabs)/events/index.tsx`) filter strictly by `event.eventStatus === 'LIVE'` / `!== 'LIVE'`. The event detail screen (`app/(tabs)/events/[id].tsx:448`) is more lenient: `isEventLive = (now >= startTime && eventStatus !== 'COMPLETED') || eventStatus === 'LIVE'`. The backend lifecycle job that flips `UPCOMING → LIVE` runs every 5 minutes (`services/eventLifecycle.ts`), so there's always a window of up to ~5 min where start-time has passed but `eventStatus` is still `UPCOMING`. During that window the detail screen says live but the list tabs disagree.

**Fix**: added an inline `isEventLiveNow(event)` helper to both list screens that mirrors the detail-screen fallback:

```ts
const isEventLiveNow = (event: Event): boolean => {
  if (event.eventStatus === 'LIVE') return true;
  if (event.eventStatus === 'COMPLETED') return false;
  const startStr = event.earlyPrelimStartTime
    || event.prelimStartTime
    || event.mainStartTime
    || event.date;
  if (!startStr) return false;
  return Date.now() >= new Date(startStr).getTime();
};
```

- `live-events.tsx:122` now filters with `deduped.filter(isEventLiveNow)`.
- `events/index.tsx:176` now filters with `.filter((event: Event) => !isEventLiveNow(event))`.

The `COMPLETED` short-circuit is the safety valve: once the lifecycle job eventually flips the event to COMPLETED, it immediately drops off the Live tab even if start-time is still in the past. Both screens already poll every 30s via React Query (`refetchInterval: 30000`), so as the clock crosses start-time the tabs update automatically without a manual refresh.

Shipped via OTA on the `production` branch:

```bash
cd packages/mobile && eas update --branch production --message "Live/Upcoming tabs: time-based live fallback (no 5-min lag)"
```

- iOS update group: `d1dc7d89-dd72-40de-beba-582a177bd5ba`
- Android update group: `c26f5816-f86d-4193-aa3e-9bdb4ef465ed`

Chose inline helpers in each file over a shared `utils/eventStatus.ts` because the helper is 7 lines and only used in two places; adding another util file would be net negative.

**Remaining inconsistency (not fixed)**: the 2-page autofetch cap on `live-events.tsx` (`EVENTS_PER_PAGE=5`, autofetch up to 2 pages = 10 events) means if a now-live event is beyond the first 10 upcoming events, it still won't appear on the Live tab until the backend flips its status and it rises to the top of the upcoming query. In practice the backend sort puts imminent events first so this has never mattered, but it's a latent edge case.

---

### Backend: un-cancel events that reappear on source (15 data parsers)

- **Symptom**: Live PFL Chicago 2026 (Apr 11, currently airing) missing from the app. Spot check revealed PFL Belfast, Sioux Falls, and Brussels — all still on pflmma.com's upcoming tab — were also stuck at `eventStatus = CANCELLED`. PFL Africa Nigeria (Jun 13) was the only upcoming PFL event marked `UPCOMING`.
- **Root cause**: every promotion parser has an "event-level cancellation detection" block (`pflDataParser.ts:590-616` and siblings) that marks any DB event as `CANCELLED` when it's missing from a scrape. That part is fine on its own. The bug is the inverse: the `if (event)` update branch deliberately refuses to touch `eventStatus` ("lifecycle service manages it"), so once an event was cancelled — e.g. during an earlier partial/flaky run that produced an incomplete scraped-events set — subsequent successful scrapes would re-import all its fights but never un-cancel the event itself. Today's 22:21 UTC PFL scraper log confirms: `✓ Event: PFL Chicago 2026 (4/11/2026)`, `✓ Imported 10/10 fights`, zero cancellation messages, yet the event stayed `CANCELLED` and hidden from the Live/Upcoming tabs.
- **Fix**: every parser's "existing event" update path now captures `wasCancelled = event.eventStatus === 'CANCELLED'` before updating, and when true spreads `{ eventStatus: 'UPCOMING', completionMethod: null }` into the update data and un-cancels the event's `CANCELLED` fights. Un-cancelling only fires when the event is re-discovered by the scraper, so it never fights the lifecycle service for non-cancelled events.
- **Applied to 15 parsers**: pfl, bkfc, dirtyBoxing, goldenBoy, goldStar, karateCombat, matchroom, mvp, oktagon, oneFC, raf, rizin, topRank, ufc, zuffaBoxing. The `dirtyBoxingDataParser` P2002 fallback shares one `updateDataWithUncancel` object so both inner update calls inherit the fix. The `ufcDataParser` computes `wasCancelled` from the pre-existing `existingEvent.eventStatus` since it uses an `upsert` with a separate `findUnique`/`findFirst` lookup. TypeScript check: 0 errors in any `*DataParser.ts` file.
- **Manual remediation during the live event**: un-cancelled PFL Chicago 2026 via admin panel so viewers could rate tonight's card; Belfast / Sioux Falls / Brussels still need to be un-cancelled manually (or will self-heal on the next scraper run with the new fix deployed). Also identified a duplicate ghost event `PFL Africa 2 Pretoria` (same date 2026-04-10, mainStartTime 20:00 UTC, vs the real `PFL Africa Pretoria` at 22:00 UTC — now `COMPLETED`) that should be deleted via admin.
- **Files touched** (15): `bkfcDataParser.ts`, `dirtyBoxingDataParser.ts`, `goldStarDataParser.ts`, `goldenBoyDataParser.ts`, `karateCombatDataParser.ts`, `matchroomDataParser.ts`, `mvpDataParser.ts`, `oktagonDataParser.ts`, `oneFCDataParser.ts`, `pflDataParser.ts`, `rafDataParser.ts`, `rizinDataParser.ts`, `topRankDataParser.ts`, `ufcDataParser.ts`, `zuffaBoxingDataParser.ts`.

---

### Backend: RAF scraper — banner URL truncated on filenames containing parens

- **Symptom**: RAF 08 event card on the app had no banner image. DB row showed `bannerImage = https://cdn.prod.website-files.com/.../69d521cce17d0ba08a8a7365_RAF08%201920x1080fff%20(1` — truncated with an unclosed paren. Unlike RAF 06, 07, 09 (which all live on R2 as `pub-7bef67fde....r2.dev/events/raf-XX-HASH`), RAF 08 was pointing at the raw Webflow CDN, meaning the R2 upload failed — the parser then fell back to the (already-mangled) source URL.
- **Root cause**: `scrapeAllRAFData.js:131` extracts the banner via regex `/url\("?([^")\s]+)"?\)/`. The character class `[^")\s]+` stops at the first `)`. The real URL is `..._RAF08%201920x1080fff%20(1).webp` — the `(1)` in the filename is a duplicate-upload suffix from whoever uploaded the asset to Webflow. The regex captures everything up to the `)` inside `(1)`, dropping `.webp` and the outer closing paren. The truncated URL 404s in the R2 upload step; parser falls back to the same broken URL.
- **Verified against live site**: fetched `realamericanfreestyle.com/events-gallery` and confirmed the inline style is `background-image:url(&quot;https://cdn.prod.website-files.com/.../69d521cce17d0ba08a8a7365_RAF08%201920x1080fff%20(1).webp&quot;)` — real filename contains literal `(1)`.
- **Fix**: rewrote the extraction to (a) isolate the `background-image` declaration via `split(/;/)`, (b) take everything between `url(` and the LAST `)` in that declaration, (c) strip optional wrapping quotes. Handles URLs with unescaped parens regardless of whether the source HTML wraps the URL in `"..."`, `'...'`, or bare.
- **Latent bug spotted in `scrapeAllPFLData.js:292`**: `background-image:\s*url\(['"]?([^'")\s]+)['"]?\)` has the same truncate-on-first-`)` defect. Not affecting any PFL event today (none of their banners contain literal parens), but fixing preemptively is cheap — deferred per user scope.
- **Remediation**: re-run RAF scraper via `workflow_dispatch` after pushing the fix; the parser's existing-event update path will overwrite the broken URL, upload the full `.webp` to R2, and replace `bannerImage` with a proper `pub-...r2.dev/events/raf-08-HASH.webp` URL. No manual DB edit needed.
- **Verified end-to-end**: dispatched `raf-scraper.yml` on `main` post-push (run `24294304448`), completed success. DB check confirms `RAF 08 | UPCOMING | https://pub-7bef67fde....r2.dev/events/raf-08-6e213076.webp` — matches the format of RAF 06/07/09.
- **Same latent bug in `scrapeAllPFLData.js`** (line 292 inline style + line 302 computed style): same `[^'")\s]+` pattern. Not currently hurting any PFL event (no filename with literal parens today), but fixed in a follow-up commit with the same `extractCssUrl` helper defined inside the Puppeteer `page.evaluate` closure, used for both inline and computed-style paths.

---

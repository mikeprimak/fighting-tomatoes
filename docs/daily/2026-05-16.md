# 2026-05-16

## Planning session — project triage + Fan DNA system design

No code shipped today. Long planning conversation that locked in a massive new feature (Fan DNA) and clarified the priority order across the 6 active workstreams. Everything captured in `CLAUDE.md` under a new "Next session — triage" section.

### Triage of active workstreams

Ranked plan added to `CLAUDE.md`:
1. **Ship the work that's already done** — `git push` commit `f050c64` (AI preview on UpcomingFightCard) + commit the uncommitted hype-modal AI preview variant + EAS update. **PAUSED** pending refinements (see below).
2. **AI enrichment cron** — T-10/T-5/T-2 cadence, GH Actions, ~3-4 hr. Needed before UFC White House (June 15).
3. **Marketing — activate attribution spine** — PostHog SDK + identify + install referrer all dormant until next EAS production build.
4. **Rewarding Users — Wave 3 (Fan DNA)** — see below; blocked on #2 producing aiTags across upcoming cards.
5. **Follow-fighter** — blocked on Mike's Q1-Q6 from 2026-05-14.
6. **Web app continuation** — mid-QA; custom-domain migration is the real unblocker.

### Refinements before pushing #1

- **AI one-liner phrasing rules** — too many one-liners lead with "Netflix" / streaming platform / event-meta nouns. When read in aggregate they sound robotic. Need explicit prompt rules in `extractFightEnrichment.ts`: avoid leading with broadcaster, vary openers across a card, prefer the fight-specific hook (storyline, stake, style) over event-meta hook. Re-extract pass after prompt change.
- **AI one-liner UI placement** — current `UpcomingFightCard` + full hype modal placements disrupted visual balance Mike previously dialed in. Consider **fight detail screen** as the primary home before pushing `f050c64`. Card and modal placements are not load-bearing.

### Marketing attribution — granularity bar raised

Mike wants per-channel **per-campaign** attribution: Reddit campaign X vs Google ad Y vs specific Twitter post vs organic App Store search for "good fights". Implications:
- UTM schema needs a documented campaign-naming convention with a slot for the specific post/ad/keyword
- Twitter Hype Index posts each get their own `utm_content` (per-tweet ID)
- App Store organic search requires App Store Connect Search Ads / search-term reporting setup
- Reddit + Google ad campaigns each need a campaign-level UTM template

Document at `docs/marketing/attribution-spine.md` before the next EAS production build.

### AI enrichment — pre/post-fight architectural commitment

AI enrichment is now explicitly a **two-sided** workstream:
1. **Pre-fight scrape + LLM parse** (Phase 1 shipped) → narrative for hype/preview surfaces.
2. **Post-fight scrape + LLM parse** (NEW) → fight result narrative (how it went, pace, momentum, finish quality, surprises). Feeds Fan DNA traits (back-and-forth affinity, pace preference, finish quality bias) + closure-loop row copy + Hype DNA inputs.
3. **Commercial dataset value** — the combined pre+post corpus across promotions is a sellable dataset (second moat after follow-fighter). Build the pipeline aware of this: provenance, source URLs, confidence, timestamps, exportable format.

Action items flagged for next AI Enrichment session — add post-fight phase to `docs/areas/ai-enrichment.md` roadmap, add insight family to `docs/areas/rewarding-users.md` Wave 3, link commercial dataset thesis across both area docs + `docs/areas/follow-fighter.md`.

## Fan DNA — full architectural plan

The bulk of the session. **Rewarding Users Wave 3 ("Hype DNA") is renamed and rescoped to Fan DNA**: a multi-signal personality engine that surfaces insights about the user as a fan, not just their hype accuracy.

### Architectural consolidations

- **Hype accuracy folds INTO Fan DNA as one trait among many.** The `Hype vs Outcome` profile section that shipped 2026-05-15 gets replaced by a single `Fan DNA` SectionContainer when the new wave ships. Prevents the profile from becoming a wall of single-insight sections.
- **Wave 2 and Wave 3 collapse into one Fan DNA wave** that grows trait-by-trait as data allows. `Hype vs Outcome` is reframed as the first Fan DNA trait, not a standalone deliverable.

### Trait taxonomy

Four families × four tiers by data dependency (= shipping order):
- **A. Affinity** — style, pace, finish, outcome, weight class, card position, org, gender
- **B. Behaviour** — rating bias, cadence/tenure, follow breadth, comment vs lurk, early vs late rater
- **C. Prediction** — hype accuracy (folded in), hot take rate, predicted-winner accuracy, sweet spot
- **D. Identity arc** — tenure, DNA shift over time, discovery diversity, cross-promo openness

- **Tier 1** ships now (existing signals): ~12 traits.
- **Tier 2** unlocks with AI enrichment Phase 1 cron: style affinity, stakes affinity, calibration-by-stakes.
- **Tier 3** needs post-fight scrape: back-and-forth affinity, pace, finish quality, round-arc, surprise factor.
- **Tier 4** needs longitudinal time: DNA shift, year-over-year, Hype Wrapped fuel.

### Tone (Worms-style)

Tonal reference locked: **Worms (the video game)** — dry, absurd, affectionate, theatrical understatement. The narrator finds the user amusing the way a witty butler finds them amusing, not the way a sarcastic friend roasts them.

Tone rules (load-bearing):
- Affectionate, not diminishing. User is delightful; the *data* is the joke.
- Absurd metaphors > insults. *"The bar is somewhere above the moon"* > *"the community thinks you're broken."*
- Specific imagery > judgment. Numbers in setup are the comedic engine.
- **Banned words** (land as verdicts on the user): sicko, broken, cooked, joyless, obsessed, addict, problem, weird, twisted, embarrassing.
- Never punching down at fighters or promotions.
- Two-beat structure: setup (fact) + punchline (wry observation).
- "We" narrator voice has personality.

Soft tone is baseline (~60%), humor is heavy and first-class (~40%). Humor variants iterate fastest, so copy lives as data not code.

### Emotional design principles

Eight principles documented in chat transcript — the *why* behind the tone rules:
1. Be noticed, not surveilled
2. The number IS the joke
3. Surprise without strangeness
4. Validate the weird without flagging it
5. User is the hero of the joke, not the butt
6. Earned wit, not constant performance
7. Negative space is generous (not every action gets a line)
8. Describe behaviour, never assign identity (*"You tend to love wars"* not *"You're a war fan"*)

### Reveal modal integration

The hype/rating reveal modals that shipped 2026-05-15 get a **third beat**: a contextual Fan DNA line tied to the action just taken. Sequence: community chart → comparison → DNA observation → close.

Architectural implication: trait registry must support **two query modes** from day one — `batchCompute` (nightly) and `eventEvaluate({ user, action, fightId, value })` (real-time). Retrofitting event-mode later is painful.

This is the primary user entry point to Fan DNA. Users learn the feature exists by being shown 30+ DNA lines on fight night before they ever tap the profile section.

### Recommendation surfaces inside Fan DNA

- **Fights you might love — UPCOMING is primary view** (rate-it-after closure-loop hook). Past unrated fights = secondary tab. Each rec cites the driving trait: *"Because you tend to love striking matchups."*
- **Fighters you might love** — same pattern; doubles as a follow-growth surface tied to the acquisition narrative.
- **Cross-promotional lanes** are first-class — *"Fighters you'd like from PFL"*, *"Bellator alumni now in ONE"*, etc. Scorer weights *promotion novelty* as a factor. This is load-bearing for the follow-fighter dataset's acquisition value.

### Three-layer freshness strategy

The big design problem: how does the system stay surprising indefinitely without repeating?
- **Layer 1 (~60%)** — canned pool with combinatorial templates, 30-day cooldown per exact line per user.
- **Layer 2 (~30%)** — Claude Haiku 4.5 generates 3 candidates per surfacing, tone-filter picks one, fallback to Layer 1 on quality failure. ~$1/mo at expected volume. Always fresh.
- **Layer 3 (~10%, priority-bumped)** — nightly fun-fact scanner finds surprising patterns (streaks, anniversaries, contradictions, milestones), hand-curated copy per pattern.

Same-trait pacing (corrected from earlier "no same trait twice per session" rule): repeats are fine and realistic (5 KO ratings → multiple finish-affinity mentions is honest). Rules: no back-to-back fires of the same trait, soft cap ~40% per session for any single trait.

### Toggle-storm / harvest contingency

Detection signal — `(userId, fightId, actionType)` tuples, NOT raw action velocity:
- **Toggle storm** (fires meta response): same user × same fight × same action type, 5+ value changes in 10min.
- **Card sweep** (normal DNA fires): different fights, fast pace. Even 12 ratings in 5 min during a live card is engaged-fan behavior.

Response tiers:
1. 5+ toggles → META line from dedicated pool (*"You've changed your mind six times. We're enjoying watching this."*).
2. 10+ toggles → EXIT meta + quiet that (user, fight) for 1 hour. The user gets a wink, not a stutter; the system never visibly runs dry.

Bonus: persistent toggle-prone users earn a real trait (*"You revise your scores more than the average. Considered. We respect it."*). Every pattern, including this one, is its own coherent fandom.

### Resilience architecture (mandatory)

The system must hold up at trait breadth. Design constraints:
1. **Trait registry, file-per-trait, auto-discovery.** Each trait = one folder under `packages/backend/src/services/fanDNA/traits/{trait-id}/` containing `trait.ts` + `copy.json` + `fixtures.test.ts`. Registry auto-discovers at boot.
2. **Copy as data.** Soft + humor variants in `copy.json`, hot-reloadable.
3. **Trait independence.** One trait throwing must not break the screen.
4. **Versioning per trait.** Computed values stored with `{ traitId, version, computedAt, value, confidence }`. Bumping a version triggers backfill.
5. **Backfill cron**, not manual reruns.
6. **Surface independence.** A trait renders the same regardless of surface (profile card, full screen, recommendation reason, share image, weekly email).
7. **Health dashboard** at `/admin/fan-dna` — firings/week, dwell, complaint rate per trait. Dead traits surface themselves.
8. **Sunset path** — `deprecated: true` stops new computation without deleting historical values.
9. **Fun-facts engine** sits alongside trait registry — same pattern, lower bar.
10. **Test harness** — each trait has synthetic-user fixtures.

### Five-phase build plan (2-3 months total)

- **Phase 1 (~15-20 hr):** Schema + registry + 3 Tier 1 traits + reveal modal integration. **Layer 1 (canned) only.** Real working feature in production.
- **Phase 2 (~15 hr):** Layer 2 LLM + tone filter + 3 more traits + toggle-storm contingency.
- **Phase 3 (~20 hr):** Profile section + full-screen + Layer 3 fun-fact engine + 5 more traits.
- **Phase 4 (~20 hr):** Recommendation engine + cross-promo + Tier 2 traits (post AI enrichment cron).
- **Phase 5 (ongoing):** Quarterly trait additions per maintenance schedule.

Safety: every Fan DNA piece is **additive** — new tables, new endpoints, new UI. Existing flows untouched. Reveal-modal DNA beat is non-blocking (API failure → reveal renders without beat, no user-facing breakage). Trait engine isolated from core fight/rating logic.

## App-wide humorous tone audit (new sub-workstream)

The witty butler voice is a **brand voice**, not a Fan DNA feature. Create `docs/brand/voice.md` as single source of truth for tonal rules. Then walk the app screen-by-screen.

High-value surfaces: onboarding suggested-fighters modal, empty states, error states, login/register, permission requests, push notifications (with restraint), email subject lines.

Surfaces to leave neutral: payment/billing, failed-rating errors, legal/privacy copy.

Effort: 5-10 hr. Lower-risk than Fan DNA — static strings, no backend. Slot-in between Fan DNA phases.

## Maintenance doc created

New `docs/operations/maintenance.md` — source of truth for all recurring/ongoing ops tasks. Seeded with 13 tasks across Daily / Weekly / Monthly / Quarterly / As-needed cadences. Covers Fan DNA Layer 2 sample review, marketing attribution review, Hype Index posting, AI enrichment cost audit, scraper health, sector swell monitor, app store reviews, quarterly trait additions, humor pool refresh, dependency updates, EAS version review, pre/post-event marketing.

Added rule to `CLAUDE.md` Important Rules: **any new recurring maintenance task surfaced in any session must be appended to this doc.** One-offs stay in daily docs or area docs. Memory entry `reference_maintenance_doc.md` added so the rule persists across sessions.

## Naming + framing decisions

- **"Hype DNA" → "Fan DNA"** (renamed across triage note; sweep across `docs/areas/rewarding-users.md`, `docs/areas/ai-enrichment.md`, `docs/HANDOFF-ai-enrichment-2026-05-15.md`, `docs/feature-inventory.md` flagged for next workstream session).
- Confirm exact name with Mike before sweep — working candidate is Fan DNA, alternatives floated: Fight DNA, Your Profile, Fan Print.

## Sign-off

Mike signed off on the full Fan DNA plan + phased build approach. Closing the session without code changes. Next time Rewarding Users or AI Enrichment workstream opens, the triage note at the top of `CLAUDE.md` is the entry point — promote the relevant pieces into the area docs and start Phase 1 implementation.

## What changed in the repo

- `CLAUDE.md` — added "Next session — triage" section (large), added "Log new ongoing/maintenance tasks" rule to Important Rules.
- `docs/operations/maintenance.md` — NEW file, seeded with 13 recurring tasks.
- Memory: `reference_maintenance_doc.md` added, `MEMORY.md` index updated under new `## Operations` section.

No code or app behavior changed today. Pure planning session.

---

## Late-session shipment — push permission nudge

After the planning conversation closed, Mike asked an ops question that turned into a small ship: how many users had notifications set for fights tonight, and whether duplicate notifications could fire.

### Findings from prod query

- 10 distinct users × 37 active `FightNotificationMatch` rows across 4 events tonight (MVP Rousey vs. Carano, UFC Allen vs Costa, Top Rank Davis II, OKTAGON 88 — already LIVE with zero matches).
- MVP card dominates: 6 of 7 real users have at least one MVP match. cbuddscott is the heaviest real user (8 matches).
- **EvilHead has `notificationsEnabled=true` but `pushToken=NO`** — 2 fighter-follow matches that will silently fire to nobody. This is the failure mode the nudge below was built to fix.

### Dedupe verification

Walkout + section-start notifications already dedupe per-user at the send layer (`validUsers.map(u => u.id)` in both `notifyFightStartViaRules` and `notifyEventSectionStart`). cbuddscott's 3 fights tonight that each have a Manual Fight Follow + Fighter Follow row both pointing at the same fight will still collapse to one push each. **No code change needed.** Worth re-checking this invariant when a new notification lane (day-before digest, per-fight result lane, etc.) is added.

### What shipped (commit `a6afc9b`)

- New helper `ensurePushPermissionAfterAction()` in `packages/mobile/services/notificationService.ts`. Checks OS permission after a follow action succeeds; if denied, shows a one-time soft Alert ("We'll ping you 15 min before [Nate Diaz] walks out — if notifications are on. Enable?"); on tap-yes calls `requestPermissionsAsync()` or deep-links to Settings when `canAskAgain=false`; writes `@gf/lastNotifPromptAt` to AsyncStorage on dismissal for a 14-day cooldown. In-flight lock collapses concurrent calls.
- Wired to 4 call sites covering 6 surfaces total: `FollowFighterButton` (which the three reveal modals — Rate, Upcoming, Completed — all use), `app/fighter/[id].tsx`, `app/followed-fighters.tsx`, `LiveFightCard.tsx` (fight follow). Unfollow transitions are skipped.
- Anti-spam guarantees: only fires on intentional notification-setting actions (never on app open, login, browsing, rating), at most once per 14 days per install, in-flight lock prevents concurrent prompts.

### Pipeline

- Committed `a6afc9b` and pushed `main` → also carried `f050c64` (AI preview render) with it. AI preview was previously **PAUSED** per the earlier triage note pending phrasing/placement refinements — but Mike confirmed shipping it via the EAS update question, so it rides along intentionally.
- `eas update --branch production` published for both runtimes (iOS 2.0.1, Android 1.0.0). Activates after 2 app restarts.
- Render backend auto-deploys from main — the `f050c64` backend `select` fix for `/api/events?includeFights=true` will ship with it.

### Open follow-ups

- AI one-liner phrasing rules (broadcaster-lead avoidance, opener variety) and detail-screen placement decision are still unresolved — `f050c64` is now in prod, so subsequent edits will be live-traffic visible. Address before the next re-extract pass.
- After the OTA reaches enough users, query `users WHERE notificationsEnabled=true AND pushToken IS NULL` over a few days to confirm the nudge is converting denied/blank-token users to granted (EvilHead is the obvious starting test case).

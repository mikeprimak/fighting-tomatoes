# 2026-06-24

## Backlog: added 4 strategic items + converted the card-placement article into an SEO blog draft

Remote-control session. Mike asked to continue the BACKLOG (`docs/BACKLOG.md`) work and to add four
new items. Added all four, then acted on item 4 (the article).

### BACKLOG.md additions

1. **SEO play is now a strategic direction (§9).** Added a prominent note at the top of §9: we are
   going HARD on SEO-targeted blog articles to cumulatively build organic web traffic and compound
   the asset's value over time. Every editorial item should be evaluated through that lens (search
   demand, on-page SEO, hub+cluster internal linking, structured data, cadence). Mechanics live in
   `docs/playbooks/seo-fight-preview-cluster.md`; track organic visitors/mo in the admin Acquisition
   tab.
2. **Shareable card from post-hype / post-rate modal (§7, virality play).** Convert the existing
   post-hype / post-rating modals into a branded shareable card ("I'm hyped for this fight: 8/10.
   Backing [Fighter]. '[comment]'") with a native share CTA (text chats, IG, X). Mobile uses the
   native share sheet (render card to image). **Web parity noted: the web app does NOT have post-hype
   / post-rate modals yet**, so web needs those built first, then the shareable card + Web Share API.
   Each card carries brand watermark + deep link back to the fight (drives installs / web traffic).
3. **Affiliate links on ALL "How to Watch" links (§15) is now an active revenue play.** Re-scoped the
   existing §15 item from `⚪ idea` to `🟢 ready` and flagged it STRATEGIC: we're going to start
   making money as an affiliate. Wrap every broadcaster / "How to Watch" link (mobile + web +
   editorial) in an affiliate URL where a program exists (DAZN, ESPN+, Sky, FITE, Amazon, etc.) with
   UTM + click tracking; centralize via a single `channelId → affiliate template` map.
4. **Continue "Placement on the Card Rankings" article (§9).** Added as a live editorial item, then
   actioned it (below).

### Item 4 — card-placement article → SEO-optimized blog draft

Took the canonical candidate `docs/marketing/card-placement-rankings-article-v3-ppv-split.md`
(avg-card-slot scale, PPV vs Fight Night split, activity gate, em-dash-free; finished 2026-06-23) and
converted it into a real blog post **draft**:

- **New file:** `packages/web/src/content/posts/2026-06-24-ufc-card-placement-rankings.md`.
- **SEO frontmatter:** title "UFC Card Placement Rankings: Who the UFC Books Highest", slug
  `ufc-card-placement-rankings`, a keyword-rich 319-char excerpt (→ meta description + OG/Twitter),
  tags `[ufc, rankings, card-placement, pound-for-pound, feature]`. The blog system already emits
  `<title>`, meta description, canonical, OpenGraph, Twitter card, and BlogPosting + BreadcrumbList
  JSON-LD from these fields (`blog/[slug]/page.tsx`), so good frontmatter = good SEO.
- **Hub+cluster internal links (the SEO play):** wove 7 internal links to existing fighter posts at
  natural prose points — Topuria, Max Holloway, Gaethje, Pereira, Conor McGregor, Sean O'Malley,
  Josh Hokit. This cross-links the back catalog (compounding internal-link equity) exactly per the
  new §9 direction.
- **Removed the in-body H1** (title renders from frontmatter, matching every other post).
- **`draft: true`** — so it does NOT appear on the public `/blog` list or in prod `getPost`
  (`includeDrafts = NODE_ENV !== 'production'`). It is reviewable in the **admin Blog > Drafts**
  viewer / `/blog-preview/ufc-card-placement-rankings` (which calls `getPost(slug, {includeDrafts:
  true})`) once deployed.

**Verification:**
- `gray-matter` parses the frontmatter cleanly (title/slug/tags/excerpt all correct; 7 `/blog/`
  internal links).
- `syncBlogPosts.js` runs (21 posts now → backend `src/content/posts` copy for `/api/editorial`).
- Em-dash sweep: **0** (`[[feedback_blog_no_ai_tells]]`).
- No `## ...?` headings, so no accidental FAQPage structured data.

**Open / next session:**
- **Custom hero image.** `image: ""` falls back to the Good Fights logo (`/good-fights-logo.png`).
  Worth a dedicated card-placement hero graphic before publishing (good for OG/Twitter share cards).
- **Canonical-file cleanup (Mike's call).** Publishing v3 as the blog draft effectively makes it
  canonical. The two redundant working drafts (`card-placement-rankings-article-v2-position.md` and
  the single-list "editing version" `card-placement-rankings-article.md`) can be deleted once Mike
  confirms. Left them in place this session.
- **Publish step** (Mike-gated): set `draft: false` + place the slug in SystemConfig `blog_highlights`
  (admin.html Blog tab) when ready to go live + feature it.

**PUBLISHED LIVE (Mike's call).** Flipped `draft: false` and pushed to `main` → Vercel auto-deploys
to goodfights.app. The post is live + indexable at `/blog/ufc-card-placement-rankings` (in sitemap +
RSS). Set `hideFromHome: true` so it does NOT take over the homepage EditorialHero band with the
logo placeholder; it stays off the homepage until Mike adds a hero image and decides to feature it.
Mike will edit + add the image afterward. To feature on the homepage later: add a real `image:`, set
`hideFromHome: false` (and/or `featured: true`), or add the slug to SystemConfig `blog_highlights`
(admin.html Blog tab).

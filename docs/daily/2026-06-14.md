# 2026-06-14

## Live-tracking incident: lifecycle failsafe completed a UFC event mid-card

**Symptom (reported during UFC Freedom 250, live):** event + all 7 fights showed
COMPLETED while only the first 2 had actually finished. Mike corrected it in the
admin panel; the lifecycle re-clobbered the fix within ~5 min (event came back as
`completionMethod: lifecycle-auto`).

**Root cause — `eventLifecycle.ts` Step 3 (LIVE→COMPLETED):** for tracker-backed
events the soft duration estimate is `min(numFights*30min + 60min, 10h)`, anchored
on `getStartTime()` = the *earliest* section start time. UFC Freedom 250 had a
spurious `prelimStartTime` of 21:00Z but **zero prelim fights** (all 7 cardType
"Main Card", real start = `mainStartTime` 00:00Z). So the 4.5h estimate elapsed at
01:30Z — 1.5h into the main card, 2 of 7 fights done — and the failsafe force-
completed the event, overriding the live tracker. Once it fires, the tracker's
no-downgrade rule makes COMPLETED stick.

**Fix (shipped to `main` → Render, commit 77311011):** the soft estimate may no
longer force-complete a *tracker-backed* event that still has UPCOMING/LIVE fights.
The live tracker owns fight-by-fight completion; if the estimate elapses with fights
still pending, defer to the 10h hard cap instead of closing the broadcast. The soft
estimate still promptly flips the event to COMPLETED once all fights are done
(cleanup when the tracker dies after the last fight). Non-tracker events unchanged.

**Live mitigation:** restored event→LIVE + the 5 wrongly-completed fights→UPCOMING
(left the 2 genuine COMPLETED). Nulled the spurious `prelimStartTime` so even the
*old* code's estimate moved out to 04:30Z, covering the deploy window.

**Note:** this is the failsafe misfiring, not the UFC live tracker itself — the
tracker correctly marked the first 2 fights. The general lesson: Step 3 is a
cleanup net for abandoned events, and must never race a live, tracker-backed card.

## VPS tracker confirmed healthy (the tracker itself was fine)

Confirmed UFC Freedom 250 is live-tracked by the **Hetzner VPS** (`scraperService.ts`,
~30s poll), NOT GitHub Actions — the `ufc-live-tracker.yml` GH runs are stale
(March/April 2026). Evidence: VPS `/health` (`http://178.156.231.241:3009/health`)
= up 24.7h, tracking 1 event; and fights #5/#6/#7 carried write timestamps
(02:06–02:10Z) authored by the VPS, not by me (my last manual write was the 01:54Z
restore). A manual `runUFCLiveTracker.ts` pass scraped the real ufc.com page
correctly (Ruffy/Chandler LIVE, 2 complete, 4 upcoming) and made 0 changes because
the DB already matched. **Lesson banked:** UFC/PFL/BKFC/ONE FC track on the VPS, not
GH Actions — don't check GH workflow runs to judge whether tracking is live
(memory `lesson_live_trackers_run_on_vps_not_gh_actions`).

## NEXT SESSION — finish the resilience hardening (deferred)

The shipped fix closes the hole, but Mike's expectation ("I had the correct 8pm
start time, the system should've been resilient") only fully holds after these.
Root of the bug: `getStartTime()` picked the **earliest** section time — a spurious
`prelimStartTime` of 5pm ET (21:00Z) with **zero prelim fights** — over the correct
8pm ET `mainStartTime`, then a blind duration guess (not the tracker) declared the
event over. Three follow-ups, in priority order:

1. **Ignore phantom section start times.** A `prelimStartTime`/`earlyPrelimStartTime`
   with no fights actually in that section must not anchor lifecycle timing (Step 3
   `getStartTime`, and arguably Step 1 UPCOMING→LIVE + Step 1.7 section notifs).
   Anchor on the earliest section that *has* fights.
2. **Trigger the Step-3 failsafe off tracker silence, not a clock.** For tracker-
   backed events, only force-complete when the live tracker has gone silent (e.g. no
   successful VPS scrape / `lastScrapeAt` heartbeat in ~30 min) — not on a duration
   estimate. The VPS `/status` exposes `lastScrapeAt`/`scrapeCount` per tracker; could
   surface a heartbeat to the backend. This is the real "resilient" version.
3. **Data hygiene at scrape/discovery time.** Don't write a section start time when
   no fight is tagged to that section (prevents the orphan-5pm condition at the source).

Mike asked for #1 and #2 explicitly ("belt-and-suspenders"). #3 is the upstream fix.

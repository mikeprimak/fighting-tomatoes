# 2026-05-18

## Fan DNA personality "types" — SHIPPED

Picked up the top forward path from yesterday's handoff: aggregate the 5+ batch-computed trait values into a single shareable identity label. Highest user-visible payoff per the area-doc ranking because it turns N independent trait cards into one synthesized hero.

### Architecture

New module `packages/backend/src/services/fanDNA/personalityType.ts` lives **outside** the trait registry. Keeping it separate preserves trait independence — each `Trait` stays a single-signal plugin, and the type aggregator reads `TraitValue` rows after the engine has computed them.

Public surface is one function:

```ts
computeUserType(prisma, userId): Promise<UserPersonalityType | null>
```

Wired into `GET /api/fan-dna/profile` — returns `{ personalityType, cards, count }` alongside the existing trait cards. The `/profile` endpoint's staleness pass already lazily recomputes TraitValue rows before the type pass runs, so the type always reflects current data.

### Rule design

Priority-ordered ladder of 11 rules, first match wins:

| Rank | Type | Trigger |
|---|---|---|
| 1 | Hot Take Artist | `hotTakeCount ≥ 3 AND totalHypedFights ≥ 10` |
| 2 | Trailblazer | `firstHypeCount ≥ 5` |
| 3 | The Oracle | `accuracyPct ≥ 60 AND totalHypedFights ≥ 15` |
| 4 | The Skeptic | `rating-bias.n ≥ 15 AND avgDelta ≤ −0.7` |
| 5 | The Generous Critic | `rating-bias.n ≥ 15 AND avgDelta ≥ +0.7` |
| 6 | The Hype Believer | `hype-bias.n ≥ 15 AND avgDelta ≥ +0.7` |
| 7 | The Doubter | `hype-bias.n ≥ 15 AND avgDelta ≤ −0.7` |
| 8 | {Org} Loyalist | non-UFC dominant ≥ 60% AND total ≥ 15 |
| 9 | UFC Mainstay | UFC dominant ≥ 60% AND total ≥ 15 |
| 10 | The Globetrotter | ≥ 4 promotions AND no org ≥ 60% share |
| 11 | The Calibrator | both biases within ±0.3 with n ≥ 15 each |

Returns `null` when no rule clears its floor — hero card stays hidden, trait cards still appear if they fired.

### Two corrections during verification

First probe of top-5 raters showed 4/5 collapsing to "Trailblazer" because legacy ratings were bulk-imported and `firstRateCount` is dishonest signal for legacy users (cookmeyer had 709 firsts out of 1635 ratings, qegjkirljzy had 901/2384 — both clearly migration artifacts). This is the existing `lesson_dataset_aggregates_dishonest` memory in action.

**Fix 1:** Trailblazer rule now gates on `firstHypeCount` only. Hypes happen pre-fight and weren't migrated en masse, so they're a truthful "you saw it coming" signal. Renamed primary stat from "firsts" to "first hypes" so the copy matches the metric.

Second issue: Globetrotter rule was guarded by `if (ratingDominant) return null`, but org-affinity sets `ratingDominant` once any org passes 40%. A 48%-UFC user with 10 promotions is a Globetrotter, not "ufc-but-not-quite-dominant" with no type at all. Tested case: cry.hagen@mailbox.org (10 orgs, UFC at 48%).

**Fix 2:** Globetrotter now checks `dominantPct < 60%` instead of `!dominant`. The 40-59% band gets the diverse-fan identity.

Also added `totalHypedFights ≥ 10` to Hot Take Artist so thin-data users can't backflip into it by 3 lucky hot takes among 5 hypes.

### Distribution check (15 users)

| Type | Count |
|---|---|
| hot-take-artist | 10 (top hypers, including avocadomike with 14 hot takes / 37 hypes) |
| ufc-mainstay | 4 (legacy raters, UFC ≥97%) |
| skeptic | 1 |

Healthy spread. Seed users (`seed-user-01@goodfights.app` through `06`) all hit Hot Take Artist because the old hype-seeding regime gave them volume — that's fine, they're internal accounts. Real users like avocadomike, brovak02, ethankeeping correctly earn Hot Take Artist on genuine signal. Mid-tier raters split between UFC Mainstay and Skeptic.

No "everyone gets the same label" collapse, which was the failure mode the rule tightening was meant to prevent.

### Mobile UI

Two surfaces — both render the hero card with the same shape:

**Profile section** (`(tabs)/profile.tsx`): purple-tinted box at the top of the Fan DNA SectionContainer, above the two compact trait rows. "YOUR TYPE" eyebrow + large label + body sentence + primary stat callout. Tapping the card opens the full-screen page (same affordance as "See All").

**Full-screen page** (`activity/fan-dna.tsx`): same shape, larger scale, sits between the dna-icon hero block and the trait card list.

Section now also renders when only a personality type exists (no fireable trait cards), which can happen for users who hit a type via thin data on one trait.

### Backend type-check

- `tsc --project tsconfig.production.json --noEmit`: clean.

### Shipped

- Commit `8309809` pushed to main at 14:32 UTC. Backend auto-deploys to Render.
- OTA via `eas update --branch production`:
  - iOS runtime 2.0.1: update group `8097e28a-f35a-4f1a-8dc8-753214c7ad5d`
  - Android runtime 1.0.0: update group `62bcd67f-1a64-46fa-a958-562c8470cce9`
- Two app restarts to apply for users on existing builds.

### AI enrichment cron — observed late, not failed

Today's 14:00 UTC enrichment cron hadn't fired as of 14:25 UTC. Last successful run was 2026-05-17 17:38 (the manual re-enrichment after the prompt rewrite). GitHub Actions cron routinely slips 15-30 minutes under load — not surprising and not a failure mode. Mike can confirm via the admin widget once it does land; if it's still missing at 15:30 UTC, that's the signal to investigate.

The admin widget shipped 2026-05-17 (`/api/admin/health/enrichment` + ops-tab section) is the actionable observability — "Last enrichment timestamp" is the canary.

### Subtle decisions logged

- **Personality type lives outside the trait registry.** Traits are independent single-signal plugins; type is a cross-trait synthesizer. Keeping it in a separate module preserves the engine invariants (one trait can't break another, registry auto-discovery, etc.).
- **First match wins, no scoring.** The priority order encodes specificity (Hot Take Artist before generic UFC Mainstay). A scoring system would let multiple types blend, but the requested UX is one identity per user.
- **No secondary type yet.** First-cut ships the primary label only. "With shades of {secondary}" copy line is a future iteration once Mike sees the v1 land.
- **`hasFloor` gates rule access.** The `get<T>` helper in `personalityType.ts` returns `null` for any trait whose `TraitValue.hasFloor` is false. Rules can't accidentally read thin-data values — they have to opt in by checking explicit thresholds.
- **Trailblazer rule body uses "first to hype" not "first to weigh in".** Yesterday's commit `8d48e6f` already moved to plain English ("first to rate", "first to hype"); the type body copy matches that vocabulary instead of the older `secondaryStat: 'first-ever'`.

### Open follow-ups

- Probe scripts (`probe-personality-type.ts`, `probe-personality-wider.ts`) committed alongside the code. They're QA tools, not production code — fine to keep for future tuning.
- Nightly batchCompute cron still queued from yesterday's daily log. Personality type makes this slightly more attractive — first profile load can now trigger 5 sequential batchComputes AND the type rollup, but in practice the latency is invisible.
- More tag-aware traits queued: weight-class affinity, main-event watcher, method-affinity. ~45 min each.
- Recommendation engine (Phase 4) — "Fights you might love" / "Fighters you might love" — would consume the personality type as one of its features. Bigger build.

### Pick up next session

Two reasonable forward paths:
1. **More tag-aware traits** — weight-class affinity, main-event watcher, method-affinity. Each is ~45 min and they accumulate without anywhere else to spend before personality types refine on more data.
2. **AI enrichment Phase 2.B render targets** — stakes-bullets card in the hype modal (uses `aiTags.stakes[]`), web SEO `aiPreview` block. Pulls the recent prompt-rewrite data through to user-visible surfaces.
3. **Polish + un-hide Hot Takes list** — see below; needs row layout/copy pass before flipping the flag.

Phase 4 recommendation engine is the biggest unbuilt thing left, but it's also the highest-cost first step (needs a new RecommendationCandidate concept, ranking surface, etc.). Defer until the type concept lands and we have read on whether the data quality justifies a recommender.

## Fan DNA — Mike's tested-it review

Mike OTA'd the personality-types build, confirmed:
- Profile section hero card: **looks good**.
- Full-screen `/activity/fan-dna` page: hero card good, trait card list good.
- Bottom "Hot Takes" list (per-fight rows pulling from `/profile/hype-accuracy`): **needs work**. Row layout/copy not ready for users.

### Hot Takes list — hidden behind flag (commit `ea6701e`)

Added `SHOW_HOT_TAKES_LIST = false` at the top of `packages/mobile/app/activity/fan-dna.tsx`. The block is gated on the flag; everything else (data fetch, styles, route logic) is untouched so re-enabling is a one-character flip.

The `hypeAccuracyQuery` still runs in the background even though its data is hidden — kept as-is because (a) it's a small payload, (b) flipping the flag back on requires zero other changes, (c) the count is still needed if/when we surface a "N hot takes" summary stat elsewhere.

OTA shipped to both runtimes:
- iOS: update group `c70d72b7-292d-45cb-8775-22b6a46c0977`
- Android: update group `a0ed8b3e-459e-4c7f-b4a9-aa947da383be`

### Open work on the Hot Takes list (when picked back up)

The list shows: fight name → event → user's hype number (colored by heatmap) → "avg X.X" community rating. What's not working visually wasn't diagnosed precisely — Mike's signal was "needs some work" at the row level. Investigate:
- Row density / vertical rhythm — current `paddingVertical: 8` may be too tight given the two-line fight+event copy.
- The "avg X.X" secondary stat — yesterday's profile copy rewrite (`8d48e6f`) replaced "room" with "avg" on the trait cards; the Hot Takes row was updated to match but the typography hierarchy (large hype number vs small avg) may be over-loud against the editorial vibe.
- The Hot Takes block header sits at `marginTop: 28` from the last trait card — feels visually disconnected from the hero. Consider tighter spacing or a separator instead of a margin gap.
- Empty state — the list only renders when `hotTakes.length > 0`. Users with no hot takes get nothing; the page just ends at the trait list. Either keep this (clean) or add a one-line "your hot takes will appear here as the community catches up" promise (cheap).

The data path itself is solid — `/profile/hype-accuracy` works, the trait's new contrarian-right definition fires correctly (verified 2026-05-17). The work is purely on the rendering surface in `fan-dna.tsx` lines 152-211 once the flag is flipped.

## Fan DNA — three new affinity traits (SHIPPED)

Phase 2 trait expansion (option from yesterday's pickup notes). All three are batch-computed profile traits — no reveal-modal copy work, they surface as cards on the Fan DNA section + full-screen page via the existing `profileSummary` contract.

### `weight-class-affinity`

`packages/backend/src/services/fanDNA/traits/weight-class-affinity/{trait.ts,copy.ts}`. Mirrors org-affinity: aggregates ratings + hypes by `Fight.weightClass`, splits into two cards (ratings · hypes). Three eventEvaluate modes (class-first / cross-class-foray / dominant-milestone) reuse the same surface-discipline as org-affinity — only fires on first-evers, foray, or milestones; silent on routine taps.

Dominant threshold 20% (vs org-affinity's 40%) because weight classes spread thinner — uniform across 14 MMA classes is ~7%, so 20% is ~3x uniform.

### `main-event-watcher`

`packages/backend/src/services/fanDNA/traits/main-event-watcher/{trait.ts,copy.ts}`. Single-dimension: % of user's ratings/hypes on `orderOnCard === 1` fights. Fires "Main event only" (≥60%) for selective viewers, "Full-card watcher" (≤25% with ≥20 signals) for users who rate prelims too. Ratings-axis and hype-axis emit separate cards.

Event-side stays silent — profile-only trait. Empty `lines: {}` in copy.ts is intentional (profileSummary returns its own headline + body).

### `method-affinity`

`packages/backend/src/services/fanDNA/traits/method-affinity/{trait.ts,copy.ts}`. Rating-only taste signal (no hype dimension — method is unknown pre-fight). Buckets `Fight.method` into Knockout (KO + TKO), Submission, Decision via ILIKE patterns that handle the messy method strings ("TKO (punches)", "Decision (unanimous) (30-27)", "U-DEC", "SUB", "KO/TKO").

Computes per-bucket avg vs personal baseline. Surfaces the method with highest positive delta (≥0.5) and, separately, the strongest negative delta. Floor: 10 ratings on the candidate method AND 25 total method-bucketed ratings.

### One bug caught during probe

First version used Postgres word-boundary anchors (`\m`, `\M`) for method matching. Prisma template literals don't preserve those escapes intact (`\m` reached Postgres as something the regex engine didn't honor), so the Knockout bucket returned zero rows. Switched to ILIKE patterns — direct prefix/suffix matching, no regex escaping at all.

### Probe results (8 top raters + 5 top hypers)

`packages/backend/scripts/probe-new-affinity-traits.ts` — runs batchCompute for each trait, prints the resulting profile cards.

- **weight-class-affinity**: 5/8 users hit floor; 2/8 emit a card (Middleweight 23%, Strawweight 25%). Three hit floor but no class crossed 20% — diverse-class viewers correctly get null.
- **main-event-watcher**: 8/8 fire "Full-card watcher" at 6-14% main events. Every high-volume rater is a full-card watcher by definition — they touch many prelims per event. The "Main event only" path is rare; not in this sample.
- **method-affinity**: 6/8 fire "Knockout fan" + "Decision sceptic" together. The pattern across users is unusually consistent — knockouts +0.6 to +0.9 above baseline, decisions -0.5 to -0.9 below.

### Why method-affinity looks like wallpaper (data note)

Mike flagged mid-session: weight-class and method-on-fight data are recently collected — older fights in the DB don't have these populated. The probe is reading the SUBSET of fights where method is non-null, which skews recent (UFC PPVs etc.) where finishes consistently rate higher than decisions. So the "Knockout fan + Decision sceptic" wallpaper is an artifact of which fights HAVE method, not user preference.

The trait code is correct; the signal will normalize as the enrichment cron + scrapers backfill method onto more historical fights. Same logic applies to weight-class-affinity firing thin on legacy-heavy users.

No code changes needed here — the trait floors (5 weight-class signals, 25 method-bucketed ratings) already gate out thin-data noise. The wallpaper effect resolves itself as data fills in.

### Type check + ship

- `tsc --project tsconfig.production.json --noEmit`: clean.
- Commit `5ceaa86` pushed to main 18:xx local. Backend auto-deploys to Render. Mobile doesn't need OTA — these surface through the existing Phase 3 `/api/fan-dna/profile` endpoint and `app/(tabs)/profile.tsx` + `app/activity/fan-dna.tsx`.

### Open follow-ups

- **Personality type ladder doesn't read these new traits yet.** The 11-rule type aggregator at `services/fanDNA/personalityType.ts` was built before these three landed. Could add rules like "Decision Sceptic" type, "Strawweight Specialist", "Full-Card Hero". Defer until usage data shows whether the new traits' cards are valued — then layer them into the type rules.
- **Diverse-class watcher card.** Considered + rejected. Most heavy raters spread across many classes naturally — a "you watch every division" card would be wallpaper. Org-affinity has a cross-promotion-fan card because spreading across orgs is unusual; spreading across weight classes is the default.
- **Method-affinity vs population baseline.** Current implementation compares to personal baseline. A user at +0.6 on knockouts isn't unique because the population average is also ~+0.6. Future iteration: compare against population baseline so the "Knockout fan" card only fires for users who DEVIATE from norm. Cheap once a daily aggregate exists. Skipped for v1.
- **Hot Takes list polish** still pending (flag-gated since `ea6701e`).

### Pick up next session

Three forward paths remaining from yesterday's notes plus today's:

1. **AI enrichment Phase 2.B render targets** — stakes-bullets card in hype modal, web SEO `aiPreview` block. Bridges enrichment + rewards workstreams into user-visible value.
2. **Hot Takes list polish** — un-hide the flag in `fan-dna.tsx` once row layout + copy is fixed.
3. **More tag-aware traits** — card-section affinity (`Fight.cardType`), title-fight aficionado (`Fight.isTitle`), late-decision watcher (`Fight.round`). Each ~30-45 min.

Personality type rule additions are a small follow-on if the new cards land well with users.

## AI enrichment Phase 2.B — stakes-bullets in the hype modal (SHIPPED)

First user-visible AI enrichment render outside the Fan DNA reveal-modal line. The hype modal already shows `aiPreviewShort` (a narrative one-liner). Added a bullet list below it rendering `aiTags.stakes[]` — what's on the line + what each fighter wants.

### Architecture

The data path was free. `/api/events` list endpoint already includes `aiTags` in the select (`routes/index.ts:390`), so the mobile change is local to the modal — no backend changes, no new endpoints.

`UpcomingFightModal.tsx` adds a second self-contained IIFE block below the existing `aiPreviewShort` block. Same gating (`aiConfidence ≥ 0.5`), additional check that stakes is a non-empty string array. Filters out any non-string or whitespace-only entries (defensive against LLM output drift).

### What renders

For a fight like PFL's Jean vs Musaev (real production data):

```
[Thad Jean]  vs  [Shamil Musaev]

Undefeated Thad Jean (11-0) wins the vacant PFL welterweight title
against proven contender Musaev in a championship bout.

• PFL Welterweight World Championship
• Jean seeks to cap 2025 tournament victory with gold
• Musaev chasing his first PFL title after Feb loss to Kuramagomedov

[Hype flame wheel]
```

The bullets break out each fighter's narrative angle separately — richer than the one-liner alone, lighter than the full storylines array. Mike's mental model: "Francis Ngannou looks to defend his championship while Felipe looks to establish his name as a new champion" — that's the exact phrasing pattern the LLM is already producing in `stakes[]`.

### Subtle decisions

- **Stakes-only for v1, not stakes + storylines.** Storylines is the other rich array ("Jean won the 2025 PFL tournament...") — backstory rather than what's-on-the-line. Could overflow the modal. Defer to a later iteration if users want more depth — easy to add as an expandable section.
- **No header label.** No "What's at stake" or similar above the bullets. The visual relationship to the one-liner above + the bullet glyphs are enough; a label would feel formal against the editorial vibe.
- **Style: tighter than the one-liner.** Bullets at 12px / line-height 16, vs the italic 13px / 17px one-liner. Hierarchy: one-liner = topic sentence, bullets = supporting points.
- **No truncation in v1.** Some stakes phrases are long ("PFL Welterweight World Championship (vacant following Ramazan Kuramagomedov's retirement)"). Letting them wrap is fine — the modal scrolls. If post-launch usage shows excessive scroll, can truncate at ~80 chars with ellipsis.
- **Coverage: 39% of UPCOMING enriched fights have stakes populated** as of the 2026-05-17 prompt rewrite. For fights without, the bullets section is invisible. No empty-state copy.

### Type check + ship

- `tsc --noEmit` (mobile): clean.
- Commit `d2ca499` pushed to main.
- OTA shipped via `eas update --branch production`:
  - iOS runtime 2.0.1: update group `59e18074-9029-4574-8178-6cd472b4ded6`
  - Android runtime 1.0.0: update group `c64d7701-93af-4e44-be5d-08f1811b1493`

### Pick up next session

The bridge between AI enrichment + Rewarding Users is established at the hype-modal level. Two natural follow-ons:

1. **Web SEO `aiPreview` block.** Same data, different surface — the long-form `aiPreview` field on fight detail pages for SEO. Pure web work, defer until a web session.
2. **Storylines expansion.** If users want more depth than the stakes bullets, expose the storylines array via a "more" affordance. Defer until usage data justifies.
3. **CompletedFightModal stakes** — should the rate-reveal modal show stakes too? Probably yes for post-fight context ("you rated 9 on a title fight"). ~15 min copy of the same block.

Personality type integration still queued behind data fill-in (see `followup_personality_type_new_traits` memory).

## Method-affinity render bug — RESOLVED (not a bug)

Picked up the handoff at `docs/HANDOFF-fan-dna-method-affinity-render-bug-2026-05-18.md`. Sunk an evening into hypotheses — duplicate keys, response shape, registry, cache. Re-verified the live API still returns 10 cards for avocadomike including Knockout fan + Decision sceptic. Shipped a one-off temp on-screen debug `<Text>` dump (commit `5fb3dda`, then reverted in `364138c`) so Mike could see what his app actually receives.

**Root cause**: Mike was logged in as `test@goodfights.app`, which has nowhere near the 25-rating floor `method-affinity` requires. avocadomike@hotmail.com renders all 10 cards correctly. No code bug.

Lesson for next time: confirm the test account BEFORE assuming render bug. The handoff explicitly noted "I never had eyes on his rendered screen" — that was the cheap check we should have done first.

Debug dump removed and OTA'd cleanly. Handoff doc is now historical.

## CompletedFightDetailScreen stakes block — SHIPPED

Picked up "keep going with our user rewarding system and ai enrichment system." Bridges both workstreams.

### Dead end first: title-fight-aficionado trait

Started building a `Fight.isTitle`-based trait mirroring `main-event-watcher`'s shape. Probe killed it before merging:

| Source | Title-fight share |
|---|---|
| Whole DB | 86 / 16899 (0.5%) |
| UFC scraper | 5 / 242 (2.1%) |
| Tapology | 9 / 3656 (0.2%) |
| BKFC, PFL | 0 |
| RAF | 31 / 110 (28%) ← only honest source |

Last-180d records same as all-time: scrapers aren't trending toward better `isTitle` capture, so the trait would never fire. Different problem from weight-class / method (where data fills in as new ratings accumulate). Deleted the trait files + probe scripts. Note for future: a meaningful "title-fight watcher" signal would have to read `aiTags.stakes` containing the literal `"title fight"` token, not the dishonest `Fight.isTitle` column.

### Pivot: mirror upcoming-detail stakes block onto completed-detail

Mike's earlier ee8d7e8 revert decision ("detail screen is the better surface for stakes context") had only been applied to `UpcomingFightDetailScreen`. The completed-fight detail showed nothing — symmetrical gap.

Added the same block to `CompletedFightDetailScreen.tsx` between the Event Info row and the Reactions Section (line 1581). Identical to the upcoming version: `aiPreviewShort` italic centered one-liner + `aiTags.stakes[]` bullet list, gated on `aiConfidence >= 0.5`.

**Why it's safe for spoiler-free mode:** both fields are written by the pre-fight enrichment cron (T-10/T-5/T-2). They describe what was on the line going into the fight, never the outcome. So the block renders regardless of `spoilerFreeMode` — same content the user could have seen before they rated.

**Data path was free:** `/api/fights/:id` uses Prisma `include` (not explicit `select`), so `aiTags`/`aiPreviewShort`/`aiConfidence` already flowed through. No backend changes — matches the `lesson_two_fight_endpoints_different_selects` memory's escape hatch (single-fight endpoint is the easy one).

### Ship

- Commit `6b98ba9`, pushed to main.
- OTA via `eas update --branch production`:
  - iOS runtime 2.0.1: update group `5082cf91-7003-4703-aeea-7f5398ff975d`
  - Android runtime 1.0.0: update group `16a69ea6-62ea-4b8a-8eb0-bda617357925`
- `tsc --noEmit` on mobile clean for the touched block. Pre-existing type errors elsewhere (UpcomingFightDetailScreen, FightDisplayCardNew, FightDetailsMenu) untouched.

### Pick up next session

Forward paths haven't changed from earlier today's notes, minus the dead title-fight branch:

1. **Hot Takes list polish** — flag-gated since `ea6701e`; needs row layout + copy fix.
2. **More tag-aware traits using AI tags, not raw schema columns** — e.g. read `aiTags.stakes` for "title fight"/"main event" tokens. Avoids the `isTitle` data-coverage trap. Same pattern `stakes-aware` already uses for the event-side line; would now extend to a profile-side trait.
3. **Web SEO `aiPreview` block** — defer to a web session.
4. **Phase 4 recommendation engine** — biggest unbuilt thing left in Rewarding Users; consumes the personality type + new affinity traits.

Personality type ladder still queued on whether the three new affinity traits (weight-class, main-event-watcher, method-affinity) and now this surface accumulate enough usage data to warrant new type rules.

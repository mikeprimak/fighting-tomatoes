# 2026-05-25

## Goals for today

1. **Confirm the Broadcast Discovery system is fully up and running.**
2. **Confirm AI enrichment of upcoming fights — and of recent fights — is fully up and running.**
3. **Get the web app to minimum viable product so it can be published.**

## Goal 1 — Broadcast Discovery: CONFIRMED fully up and running ✅

Verified end-to-end on 2026-05-25.

**Weekly cron is healthy.** `.github/workflows/broadcast-discovery.yml` (Mondays
09:00 UTC). Last 3 *scheduled* runs all succeeded, including today's
(2026-05-25). Today's run completed every step: `Run discovery` ✅ +
`Auto-apply high-confidence findings` ✅ (the `Report failure` step correctly
skipped — nothing failed).

**It's producing.** Today's run created **13 new findings**:
- UFC EU PRELIMS → Max / HBO Max / RMC Sport / Discovery+ / Nova (conf 0.92)
- Karate Combat GB Fallback → Premier Sports / Free TV / FreeSports / Box Nations (conf 0.90)
- RAF GB/EU Fallback → YouTube (0.85 / 0.75); MVP GB → Sky Sports (0.75); PFL GB → Channel 4 (0.75)

**Auto-apply ran but applied none of today's high-conf findings — by design.**
The 0.90–0.92 findings are all *new channels* (Max, HBO Max, Discovery+, RMC
Sport, Nova, etc.) with no channel record yet, so they can't be auto-applied
without a channelId. They correctly sit in the inbox awaiting one-click channel
creation. Not a bug — matches the "Create Channel button" design.

**Inbox state:** 24 APPLIED · 51 PENDING · 8 REJECTED · 3 DUPLICATE.
Defaults span **17 promotions** (UFC 18, RIZIN 10, Karate Combat 9, Matchroom 8,
OKTAGON 8, others 4–6). Discovery's verify loop is live too — it touched
`PromotionBroadcastDefault` rows during today's run (PFL GB updatedAt 12:16 UTC).

**Schema confirmed section-aware:** `cardSection` present on
`BroadcastDiscovery`, `PromotionBroadcastDefault`, and event broadcasts.

### Flags / open items (not blockers)
- **51 PENDING is a growing triage backlog.** Needs an admin sweep via
  `/admin.html` → Broadcasts → Pending. Many are new-channel creations.
- **`eventType` (numbered vs Fight Night) never landed on the broadcast side.**
  The `EventType` enum exists but only on the `Event` model — the
  PromotionBroadcastDefault re-tag follow-up from the 2026-05-12 handoff is still
  open. Lower priority; system works without it.

**Verdict: Goal 1 done — system is live, scheduled, and generating reviewed findings.**

## Goal 2 — AI enrichment: UPCOMING is live ✅ / RECENT does not exist ⚠️

### Upcoming-fight enrichment — fully up and running ✅
- Daily cron `.github/workflows/fight-enrichment.yml` (14:00 UTC). Last **7
  scheduled runs all succeeded** (through 2026-05-24). Today's hadn't fired yet
  at check time (runs at 14:00 UTC).
- Pipeline (`services/aiEnrichment/run.ts`) targets `eventStatus: 'UPCOMING'`
  only, with T-10/T-5/T-2 windowing + 36h dedup.
- **Live DB coverage:** 74 upcoming events · 258 upcoming fights · **82 have
  `aiEnrichedAt`** · 53 clear the 0.5 display floor. Most recent enrichment:
  2026-05-24 14:59 UTC.
- Windowing works as designed — soonest events are fully covered (UFC Song vs
  Figueiredo 5/30 = 13/13; Han vs Holm 2 = 10/10), farther-out / thin-editorial
  cards fill in progressively (BKFC 90 2/16, RAF09 6/15). The partial coverage on
  non-UFC cards is the known editorial-source-thinness issue, not a pipeline
  failure.

### Recent-fight (post-fight recap) enrichment — DESIGNED, NOT RUNNING ⚠️
Mike's recollection is correct: we scoped a recurring system that runs a
**T+5-days post-event AI scan** to summarize what happened in each fight. That's
**Phase 6 post-fight enrichment** (`docs/areas/ai-enrichment.md` lines 82–91).

Status:
- ✅ **Schema shipped** (migration `20260518000000_add_ai_post_fight_enrichment_fields`):
  `aiPostFightTags` (JSONB), `aiPostFightSummary` (TEXT), `aiPostFightEnrichedAt`.
- ✅ **2,022 historic fights populated** — but ALL from the one-time hand-curated
  Phase 6.5 campaign (Claude Code as LLM), not from any recurring job.
- ❌ **The recurring T+5d cron was never built.** No
  `.github/workflows/post-fight-enrichment.yml`; `run.ts` is UPCOMING-only;
  the planned `services/aiEnrichment/postFight/` split was never created.

**Live gap (proof it isn't running):** of 29 COMPLETED events in the last 30
days (332 fights), only **18** have a post-fight recap — and those are
historic-campaign overlap, not freshly generated. New events get nothing.

Designed cadence (from the area doc): one pass per fight at **T+5d after
`COMPLETED`**, reusing ~80% of Phase 1 plumbing (DB-as-source, Brave editorial
fetch, UFC/Tapology page fetch, fightId persist, cron orchestrator). LLM schema:
`aiPostFight` = `{ methodNarrative, momentDescription, bonuses[], callouts[],
aftermath, fotyConsideration }`. **Est. ~4–6 hr plumbing + 1–2 hr first surface.**

**Verdict: Goal 2 = HALF done.** Upcoming enrichment is live ✅. The recurring
post-fight recap system is a queued build, not a confirmation — needs to be built.

### → BUILT the recurring post-fight recap system (2026-05-25) ✅
Phase 6 is now live. New files under `packages/backend/src/services/aiEnrichment/postFight/`:
- `extractPostFightEnrichment.ts` — Haiku 4.5 extractor. Card carries the
  authoritative DB result (winner/method/round/time); LLM writes a grounded
  recap (`summary` + `methodNarrative`/`momentDescription`/`bonuses`/`callouts`/
  `aftermath`/`fotyConsideration`) and is forbidden from contradicting the result.
- `enrichOnePostFightEvent.ts` — loads COMPLETED, winner-set, recap-missing
  fights; pulls ufc.com/Tapology/BKFC page + Brave editorial in `mode:'recap'`.
- `persistPostFight.ts` — additive write to `aiPostFight*`, confidence floor 0.5.
- `runPostFight.ts` — selects COMPLETED events in the **T+5d→T+45d** window,
  per-fight dedup, most-recent-first, cap 25.
- `scripts/run-post-fight-enrichment.ts` + `.github/workflows/post-fight-enrichment.yml`
  (daily **16:00 UTC**, offset from Phase 1's 14:00).
- `fetchEditorialPreviews.ts` gained a `mode` option ('preview' default →
  Phase 1 untouched; 'recap' flips the query + filter).

**Deliberate divergence from the doc's plan:** did NOT move Phase 1 files into
`preFight/` or create `shared/` — that refactor risked the live pre-fight cron
for zero functional gain. `postFight/` imports the existing standalone fetchers.

**Verified live:** dry-run + real write on UFC Allen vs. Costa (5/16). Pulled
mmajunkie + sherdog play-by-play; recaps are accurate and result-consistent
(Allen UD R5, Choi TKO R2 body shot + POTN, Diaz RNC R2 4:08). **~$0.025/event.**
6 of 12 fights written on the live pass; the rest stay null and retry next cron
as more editorial lands. Typechecks clean (prod + strict tsconfig).

**Goal 2 now fully satisfied:** upcoming ✅ + recent/post-fight ✅ (running on cron).

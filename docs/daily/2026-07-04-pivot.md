# 2026-07-04 (pivot branch)

Branch `claude/user-focused-pivot-l8l6mg`, worktree `C:/Users/avoca/fight-mobile-app-pivot`.
(Named `-pivot` because main's worktree has its own 2026-07-04 daily log.)

## Voice guide applied throughout + Fan DNA revamp onto the taste engine

Session inputs: `docs/HANDOFF-home-mirror-iteration-2026-07-03.md` +
`Good_Fights_Voice_Guide.docx` (root — Mike's voice doc; its DEAD examples are
literal quotes of our shipped copy, so it doubles as a punch list).

### 1. Taste copy engine rewritten in the voice (`tasteProfile/copy.ts`)

- Every HEADLINES / SUBLINES pool rewritten per the guide: headline makes the
  claim, subline adds proof/consequence/twist (never an echo), enemies in the
  lines ("the guy on the bike" energy), no hedges, no customer-service cheer,
  no app self-reference, no "their" for a named fighter.
- Kill-list fixes in the pools: "You might like {name}" → "Watch {name}" /
  "Your taste already picked {name}"; "rarely disappoint / never let you down /
  go home happy" all gone; "You seem to love" → "{name} never misses for you".
- Latent bug fixed while there: 'The road to your 10s runs through {X}'
  violated the no-digits-in-headline hygiene test (never picked in CI runs so
  far — pickVariety luck). Reworded.
- `tasteProfile.test.ts` passes (kept `{X}` in every token-kind headline
  variant, `{recList}` in fighter-rec sublines, chess-cluster keywords).
- CLUSTER_HEADLINES untouched (already Mike-voiced from the pilot review).

### 2. Fan DNA surfaces rebuilt on the taste engine (new data system)

The frozen `personalityType` ("Your Type: Hot Take Artist") is now removed
from every consumer surface, honoring the locked rotating-signature decision
(2026-06-09). All surfaces share the same daily salt + `max` args so the
identity noun agrees across app and web on any given day.

- **Mobile `app/activity/fan-dna.tsx`** — full rewrite: hero = "THIS WEEK
  YOU'RE A" + rotating identity noun + "Here's what your ratings gave away.";
  body = 12 ranked taste insights (gold-accent cards matching the mirror
  rail); quiet "N fights rated" footer; old trait cards + personality card +
  their queries deleted; hot-takes block still parked behind its flag (its
  query now only fires when the flag is on).
- **Mobile `app/(tabs)/profile.tsx`** — Fan DNA row now shows "This week:
  {identity noun}" (top insight headline as fallback); trait-card state and
  the personalityType fetch removed.
- **Web `lib/api.ts`** — new `getTasteProfile()` + `TasteProfileResponse`
  types (the endpoint existed; web just never called it).
- **Web `/fan-dna` page** — same revamp as mobile: identity-noun hero,
  12 insights, plain plumbing states.
- **Web sidebar `FanDNABlock`** — top 3 taste insights (was trait cards).
- **Web sidebar `IdentityBlock`** — identity noun under the display name
  (was personalityType.label), top insight as the body line.

### 3. Voice sweep across other surfaces

- **Onboarding payoff (`your-profile.tsx`)** — "The app already knows you /
  Built from your ratings... It sharpens with every one" (the guide's DEAD
  example, verbatim) → "Here's what your ratings gave away" / "This is the
  rough sketch. Rate more, and it gets uncomfortably accurate."
- **Onboarding welcome** — "fights you might love" → "Upcoming fights picked
  off your taste, not the crowd's."
- **Home mirror** — rail heading "MORE ABOUT YOU" → "WHAT YOUR RATINGS GAVE
  AWAY"; insight card typography up (16/700 headline, 13 subline, 3-line
  clamp, 260 width). Round-1 structure untouched — Mike hasn't given his
  on-device verdict yet.
- **Web sidebar rec blocks (Rule 8)** — "A good fight you might love" → "You
  missed one" (+ "23 fans agreed." → "Rate it and see where you land.");
  "Upcoming fight you might like" → "Your next fight"; "Fighters you might
  like" → "Your kind of fighters"; "Popular on Good Fights" → "Most followed".
- **Backend rec reasons** — `community.ts`: "Popular on Good Fights" → "Most
  followed" (cold start, no user signal to cite); "Highly rated fighter" →
  "Always in good fights"; "Same weight as X" → "Runs in X's division".

### Verification

- Taste engine test suite: all assertions pass, sample output reads in-voice.
- `tsc --noEmit`: backend, web, mobile — zero errors in touched files (all
  remaining errors pre-date the session: admin JSX config, 4 trait `evidence`
  typings, events/news screens).
- Kill-list grep across backend fanDNA + mobile + web: clean (only doc
  comments reference the banned phrases).
- NOT device-walked (remote session). Next on-device pass should eyeball the
  new fan-dna screens + longer sublines on the mirror rail.

### Follow-ups

- Old trait-engine reveal-modal quips (`traits/hype-accuracy/copy.ts` etc.)
  still speak "the room" and pre-guide voice — separate sweep.
- `personalityType.ts` still computed by `GET /fan-dna/profile` (no consumers
  left on this branch) — candidate for removal at release cleanup.
- Un-hype gate bug from the 07-03 handoff still open (DELETE prediction
  hard-gated while create is soft-capped).
- More identity nouns as new tokens surface (`identityLabel.ts` curated map).

# 2026-06-23

## END-OF-SESSION STATE (read this first)

All work today was on the **card-placement blog article** (still a `docs/` working draft, NOT
published, no DB writes). It went through 4 iterations as Mike gave feedback. Current file state:

| File | What it is | Status |
|---|---|---|
| `card-placement-rankings-article-v3-ppv-split.md` | **NEWEST.** Avg-card-placement scale, split into PPV vs Fight Night lists, activity filter, better stories | the live candidate |
| `card-placement-rankings-article.md` | Single combined slot list ("editing version", left intact per Mike) | superseded by v3 |
| `card-placement-rankings-article-v2-position.md` | Last night's recency-weighted slot draft | dead, delete when Mike confirms |

**Final method (in v3):** average of last 3 card slots (1.0 = main event, simple mean), fighters
split PPV vs Fight Night by majority card type of last 3, ties by recency, activity gate = ≥2 bouts
in 24mo. Marker for inferred division = † (dagger). Em-dash-free.

**Open decisions for next session:** (1) which file becomes canonical; (2) delete the 2 dead drafts;
(3) when ready, convert to a real blog post (`packages/web/src/content/posts/*.md`, `draft: true`,
run `syncBlogPosts.js`, deploy) so it shows in admin Blog > Drafts — see process below.

**Script change shipped:** `card-placement-rankings.ts` gained `--decay <n>` (default 0.6; use 1 for
simple last-3 average) and an activity gate (`MIN_RECENT_FIGHTS=2` / `RECENT_WINDOW_MONTHS=24`) +
recency final tiebreak. Read-only, prod-safe. `historical-placement-analysis.ts` already supported
`--position`.

**Data finding (no fix needed):** Conor's 2.3 all-time avg is CORRECT (includes his 2013 prelim
debut). Raw DB stores some old numbered-PPV orders inverted, but the historical script corrects them
via user-rating signal. Current board unaffected.

---

## Card-placement blog article: finished the rewrite

Continued from `docs/HANDOFF-card-placement-article-2026-06-23.md`. Goal: one article covering
card placement with current + historical pound-for-pound, a "formula" section, and a personality
section, while cutting repetition. Research done the CHEAP way (WebSearch snippets only, no
WebFetch, no subagents) per `docs/daily/2026-06-20.md`.

### What changed in `docs/marketing/card-placement-rankings-article.md`

- **Added current pound-for-pound board** (cross-division by 0-100 score). Top: Makhachev / Du
  Plessis / Holloway / Volkanovski all at 100, then Chimaev & Poirier 97.2, Merab 95.4, Ankalaev
  94.5, etc. Insights: lightweight owns the top (6 in top 15), women cap at 85 (rarely get the
  numbered-PPV main-event multiplier), heavyweight thin up top.
- **Added all-time pound-for-pound** (career avg placement, min 12 fights): Korean Zombie 93.6 tops
  it despite never winning a belt = the thesis from the other side. Conor 89.3 second.
- **Added the "time machine" overall board** for 2020 (Khabib, Whittaker, Cormier, Usman, Holloway,
  Covington, Poirier, Anderson Silva). Honesty note: pre-2015 division labels are too fuzzy to
  split, so history is presented as ONE overall most-featured board (confirmed by running
  `--asof 2015-06-01`, which produced garbage division inference: Conor at WW, Ruan Potts #1 HW).
- **Added "The formula" section** from the in-DB correlation work (done last session): tenure
  (career fights) is the strongest signal, winning second, HOW you win (finishes) barely matters.
  Then pay (prelim $12k/$12k vs champ $500k+ base + PPV points; Jones ~$6.29M 2024 vs $2M base;
  McGregor UFC 229) and attention (follower counts) as the external levers.
- **Added personality section**: Chael (43->96, no belt), Conor (89.3, "we're not here to take
  part, we're here to take over", 46.5M IG), O'Malley (74.7, 9.4M), Paddy (booked #4 / ranked #6,
  4.8M), Colby (33->100 heel turn), Nate Diaz, and Josh Hokit (HW #17 / 51.2, ex-49ers fullback +
  All-American wrestler, ~273K IG, mic-moment crossover story = placed on personality before a
  ranking).
- **Cut repetition**: removed the "Why no wins or losses?" footnote and the per-method defensive
  bullets; the no-wins/losses caveat now appears ONCE in the intro. Dropped the "this was the piece
  my old version never had" / methodology-vs-old-version meta-commentary.
- **Em-dash sweep**: confirmed zero em-dashes (`[[feedback_blog_no_ai_tells]]`).

### Web research (snippets only, 4 WebSearches total)
- Josh Hokit: ex-SF 49ers fullback / Fresno St All-American wrestler, @theincredible_hok ~273K IG.
- IG follower counts: Conor ~46.5M, O'Malley ~9.4M, Adesanya ~8.3M, Paddy ~4.8M.
- UFC pay: prelim debut $12k/$12k, $50k bonuses, champ base $500k+ + PPV points, Jones/McGregor figs.
- Chael "placement is everything" verbatim quote did NOT surface cleanly; kept the existing
  paraphrase (no quote marks) rather than fabricate one. Used the verifiable iconic Conor quote.

### Data scripts re-run (all read-only, prod DB)
- `historical-placement-analysis.ts` (career main-event leaders, career-avg top-billers, rise/fall).
- `card-placement-rankings.ts --all --csv` -> sorted cross-division for P4P.
- `card-placement-rankings.ts --asof 2020-07-01 / 2015-06-01` for the time-machine board + to
  confirm pre-2015 division fuzziness.

### Status
Article complete and saved. Not yet published to the blog / not placed in `blog_highlights`. No code
or DB changes. Marketing-side image assets (ruleset infographics, etc.) remain uncommitted in the
working tree from prior sessions, untouched here.

## Card-placement article: major revision (same day, after Mike feedback)

Mike pulled up his ORIGINAL fightingtomatoes version (web.archive screenshot in `docs/marketing/`)
and gave three directions that reshaped the piece:

1. **Drop the hypothesis framing.** It is not "testing Chael's theory," it is a straight rankings
   report. Removed "So I tested it", "Does it actually work? Yes", "the Sonnen thesis holding up",
   "the insight I'm most excited about", "Bonus: this works as a time machine". Intro now mirrors the
   original's neutral framing (Chael as rationale: "ignore the rankings, mind card placement").
2. **Scale = average card placement (slot), NOT 0-100.** The original used "Avg. Card Placement"
   (1 = main event, lower = higher up). The 0-100 score was an internal index that the script
   converts on a parallel track; it never round-trips. The direct metric Mike wanted ("just average
   the card placement, 1+1+1/3 = 1") is the script's `--position` mode.
3. **Simple average of last 3, like the original** (the original's numbers are 1, 1.3, 1.7 = plain
   means, no recency decay). Added a `--decay` flag to `card-placement-rankings.ts` and ran with
   `--decay 1` to reproduce the simple average. Ties broken by PPV weight (per Mike's pick:
   "slot scale, PPV-aware order"), which the script already does via the score tiebreak.

Regenerated EVERYTHING on the slot scale: all 11 division tables (615 fighters, built from
`p4p-simple.csv` via node to avoid transcription error), the P4P board (joined slot + PPV-score for
tiebreak ordering; 11-way tie at 1.0 ordered by card size, exactly like the original), all-time
career-avg-slot + main-event counts (`historical-placement-analysis.ts --position`), career arcs,
and the 2020 snapshot (`--asof 2020-07-01 --position --decay 1`). Kept the new analysis sections
(all-time, arcs, snapshot, card-vs-rankings, formula, personality) but rewrote them in report tone.
Em-dash swept (0).

## Card-placement article: iteration 3 (activity filter + tiebreak + prose)

Mike feedback round 3:
1. **Activity filter.** Faded/semi-retired names (Kamaru Usman: 1 fight in last 24mo, 2 in 36mo)
   were ranking. Added an activity gate to `card-placement-rankings.ts`: `MIN_RECENT_FIGHTS=2`
   within `RECENT_WINDOW_MONTHS=24` (at least 2 bouts in the last 2 years). Ranked count 615 → 560.
   Probed real fight dates first to calibrate (kept all active champs: DDP 3-in-24, Makhachev 2,
   Adesanya 3, Aspinall 2; dropped Usman 1, Poirier retired). Intro now states the rule.
2. **Tiebreak made meaningful.** The four pure-PPV fighters at 1.0 (Volk/DDP/Makhachev/Holloway)
   all have identical PPV-weighted scores (100), so their order was arbitrary. Added a **recency
   final tiebreak** (most recent fight first) to the sort: slot → PPV weight → recency. Also
   regenerated the score CSV with `--decay 1` so the tiebreak score matches the displayed simple
   average. P4P 1.0 group now reads Holloway, Volkanovski, Makhachev, Du Plessis (by last-fight date).
   Clarified in-article what "bigger cards" means (numbered PPV vs Fight Night billing, NOT buys).
3. Removed the "wins and losses are left out / champion-check / nobody tells the formula" block.
4. Women's P4P sentence reworded per Mike's exact text.

Regenerated all division tables (560 fighters) + P4P + reassembled. Em-dash swept (0). Lightweight
depth bullet updated (Poirier filtered out). `_activity_probe.ts` scratch script removed.

## Card-placement article: iteration 4 (NEW FILE, PPV/Fight-Night split)

New file (editing version left intact per Mike): `card-placement-rankings-article-v3-ppv-split.md`.

1. **Two-list split.** The single board mixed PPV and Fight Night main-eventers, so a Fight Night
   headliner (Manel Kape, slot 1.0) sat above PPV co-mains, which looked wrong. Split into two
   boards: **pay-per-view fighters (134)** and **Fight Night fighters (426)**, classifying each by
   the majority card type of their last 3 bouts (tie → most recent). Divisions split the same way.
   This restores champions to the top of their own PPV list (e.g. Pantoja now leads PPV flyweights
   instead of sitting behind Kape). Generated by parsing `position-simple.txt` markers (P/n/o).
2. **Conor 2.3 data question — investigated, data is CORRECT.** Raw DB stores several of his
   numbered-PPV main events upside down (UFC 189/202/205/246/264 at order 11-12), BUT the historical
   script DOES correct them via real user-rating signal (confirmed: Fight.totalRatings == real
   FightRating counts on those events, and the rating-monotonicity fallback fires). His 2.3 career
   avg is right: it includes his 2013 prelim debut + 2 undercard fights before he started
   headlining. (11×1 + 10 + 8 + 3)/14 = 2.29. Added an in-article note explaining this. The CURRENT
   board is unaffected (recent PPVs stored correctly; champs sit at 1.0).
3. **Better fighter stories.** Removed the weak "Paddy booked like a #4" line. Pulled richer
   over/under-featured cases from `divergence-analysis.ts`: Miesha Tate (#15→card #4, faded champ on
   name), Natalia Silva (#1 WFLY, unbeaten, kept mid-card), Tatiana Suarez (#2 SW, avoided, rarely
   featured), alongside Evloev/Adesanya/Lewis.

Inferred-division marker switched from `*` to **†** (the `\*` escaping kept getting eaten by the
shell; dagger has no markdown conflict). Em-dash swept (0).

### Heads-up for next session
- TWO older drafts now redundant: `...-v2-position.md` (last night's) AND the single-list slot
  version is the "editing version" `card-placement-rankings-article.md`. The v3 PPV-split is the
  newest. Confirm with Mike which becomes canonical before publishing; delete the dead ones.
- `card-placement-rankings-article-v2-position.md` (last night's avg-slot draft) is now redundant
  with the main article and lacks the new sections. Consider deleting it to avoid the same
  which-file-am-I-reading confusion that came up this session.
- Script change: `card-placement-rankings.ts` now takes `--decay <n>` (default 0.6; pass 1 for the
  simple last-3 average used by the published article). Read-only, prod-safe.

# 2026-04-06

## What I Worked On
- [x] Notification deep links simplified (mobile)
- [x] Live events screen performance fixes (mobile)
- [x] iOS OTA update pushed (infra)
- [x] Landing page full redesign (landing)
- [x] Documentation system created (docs)

## Changes Made

### Mobile: Notification deep links simplified
- All notification taps now go to Live Events tab instead of routing to individual screens
- File: `packages/mobile/components/NotificationHandler.tsx`
- `preEventReport` type left as-is (has special AsyncStorage logic)

### Mobile: Live events screen performance
- Show content after first page loads (not all pages)
- Changed `invalidateQueries` to `refetchQueries` on tab focus (no more spinner on tab switch)
- Increased page size from 2 to 5, reduced auto-fetch from 4 pages to 2
- Net result: 4 sequential API calls -> 1 call before content shows

### Infra: iOS OTA update
- Confirmed all ~30 commits since Feb 27 are JS-only, no native build needed
- Reverted `app.json` from 2.0.2 back to 2.0.1 for OTA compatibility
- Pushed OTA update to `production` branch

### Landing: Full redesign of goodfights.app
- Expanded from simple logo + 2 buttons to full marketing landing page
- Added: hero with phone mockups, stats bar, feature grid, feature deep-dives, promo logos, how-it-works, community section, final CTA
- Added 7 real app screenshots, 16 promo logos
- Replaced sticky nav with floating download button
- Used vertical full logo (hand + wordmark) as hero
- Removed web app browse section (not launching web yet)
- Updated stats from DB: 13,000+ fights, 66,000+ ratings, 1,400+ events
- Commits: `73cd634`, `6bac82e`, `c4cd626`, `9ae7cf2`, `fde5503`, `efe6b6a`

### Docs: Documentation system
- Created `docs/` with `daily/`, `areas/`, `decisions/` structure
- Seeded area docs for landing, mobile, backend, web, scrapers, infra

## Unfinished / Next Time
- Hero background image (`hero-bg.jpg`) still not added to landing page
- Consider adding a `hero-bg.jpg` dramatic ring/octagon shot
- Backend N+1 problem in notification reasons query (not urgent)

## Notes
- DB has 66,512 fight ratings, 1,531 hype predictions, 777 reviews, 54 comments
- Android `eas submit` still broken — must upload `.aab` manually

# 2026-04-24

## What I Worked On
- [x] Add pull-to-refresh to all five main tab screens
- [x] Confirm live events screen self-updates periodically
- [x] Ship the pre-existing Profile work (spoiler-free toggle + notifications UI) that had been sitting unstaged

## Changes Made

### Pull-to-refresh on all main tabs (commit `e3a5417`)
Added a `RefreshControl` to every main tab screen. Each refresh re-runs the tab's primary data query; React Query's `refetch()` is awaited so the spinner stays visible until the network round-trip completes.

Files touched:
- `packages/mobile/app/(tabs)/live-events.tsx` — FlatList, wraps the `['upcomingEvents', ...]` infinite query
- `packages/mobile/app/(tabs)/events/index.tsx` — FlatList, same `['upcomingEvents', ...]` query
- `packages/mobile/app/(tabs)/past-events/index.tsx` — FlatList, `['pastEvents', ...]` query
- `packages/mobile/app/(tabs)/top-fights/index.tsx` — FlatList, `['topRecentFights', ...]` query
- `packages/mobile/app/(tabs)/profile.tsx` — ScrollView; refresh fans out to `refreshUserData`, `getPredictionAccuracyByEvent`, `getGlobalStanding`, `getMyTopReviews`, `getMyTopPreflightComments`

All use the same shape: `const [isRefreshing, setIsRefreshing] = useState(false)` + an async `onRefresh` callback that awaits the refetch. `tintColor`/`colors` both set to `colors.primary` so the spinner matches the brand yellow.

### Live events periodic self-update
Live Events already polled every 30s via `refetchInterval: 30000` on the `['upcomingEvents', ...]` infinite query — it was working before. Added `refetchIntervalInBackground: false` explicitly so we don't burn requests when the app is backgrounded (default behavior anyway, but now it's intentional).

### Profile: spoiler-free toggle + notifications UI (commit `64c726f`)
Profile.tsx had substantial unstaged work from a prior session that I surfaced and shipped as its own commit rather than bundling under the pull-to-refresh message:
- **Spoiler-Free Mode switch** wired to `SpoilerFreeContext`, placed under a new "PREFERENCES" group. (Per MEMORY, the context and AsyncStorage persistence already existed; this just exposes the toggle on Profile in addition to Edit Profile.)
- **Notifications section** with a permission banner that either prompts for OS permission or deep-links into device settings when denied, plus a master toggle that calls `apiService.updateNotificationPreferences` and invalidates `fights`, `fight`, `fighterFights`, `eventFights`, `topUpcomingFights` query caches so the UI reflects the new preference without a reload.

### Commit split mechanics
To separate the two concerns cleanly, backed up the working copy of profile.tsx to `/tmp`, `git checkout HEAD -- profile.tsx`, re-applied only the pull-to-refresh hunks, committed. Then restored the full file and committed the remainder. Both commits pushed to `origin/main`.

### Event screens load-time fixes (commits `a7d2d8e`, `462b178`)
User reported Upcoming/Live Events screens taking ~10s to load on app start, target ~3s. Investigated mobile + backend; root causes were compounding.

Mobile-side quick wins (commit `a7d2d8e`):
- `packages/mobile/app/(tabs)/events/index.tsx` — swapped `queryClient.invalidateQueries` for `refetchQueries({ type: 'active', stale: true })` on tab focus. Quick tab switches within the 30s staleTime now reuse the cache instead of hitting the network.
- Same file — `refetchOnMount: 'always'` → `refetchOnMount: true`. Mount now respects staleTime too.
- `packages/mobile/app/(tabs)/live-events.tsx` — removed the auto-fetch-page-2 `useEffect` that doubled the round trips on every startup. Page 2 now only loads via pull-to-refresh or scroll.

Backend cold-start fix (commit `462b178`):
- Render free tier spins down after ~15 min idle, adding 3–5s to the first load. Added `.github/workflows/keep-backend-warm.yml` — pings `/health` every 10 min via GH Actions cron. The `/health` endpoint runs `SELECT 1` so the DB connection pool stays warm in the same request.
- Manual test run confirmed HTTP 200 in <1s with `database: connected`.
- Note: the first commit was rejected by GitHub because the local PAT lacked the `workflow` scope. User updated the token, then the push succeeded.

Not fixed yet (documented for later):
- Backend runs 2 sequential Prisma queries + in-memory sort in `packages/backend/src/routes/index.ts:406-442` (the upcoming-events endpoint). Adds 300–500ms.
- React Query cache is in-memory only — first launch of the app still has no cache to reuse. Could add the AsyncStorage persister later for instant cold-start loads.

### Notification-reasons N+1 fix (commit `a71a1d7`)
After the mobile + warm-up fixes, startup was down to ~5s from ~10s. The remaining 1–2s on the events endpoint was the per-fight notification lookup — `Promise.all(fightIds.map(getNotificationReasonsForFight))`. Each call did ~4–6 DB round trips (fight lookup + pre-synced matches + active-rules fetch + dynamic eval per unmatched rule), so a 10-fight page was firing ~60 queries in parallel and hammering the Render pool.

Investigation revealed the dynamic-evaluation pass only existed for rule types whose match set can change after rule creation — specifically **hype-based rules** (hype scores drift as predictions roll in). Per user, hype and fighter-follow *notifications* are currently turned off; manual per-fight bells are the only firing notification type. Manual follows pre-sync into `fightNotificationMatch` up-front with an immutable `fightIds` condition, so a plain read of that table is authoritative today.

Changes:
- `packages/backend/src/routes/index.ts:574-612` — events endpoint now does a single `fightNotificationMatch.findMany({ userId, fightId: { in: allFightIds }, isActive: true })` and groups results by `fightId`. Also fixed a subtle regression from the earlier `a7d2d8e` edit: reason `type` is classified by rule-name prefix (`'Manual Fight Follow:'` → manual, `'Fighter Follow:'` → fighter, else rule) the way the original code did, not hardcoded to `'manual'`.
- `packages/backend/src/services/notificationRuleEngine.ts:getNotificationReasonsForFight` — simplified to the same one-query form. Callers in `fights.ts`, `search.ts`, and `notificationRules.ts` get faster for free; output shape preserved.

**Recoverability (user will reintegrate hype/fighter notifications later)**: nothing deleted. `evaluateFightAgainstConditions`, `evaluateUserRulesForFight`, `evaluateRuleAgainstUpcomingFights`, `syncRuleMatches`, `syncAllUserRuleMatches`, all of `notificationRuleHelpers.ts` (`manageFighterNotificationRule`, `managePreEventReportRule`, etc.), and `routes/notificationRules.ts` remain intact. The fighter-follow endpoints in `routes/index.ts:1475,1542,1707` still create Fighter Follow rules on follow. A clear restoration-path comment on `getNotificationReasonsForFight` explains how to re-add the dynamic eval block when hype rules come back.

Why simplification is safe even though creation code still runs: pre-synced matches from fighter follows, pre-event reports, etc. still land in `fightNotificationMatch` the way they always did, so the one-query read surfaces them identically to the old code. The only behavior the simplification drops is dynamic re-evaluation of stale hype-threshold conditions, which are disabled from firing anyway.

Type-check of the two touched files: clean (pre-existing errors in admin UI / old controllers are unrelated).

### BKFC Clearwater start time fix
User reported the Apr 24 BKFC Clearwater event displaying as 12:00 AM in the mobile app; bkfc.com lists 8:00 PM EDT.

Root cause: `event.mainStartTime` in prod was `2026-04-25T04:00:00Z` (= April 25 00:00 EDT, i.e. midnight). The BKFC scraper had picked up "12:00 AM" from a `[data-countdown-date]` rollover element on the event detail page during an earlier production scrape. (Local Mar-27 scrape correctly produced "8:00 PM" — the bug is intermittent and depends on which selector wins.)

Fix:
- One-time DB update on Render: `mainStartTime` set to `2026-04-25T00:00:00Z` (= April 24 8:00 PM EDT) for the BKFC Clearwater event. `date` was already correct.
- Hardened scraper + parser to discard `"12:00 AM"` as a known countdown-rollover artifact:
  - `packages/backend/src/services/scrapeAllBKFCData.js` — both `scrapeEventsList` and `scrapeEventPage` now null out `eventStartTime` when it equals "12:00 AM", letting later strategies / events-list-page values win in the merge.
  - `packages/backend/src/services/bkfcDataParser.ts:parseEventStartTime` — same guard at the parser level. Returns `null` for "12:00 AM" so the upsert passes `mainStartTime: undefined` and preserves the existing correct value rather than overwriting it.

The next 19:00 UTC daily scrape will run with the new code; if BKFC's page state regresses, the existing DB value won't be clobbered.

### EAS Update push (end of session)
User reported pull-to-refresh wasn't showing up on device. Root cause: the code was committed earlier in the session but never shipped via `eas update`, so the store build (v2.0.2, iOS build 19 / Android versionCode 36) was still running the JS bundle from three days ago ("hide index route flash on launch" was the last OTA). Pushed `eas update --branch production` covering both runtimes:
- iOS runtime 2.0.2 — update ID `019dc18d-0919-7d04-bc19-566a94ab2b4a`
- Android runtime 1.0.0 — update ID `019dc18d-0919-71ef-99bd-212b482f252c`

Per EAS Update behavior, two cold restarts are required on device (first to download, second to apply).

### Live-events empty state (end of session)
User reported the Live Events tab showed a proper "No <ORG> events live currently" message when an org filter was active, but went blank when no filter was set. Root cause in `packages/mobile/app/(tabs)/live-events.tsx`: `ListEmptyComponent` returned `null` whenever `isFetching` was true, and with `refetchInterval: 30000` + `refetchOnWindowFocus: true` the query was frequently in a fetching state, so the empty-state text flickered in and out. The initial-load case is already covered by the `isLoading` branch above, so the `isFetching` gate was both redundant and the cause of the blank screen.

Fix:
- Removed the `isFetching` short-circuit in `ListEmptyComponent`.
- Renamed the no-filter empty message to "No live events currently" per user wording (filter-specific variants unchanged).

Shipped `eas update --branch production` covering both runtimes:
- iOS runtime 2.0.2 — update group `18aa8907-d475-4f44-a5ba-0d6231570bdb`
- Android runtime 1.0.0 — update group `65716e7d-2f13-4d26-9878-4a82661b5f01`

## Follow-ups
- Monitor keep-backend-warm workflow for a day to confirm the cron fires reliably. If GH Actions cron drifts badly, consider switching to UptimeRobot.
- Measure startup after `a71a1d7` auto-deploys on Render. Target was ~3s; we were at ~5s before the notification fix.
- Still available if load times remain off: merge the two Prisma queries + in-memory sort in the upcoming-events endpoint (`routes/index.ts:406-442`), and/or add a React Query AsyncStorage persister so cold app starts paint cached data instantly.
- **When reintegrating hype/fighter-follow notifications**: re-add the dynamic rule-evaluation pass to `getNotificationReasonsForFight` (and to the events endpoint's batched-match loop). The restoration notes are in the code. `evaluateFightAgainstConditions` is still exported for exactly this purpose.
- **Habit**: every JS-only mobile change needs an `eas update --branch production` before the user can see it on the store build. Don't close the session with shipped-to-GitHub-only changes.

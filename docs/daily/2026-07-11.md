# 2026-07-11 — Fight day: OKTAGON 91 + RAF Georgia + UFC 329 live-tracker verification

Three events today. UFC 329 (5pm ET early prelims) was verified yesterday. This session verified OKTAGON 91 (11:45am ET) and RAF Georgia (12:00pm ET). All checks read-only.

## OKTAGON 91: ENGIZEK VS. JOTKO 2 (`546f2bac-9874-4e91-a77c-f4292ce3acbc`) — ✅ fully autonomous, proven path

- DB: `scraperType='oktagon'`, UPCOMING, `mainStartTime` 15:45Z (= 11:45am ET, matches broadcast), `sherdogPbpUrl` null, 14 fights (12 active, 2 correctly CANCELLED).
- Lifecycle flips LIVE at 15:45Z and dispatches the **VPS** tracker (`oktagon` is in `VPS_SUPPORTED_SCRAPERS`); GH Actions `oktagon-live-tracker.yml` is the fallback if the VPS dispatch fails, and step 1.5 `/track/check` self-heals every tick.
- VPS healthy: `{"ok":true,"trackers":0}`, ~12 days uptime.
- Handler proven current: last 4 OKTAGON events (May 16 → Jun 24) all COMPLETED via scraper with winners recorded. (GH oktagon workflow hasn't run since March — VPS has owned it since, as designed.)
- `production_scrapers` includes `oktagon` → real writes.

## RAF Georgia (`ee4086c3-c1b3-476a-98c9-4ae81b0d923f`) — ✅ configured correctly, but first-ever real test of the automated path

- DB: `scraperType='raf'`, UPCOMING, `mainStartTime`/`prelimStartTime` 16:00Z (= noon ET), `ufcUrl` = `https://www.realamericanfreestyle.com/events/raf-georgia` (the tracker's required page URL), 14 fights (12 active; the 2 CANCELLED rows are the legitimate opponent changes from the 07-10 cleanup).
- RAF has **no VPS handler** — it runs via GH Actions `raf-live-tracker.yml`, dispatched by Render at the LIVE flip and re-dispatched every lifecycle tick (4-min per-workflow cooldown → ~5-min polling).
- **This path depends on Render's `GITHUB_TOKEN`, which was expired until yesterday** (refreshed + verified end-to-end 2026-07-10). That expiry retroactively explains RAF's bad history: RAF 09 tracker "not actually running", RAF 10 got exactly ONE workflow run (2026-06-14 01:03Z, likely manual) and its fights were completed manually via Yahoo (`manual-yahoo-raf10`). **Today is the first RAF event where the automated dispatch chain has a working token.**
- Dry-ran `scrapeRAFLiveEvent.js` against the raf-georgia page (scrape-only, no DB writes): **12/12 matchups parse and exactly match the DB fight names**, including all of yesterday's rename fixes (Kuat Khamitov, Ernazar Akmataliev, Razambek Zhamalov, Sangeeta Phogat, Brock Hardy). Name-keyed fight matching will resolve.
- `production_scrapers` includes `raf` → real writes, not shadow.
- Known limitations (unchanged from RAF 09 findings, `docs/areas/live-trackers.md`): official RAF page is **result-only** with ~2–5 min lag and no walkout signal, plus ~5-min GH Actions cadence → results land ~5–10 min after each bout; up-next pings lag reality (Yahoo up-next source was never productionized).

## What to watch today

- **~12:05pm ET: confirm a `raf-live-tracker.yml` run appears** (repo `mikeprimak/fighting-tomatoes` → Actions). If the automated dispatch fails again, manual fallback: dispatch the workflow by hand with `event_id=ee4086c3-c1b3-476a-98c9-4ae81b0d923f`.
- OKTAGON should show `"trackers":1` on `http://178.156.231.241:3009/health` shortly after 11:45am ET.

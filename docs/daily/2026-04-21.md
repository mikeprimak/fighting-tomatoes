# 2026-04-21

## What I Worked On
- [x] Revert UFC 328 marketing-demo pin (video shoot finished)

## Changes Made

### Removed `avocadomike@hotmail.com` UFC 328 pin
Yesterday's `58b5d5e` pinned UFC 328 at position 0 of the Upcoming feed for one user so the App Store preview video could open on it. Video is in the can, so the override is no longer needed and has been removed.

**Change:** `packages/backend/src/routes/index.ts` — dropped the `pinEventId` lookup block at the top of the `type === 'upcoming'` branch and the matching `splice`/`unshift` block after the sort. Diff is an exact inverse of `58b5d5e`; file hash returns to the pre-pin `31f64b4`.

Upcoming events now sort the standard way for every user (calendar day → UFC-first within the day → time → id).

**Commit:** `26d56dd` on `claude/connect-good-fights-app-Okcz1` — not yet on `main`. User/Render auto-deploy will pick it up once merged.

## Follow-ups
- [ ] Merge `claude/connect-good-fights-app-Okcz1` → `main` so Render deploys the revert.

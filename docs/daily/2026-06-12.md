# 2026-06-12 — Character backfill campaign COMPLETE (passes a + b)

Branch: **`claude/user-focused-pivot-l8l6mg`**. Overnight/remote session:
resumed and finished pass (b) of the character backfill.

## Pass (b) final run (recap generation, pinned top-2000)

Resumed `backfill-recaps-top-rated.ts --persist` against the pinned snapshot
(`backfill-passb-targets.json`, 2000 fights). At launch 1,878 pinned fights
were still recap-less across 481 events.

- **Swept all 481 events. 1,365 fights written total** (ride-alongs included),
  **805 pinned targets covered this run**, $8.15 spent, **0 failures**,
  0 Anthropic 429s. ~3.5 hr wall clock (1.2s Brave pacing dominates).
- **Campaign totals (DB-verified): 918/2000 pinned fights enriched; 1,082
  residual.** Residuals are fights whose cards got processed but whose
  editorial sources didn't mention them (mostly old/prelim bouts) — the
  confidence floor correctly held back fabrication. Re-running the script is
  now a loud per-event no-op for covered cards; the residual is a source
  problem, not a resume problem.
- Source-fetch noise during the run: 74× 403 (mmaweekly etc.), 19× 404,
  1× Brave 422 (**query >400 chars** on "UFC TUF 24 Finale" — long event name
  + many fighters; clamp the query builder if we chase residuals).
- Log: `packages/backend/passb-backfill.log`.

## Pass (a) confirmed complete

DB check: **all 4,189 recap-bearing fights have `aiPostFightTags.character`**
(pass (a) residual = 0; pass (b) writes get tags at write time from the same
extractor). **Total character-tagged fights: 4,200.**

## State for the taste engine

The dataset the insight engine reads is now ~5.4× the pilot (778 → 4,200
character-tagged fights).

## Post-backfill profile re-run (same day, remote session)

Re-ran `taste-profile-run.ts` (read-only) for all four accounts on the
enriched dataset. Outputs saved as `packages/backend/taste-profile-*-2026-06-12.out`
(untracked run artifacts).

**avocadomike** (the pilot comparison):
- Character coverage on his rated fights: **778 → 1,005 of 1,580 (49% → 64%)**.
  The remaining ~575 are his low-engagement prelim ratings — the same
  population as the pass-(b) residual.
- Insights above the score floor: **15 → 27**. The 12 new distinct stories are
  exactly what thin data couldn't support: durability, drama, heart, cardio,
  star-is-born, changing-of-the-guard, gatekeeper community-high, fight-IQ
  community-high, strikes-to-submission, final-seconds finishes, tactical-pace
  community-high, controversial community-high.
- Pilot's confirmed-real insights all survived with more evidence: tension-
  watcher cluster still #1 (now [0.60] "You never stop waiting for the
  finish", 65 fights), wars +1.5/47 unchanged, knockout power 8.3 across
  323 (was 8.4/256), first-exchange finishes, cold-on-women's, polarizing
  fighters (McGregor/Adesanya). Top score compressed 0.91 → 0.60 — larger
  community baselines tightening the gap math, ordering unchanged.
- Nothing got worse: no banal entries surfaced, no grammar issues, cluster
  dedupe held (one tension-watcher line, zero redundant letdown variants).

**Small accounts behave as designed (floors silence noise):**
- michaelsprimak (52 ratings): 1 insight — "tough crowd for decisions"
  (6.8 vs 7.8 baseline, 25 fights). Real and defensible.
- test@goodfights.app (23 ratings): 8 insights, all one coherent story — a
  knockout/violence skeptic vs the community. Coherence at 23 ratings is a
  strong empty-room result.
- testdev2 (5 ratings): 0 insights. Correct silence.

**Verdict: the $8.15 backfill bought what we hoped.** Coverage where his
ratings actually live, ~2× the insight bench, zero quality regressions.

## Decisions

- **Pass-(b) residual (1,082 fights) ACCEPTED as floor** — Mike, 2026-06-12.
  No tier-2 source-chasing. (The >400-char Brave query clamp is still worth
  doing if the script is ever reused.)
- **No `main` merges in either direction until Mike explicitly directs** —
  this is a long-lived branch (multi-day, possibly weeks). All work is branch
  work. Memory: `project-user-focused-pivot-branch-no-merge`.

## Next session candidates

1. Fighter re-enrichment (prod aiProfile rows predate styleArchetype/
   fighterAppeals — persona is the only fighter token live), per the
   2026-06-11 roadmap.
2. Surface work: home above-the-fold insight render → onboarding (the
   rate → instant-insight Saturday-night moment).
